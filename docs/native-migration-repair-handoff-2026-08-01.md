# Native 迁移修复 — 2026-08-01 交接

> **给下一个 session 的。** 读完这份就能开工，不必重推我这轮推过的结论。
> 上一份是 `docs/native-migration-repair-handoff-2026-07-31.md`（**它的数字与「下一步顺序」已全部作废**，
> 只留作追溯）。范围与裁定看 `docs/native-migration-repair-scope.md`。
>
> **⚠⚠ 别从这里开工 —— 入口已经换了。**
> 当前入口是 **`docs/handoff-2026-08-01-round8.md`**（短、只讲"现状 / 下一步 / 别踩什么"）。
> **本文件从此只作追溯用**：它按时间顺序保留了 §1–§15 的推理过程与证据，
> 需要查某条结论怎么来的时候再翻。本轮（第八轮）的完整记录是 **§14 / §15**。
>
> 前面各段末尾各有自己的「下一步」，**那些都已过期**（照 §4 / §8 / §9 / §13.4 开工会做到已完成的事）。

## 0. 起手三件事

1. **确认四个 canonical checkout 都在默认分支**：`scripts/task-preflight`（只读、不自愈，非零退出就停手）。
2. **Rust 工具链不在默认 PATH**：
   `export PATH="$HOME/.rustup/toolchains/1.97.1-aarch64-apple-darwin/bin:$PATH"`
3. **别信正文里的计数**（这份也一样）——`openspec list` 才是当下真相。

---

## 1. 这一轮的成果：199 → **86**，且**九条会假成功的全修了**

> **收尾状态（写于本轮结束）**：五条线全部集成进 `origin/master`，最后一次全量 **2899 通过 / 0 失败**。
> 四个 canonical checkout 都停在默认分支；三个残留开发区（拟人化 / FB 残余对齐 / 会话守卫）
> **零领先主干、零未提交**，是干净的可复用状态，不是孤儿。

「少掉 98」里只有一部分是真做完，其余是账本搬家。分清楚很重要：

| 去向 | 项数 | 真做完了吗 |
| --- | --- | --- |
| 真机验收 → 移进 `docs/real-machine-acceptance-backlog.md` | 66 | ❌ 账挪走，验收诉求一条没少 |
| 已弃守 → 划掉（10 条旧的 + 3 条打包类） | 13 | ❌ 定论早下了，只是没人划 |
| 云端编排缺陷 → 摘出另立 | 7 | ❌ 跟 Rust 迁移无关 |
| **真做完** | **约 12** | ✅ |

### 1.1 落地的 sha（edge，均已在 origin/master）

| sha | 内容 |
| --- | --- |
| `30f4d9a` + `a2edae8` | **产物门禁**：密钥四份收敛成一份 + 解码断言进门禁；打包态校验接受源码摘要（拆掉「下一个抬版本号的人会炸打包」那颗雷）；打包工具链无条件补齐声明的组件；两处契约夹具接上真牙齿；边缘仓 CLAUDE.md 新增「Native 页面引擎」一节 |
| `0a5167e` 等 4 个 | **运行时契约**：预算单一事实源（3.2）/ 会话收尾移进 finally（5.1）/ 焦点守卫诚实归因（7.4），外加集成前抓出并修掉的三条测试问题 |
| `874de95` | **同意闸可观测性** |
| `559d6dc` | **重连绑定准入实例** |
| `52a2110` | **判据三态化**：两个共享「已生效」判据合并成三态（生效 / 未生效 / **读不到**），读不到绝不塌成未生效 |
| `559d6dc` | **重连绑定准入实例**：重连前实读浏览器身份并比对基线；顺带修掉命令位释放的空转 |
| `874de95` | **同意闸可观测性**：「点过但读不回来」与「压根没探到」在回执上可分 |

控制仓（`aidcp`）本轮提交较多，逐条 sha 在各 change 的 `tasks.md` 里；重点是
`4d3afde5`（更早）之后的一串 docs / openspec 提交。

### 1.2 修掉的九条「说成功、实际没发生」

| 缺陷 | 改前 | 改后 |
| --- | --- | --- |
| **重连认错浏览器**（整批唯一会张冠李戴） | 只按端口 + 平台域名挑目标，同机多开时端口被回收复用 → 可能附着到别人的分身，之后动作全记到别人账下 | 重连时实读浏览器自报身份并比对准入基线；对不上拒绝，**没基线在碰端点之前就拒绝** |
| **命令位释放是空转的** | 「超时即释放」只在命令返回时触发，而重连后的重试**无超时包裹、永不返回** → 位子永占、后续全被顶回、无人能救 | 重试纳入剩余预算包裹，释放才真成立 |
| **发布选项设「关」** | 找不到开关就兜底成整行 div → 读不出状态 → 「否 === 否」→ 回「本来就是关的、成功」**连点都不点** | 删掉兜底；只有读到明确状态且等于期望才早退，读不到一律去点 |
| **设为封面** | 往上找容器停在轮播「当前显示」层 → 判据恒真 → 连按钮都不点 | 有界回溯只认真正的图片项容器，证据须封面专属 |
| **搜索筛选** | 误判后既不点、也进不了未确认分支 → 把**未筛选**结果当筛过的返回 | 有界轮询确认，确认不上如实报未确认且**不返回卡片** |
| **评论输入框** | 判据里硬写「或者是评论框」＝ 无条件回成功 | 改读焦点落位 |
| **评论点赞 / 兼容步骤** | 类名宽松子串，否定形（`not-selected` / `unliked`）也被读成正证据 | 改读图标状态位 + 有界轮询等翻转 |
| **同意闸** | 「点过接受但复探读不回来」与「压根没探到」回执上一模一样 | 两者可分；不确定就停手，不拿「不知道」冒充「已清掉」 |

### 1.3 归因验证的做法（**照抄，别只看「改完全绿」**）

每条都验证了「把修复还原后用例会不会红」：

| 线 | 变异 | 结果 |
| --- | --- | --- |
| 判据三态化 | 被测分片还原成改动前 | 14 条新用例 **11 条转红** |
| 重连身份 | 身份闸换回老判据 | 核心用例**当场转红**，且确认输出里有 `Compiling` |
| 同意闸 | 「点过+复探失败→停手」改回照旧放行 | 关键用例**转红** |

**`Compiling` 那句不是废话**：Rust 变异检验最容易踩的坑是还原后 cargo 跳过重编、**测的是突变前的二进制**，
于是「红了」或「绿了」都不作数。用 `git checkout` / 直接改文件（会更新 mtime），并确认输出里有 `Compiling`。

---

## 2. 本轮定下的裁定（**别再重新讨论**）

### 2.1 真机验收项**全部移出** change 的 tasks.md

用户原话「直接划掉，不要在里面了」。66 项统一收拢到 `docs/real-machine-acceptance-backlog.md`：

- **簇 122 / 123**（既有）——小红书会话守卫 + 写动作拟人化；123 本次补登 123.24–123.28
- **簇 124**（新）——重连绑定 / 同机多环境端口回收。**需要同机并行开两个以上环境，单环境跑不出来**
- **簇 125**（新）——小红书 Native 切换：只读矩阵与写动作
- **簇 126**（新）——Facebook 残余对齐
- **簇 127**（新→**已废弃**）——见 2.3

**新默认：真机项一律只活在 backlog，tasks.md 里换成指向簇号的说明。**

**搬运时三件必查的**（删条目容易、丢线索难）：
① **夹带的实装分支**——不少真机项写成「若真机确认 X，则须实装 Y」，那个 Y 必须跟着搬进簇正文；
② **别处写死的收口条数**——如「归档前须收口 9 条」，若其中含被移走的真机项，数字不改会让该 change 被永远满足不了的条件卡死（本轮已把产物门禁 10.4 从 9 改成 7）；
③ **立论已随弃守失效的别登记**——所测行为的实装若已显式弃守，该真机项应直接丢弃而非搬运。

### 2.2 打包：不为这批专门打包，但**包在持续更新**

**这条一度被我理解错，订正后才对**：用户说「不打客户端安装包」，**不是**「包不更新」——
他一直在手动打包测试其他功能。真正的意思是**不为这批工作产生任何「去打个包验一下」的待办**。

两个直接后果，别搞混：

- **需要「装了新包的运营机」的簇（122 / 123 / 125 / 126）不是无限期挂起** ——
  这批修复会随下一次出包自然进运营机，那些真机项届时可验。
- **产物门禁 5.5 一度被误判弃守、已订正回待办** —— 它是**打包态的代码路径、每次出包都跑到**，不是验收活。
  且它挂着就是一颗雷：那处校验硬性要求清单版本号等于 1，所以前置任务只敢加字段不敢抬版本，
  **下一个动版本号的人会炸打包、没有任何东西提前警告他**。

### 2.3 簇 127 废弃（打包链本身的验证）

它验的是「打包这件事本身」（产物校验流程 / 跨平台资源解析 / CI 编译器版本），
跟用户日常打包想测的东西无关；Windows 交叉打包与 CI 日志更是他根本不走的路径。

**连带作废一条立论**：跨平台打包那批弃守此前留了一条真机项当「复核依据」（万一发现不是响亮失败而是静默发错包就推翻）。
不打跨平台包 ⇒ 复核不会发生 ⇒ **产物门禁 9.1–9.3 的弃守就此成为终局**，不再是「待复核的弃守」。

### 2.4 云端编排缺陷摘出（7 项）

判据只有一条：**Rust 迁移碰过它没有。**

摘出的落到 `docs/cloud-orchestration-residuals-descoped-2026-07-31.md`，分两组：
**A. Facebook 迁移闩的生命周期**（超时清理只武装了免审强制评论那一支、闩的消费准入无相关性判据、支线超时与闩各清一半）；
**B. 评论提交预算的两份公式**（云端一份、边缘一份）。

**摘出 ≠ 弃守**：零开工，缺陷仍在、立论仍成立，只是换了账本归属，待正式立项。**建议分成两个 change，别合成一个。**

**留在原 change 的是另一种**：边缘改了、云端不跟就白改（如 FB 残余对齐 6.1 —— 边缘的导航用途开帖已就绪，只差云端把可导航地址带上）。

**摘出时订正了一处台账错误**：原抬头写「§8 与 3.2 是同一件事的两半」——**不准确**。
§3 是**提交窗口**（引擎向宿主要写窗口时长，纯边缘内部）；§8 是**评论提交预算**（云端判定方等待窗口 vs 边缘执行方命令预算，跨仓）。
不同常量、不同文件，只是主题相近。

### 2.5 三条今日新裁定

| 项 | 裁定 |
| --- | --- |
| **迁移主线 3.2**（Port DOM-first locating…） | **整条显式弃守**（不是收窄）。弃守的是「本 change 继续交付这件事」，不是抹掉已做的。归档边界见该条注释——**规格只能写「已实现」那一列** |
| **会话守卫 5.6**（重连后重注入节奏快照） | **走法乙：正式声明不再需要。** 重连沿用边缘手上那份；只在环境**首次启动**时向云端取一次。理由与一处边界（换号后兜底窗口）已写进台账 |
| **拟人化 7.17**（可替换的缝消失了） | **结案：裁定前提已被代码推翻，无需裁定。** 见下 |

**7.17 为什么不用裁**（这条我实读代码核过，值得记住）：那两个接口**没消失，是换了形态重建了**——
「页面来源」那半是 `test/native-page-engine/xhs-dom-fixture.ts`（jsdom 直接加载分片源码，**可见性与几何是真两态**）；
「执行层」那半是 `native/page-engine/src/locating.rs` 的三段式接口，测试目录里有配套桩。
**脱机断言 27 条在跑**（`tests/locating_gates.rs`），判定是三态且明写「读不出来绝不当成成功」。
两个待选走法今天都不成立：重建——已建好；接受退化——不用退化。

**但别把真缺口一起结掉**：三道闸在生产上**只接了一条真实命令**（Facebook Reels 面点赞）。
其余命令面未接入。这是**覆盖面工作、不是架构工作**（缝与桩都现成），须另立 change。

---

## 3. 剩余工作的构成（86 项）

| 性质 | 约数 | 说明 |
| --- | --- | --- |
| 真要写代码 | 25 | 见 §4 |
| 台账 / 登记 / 追认 | 28 | 回填 sha、登记范围外项、追认跨属主改动 |
| 跑门禁 / 跑校验 | 13 | 机械动作，攒批 |
| 等裁定 | 1 | FB 残余对齐 10.7（多个同文案接受按钮：取首个 vs 不唯一即停手） |
| 其余（部分完成 / 说明性） | 34 | |

**没有一项是「会假成功」的了** —— 剩下的失败方向都诚实，宁可报失败。

---

## 4. 建议的下一步顺序

1. **会话守卫收口**（只剩 2 项：1.6 真活 + 7.5 校验）—— **七条里最接近归档的一条**。
   1.6 = 小红书检出阻断后本地暂停普通浏览下发、清除后恢复；被暂停期间的浏览命令回**诚实的未开始**，
   不得静默丢弃。⚠️ 参照实现里「缺等待循环的三个出口，接管须抛出不可只 return，否则闭环死锁」。
2. **拟人化线**（27 项，最大一块，但真代码活只有约 9 项）：
   - **两条现在可以直接收**：5.3 与 1.6 卡在「实现点在别人单写区」，而 `52a2110` 已经改了那个文件
     **且正好修了尝试计数语义**（有界重试上限 3、attempts 如实回报、升级只在打满仍未确认时出现）。
     去核一下能不能勾。
   - 真代码活：指针走廊（2.6）、按下后才置位的取消要报「已派发、结果待定」（2.8）、
     取消信号与绝对截止透传进对齐循环（3.3）、小红书全线文本输入改硬件级逐字（8.1）、H.1 / H.2。
   - 登记类 7 项 + 验证仪式 8 项，攒批。
3. **另立 change：三道闸推广到其余命令面**（见 2.5 末段）。
4. **另立 change ×2：云端编排那 7 项**（见 2.4）。
5. **攒批收尾**：28 项台账 + 13 项仪式一次做完。

---

## 5. 别再踩的坑（★ 是本轮新踩的）

- ★ **别让三个 Rust 编译同时跑。** 本轮开三条并行线，三个 agent **全部被看门狗判定卡死**——
  机器资源被 cargo 挤爆。工作没丢（改动都在，只是没提交），但要人接手补格式化、补验证、补提交。
  **并行的粒度放在「写代码」上，编译与测试串行。**
- ★ **控制仓提交必须精确到路径。** 本仓长期有 5–8 个 session 并发编辑，
  **`git add -A` / `git add openspec/` 会把别人正在编辑、还没写完的文件一起提交走**。
  本轮实际发生过：我 6 个文件的改动被另一个 session 的全量暂存提交带走，内容没坏但提交说明彻底丢了。
  **救回损失的习惯**：把「为什么」写进**文件本身的注释**（`<!-- 2026-08-01 用户裁定… -->`），不要只写在提交说明里。
- ★ **`scripts/land-change` 认不出带 `codex/` 前缀的分支。** 早先由其他工具建的 worktree 分支名是
  `codex/<change-name>`，集成脚本按不带前缀的名字找，会报「分支不存在」。
  **但它此时已经做完 rebase 了** —— 手动收尾即可：确认 `git merge-base --is-ancestor origin/master HEAD`，
  再 `git push origin HEAD:master`，然后同步主 checkout。
- ★ **全量 `npm test` 很慢（本机十几分钟起），但 MUST NOT 因此跳过。**
  本轮它抓到**两条**只有它能抓到的失败，而类型检查 + `gate:native` + `test:acceptance`
  三道网**合起来也漏掉了这两条**。做 3.2 的子代理没跑全量，所以两条都没被发现。
  1. 假引擎还在发那个已从线路上删掉的预算字段，用例还在断言旧的 `min(请求, 事实源)` 语义。
  2. **3.1 建的机械对账用例失去了对账对象** —— 它从引擎源码正则抓预算数字、断言与宿主表逐字相等，
     而 3.2 把引擎侧的数字删光了。
     **⚠️ 这条的正确处置不是删用例。** 它守着两件事，3.2 只让其中一件过期：
     「两边数字不漂移」确实无从谈起了，但「**引擎新增窗口却漏进宿主表**」仍然要守 ——
     宿主表同时是**准入白名单**，标签认不出就否决这次窗口，那条不可逆写入就此失去写保护、
     且**运行期才现形**。已改写成两条新断言（引擎源码里不得再出现预算数字 + 每个标签必须在白名单里），
     并逐条做了变异验证证明它们真会红。
  **教训**：删一条红掉的守护用例之前，先问它到底在守几件事。只让其中一件过期时，
  删掉等于把剩下那件静默放生 —— 而它看起来仍然「有测试覆盖」。
  - **诊断姿势**：别用 `npm test | tail`——管道要等进程结束才吐，看着像卡死。
    `npm test > /tmp/x.log 2>&1` 落盘再看，能实时看到跑到哪、有没有 `✖`。
  - 单文件跑很快（十几秒到一分钟），定位到嫌疑文件后直接点名跑。
- ★ **有一条会拦下集成的既有 flake，别以为是自己改红的。** 会话恢复那条
  （「缓存会话的传输已死时走重建」）在满负载并行跑全量时偶发管道断裂，单跑必过。
  2026-08-01 当日出现两次，两次都用基线对照证明**非当批引入**。
  **⚠️ 但它不只是测试偶发**：同一个竞态在生产上会让「传输已死」被判成「还活着」，
  于是去写一个已死的管道、拿到异常，而不是走进已经写好的重建路径 —— 一个结构性事实被表达成了异常。
  完整登记与建议修法在运行时契约 change 的 §12。**MUST NOT 给它加重试或标成允许失败**，
  那会让唯一的现场证据消失。
- **Rust 变异检验要确认 `Compiling`**（见 1.3）。
- ★ **从控制仓跑 `scripts/land-change`，别在开发区目录里跑**（我今天犯了两次）。
  在 worktree 里 `cd` 之后接着调控制仓的脚本会报 `no such file or directory`，
  而**前面的 `git commit` 已经成功了** —— 看着像集成失败，其实是提交在、集成没跑。用绝对路径最稳。
- **台账行号普遍已漂**，一律按符号名定位；本文件给的行号同样是快照。
- **`land-change` 会跑 `gate:native`**，格式检查不过就不给合并。改 Rust 前先 `cargo fmt`。
- **改动主体是编进 Rust 二进制的引擎与页面分片**，仅 push 对运营机零效果，须重打客户端包才生效
  （用户会在他自己的打包节奏里带上）。

---

## 6. 上线后会看到的变化（**别当成回归**）

这批修复随下一次出包进运营机后：

- **小红书搜索筛选**确认不上时开始如实报未确认 ⇒ **评论支线短期失败率会上升**。
  这是该发生的——今天它在拿未筛选结果冒充按最新排序筛过的，云端看不出来。日志上会很显眼。
- **发布的选项与候选**会开始出现诚实的未确认。这两类在云端是**尽力而为、不阻断发布**，
  **稿子照发、不丢稿**，只是台账不再把没设上的合规声明记成已设置。

## 7. 一条必须知道的单侧依赖

**设为封面 MUST NOT 只改边缘。** 云端硬写不下发该指令、且它不在尽力而为白名单内 ＝ **失败即中止**。

- **今天零影响**：云端从不下发，这段边缘代码在生产上不可达。
- **一旦封面被重新接上而云端不动**：边缘现在会诚实回未确认，失败即中止会让**整帖在提交前中止**——
  危害从「封面选错」升级成「稿子发不出去」。
- **接回封面时云端必须同批做**：要么把它放进尽力而为，要么先在真机上标定出封面锚点（簇 125）。

---

# 8. 第二轮（2026-08-01 下午）—— §4 的前两项已做完

上面 §1–§7 仍然有效（除了 §4 的顺序：前两项已消化）。这一段只写**新发生的事**。

## 8.1 会话守卫已归档

`restore-native-xiaohongshu-session-guards` **43/43 → 已 archive**（控制仓 `04295ae6` 那一串），
四份 delta 并进主 spec，活跃 change 少一条。

收口时抓到**两件原本会被静默带进主 spec 的事**，都不是「跑一遍验收」能发现的：

1. **规格 delta 与实现相反。** 3.3 那条具名偏离（`inputMode` 判据从「确有字符派发」
   改成「回执带取证」）**只改了代码与台账、没改 delta**，delta 里仍写着被推翻的那句 MUST。
   照原样归档 = 主 spec 上多一条实现有意违反的 MUST。
   **没有任何自动闸会拦**：`validate --strict` 只校验 delta 内部结构，从不比对实现。
   **⇒ 新纪律：改判据的具名偏离，MUST 同批改 delta。归档前把每条偏离对着 delta 读一遍。**
2. **一条 MUST 只做了一半。** delta 写「**每一条**高危小红书写动作都要做提交前阻断复检」，
   而评论点赞一直不在受闸表里。当场补掉（edge `bc8aca7`），并把规格正文的判据
   从**枚举**改成**行为判据**（扣配额 / 写风控事实 / 在平台上留下该账号名下的新痕迹），
   枚举只作举例 —— 漏进枚举的写动作是**没设防**，不是被豁免。

归档前另跑了一次主干全量：`aidcp-edge a2edae8` 上 **2900 / 2900 绿**。

## 8.2 拟人化线：1.5 / 2.6 / 3.3 / 5.3 已落（43 → 46+）

代码 `aidcp-edge 307f3a3`，台账 `aidcp cd1261f7`。

- **2.6 指针走廊**：三处浮层提交现在共用同一个出口，`dispatch_facebook_picker_click` 转调它。
  **禁过冲与起点读不读得到无关**——起点缺席仍禁过冲，只是起步点回落通用规则。
- **3.3 取消 / 截止透传**：**这条的危害比原台账记的重**。点赞是**写命令**，宿主对写命令
  **刻意不做**外层取消竞速，所以对齐循环是唯一能看见接管信号的地方；接线前的后果不是
  「让位慢一点」，而是**协调器已叫停、循环照样滚到目标并把赞点出去**。变异实测坐实：
  去掉接线后该用例红在 `effect_phase`，实测值 **`Confirmed`**。
- **1.5**：对齐滚动此前**零假 CDP 覆盖**，现在有了（多帧 + 无单帧等于整段位移 + 滚前先移光标）。
- **5.3**：属主已在 `52a2110` 补做「在文件内修正」，据此勾选。**注意 `page.scroll` 那一支
  仍是 `escalated + attempts:1` 且属主具名裁定「有意未动」**（滚不动是结构性到底）——
  **所以 1.6 的合约测试 MUST NOT 写成「全文件不得出现 escalated + attempts:1」**，
  那会把一条已裁定的正确行为判红。

## 8.3 这一轮学到的（★ 值得进下一份 handoff）

- ★ **变异要问「哪条断言抓住的」，不是「有没有红」。** 3.3 的第一版用例是照**截止**写的，
  跑起来是绿的、看着像做完了；变异之后**仍然全绿** —— 外层的写命令超时把它喂绿了。
  改成按**取消信号**立论才真有判别力。**一条从不曾红过的用例，不构成任何证据。**
- ★ **`git checkout <file>` 会把还没提交的修复一起冲掉。** 做变异检验时用它「还原」，
  还原到的是 HEAD、不是变异前 —— 本轮丢过一次（一行改动，重打即可，但换成一天的活就不是了）。
  **变异与还原都用直接编辑**（顺带满足 mtime 要求，见 §5 的 `Compiling` 那条）。
- ★ **归档前必须把 delta 逐条对着实现读一遍**，见 8.1。

## 8.4 下一步（接着 §4 走，顺序已更新）

> **⚠ 本节前三项里的第 1 项已在 §9「第三轮」做完（1.6 / 2.8 / H.1 落地，8.1 的定时设置已裁定）。
> 最新状态与下一步一律以 §9 为准。**

1. **拟人化线剩下的真代码活**：2.8（按下后才置位的取消要报「已派发、结果待定」——
   实现点在引擎主干的命令层，目前任何带错误的写一律记「未开始」）、
   8.1（小红书全线文本输入改硬件级逐字，最大一块）、H.1 / H.2。
   **1.6 见 8.2 末段的约束**；建议按「经过校验重试循环得出的升级结论必须带实测次数」立论，
   并把滚动那支作具名例外。**顺带一件现成的**：兼容路径的 **input 支**（回读不一致 → 重试到顶 →
   `escalated`）**目前零测试覆盖**，1.6 落这里最省事又不与属主的用例重复。
2. **另立 change：三道闸推广到其余命令面**（见 2.5 末段）。
3. **另立 change ×2：云端编排那 7 项**（见 2.4）。
4. **攒批收尾**：台账 + 仪式类一次做完。

---

# 9. 第三轮（2026-08-01 晚）—— §8.4 的第 1 项做完

`aidcp-edge d7dc24b`（已在 origin/master，worktree 已清理）+ 控制仓台账。
拟人化线 **46/69 → 49/69**。§1–§8 仍然有效，这一段只写**新发生的事**。

## 9.1 落地的四件

| 项 | 结论 |
| --- | --- |
| **H.1 评论到达确认** | **两半都补回来了**：结构必要条件（编辑器已被平台清空）+ 有界轮询（8 轮 × 250ms）。另主动做了第三件：**把与编辑器有包含关系的元素剔出扫描面** |
| **1.6 升级次数合约** | 落成 9 条用例的新文件，被测文件一字未改。**立论被具名改写**，见下 |
| **2.8 已派发失败** | 任务前提查实**是错的**；但顺着这条线查出并修掉一个**真缺口**，见下 |
| **8.1 定时设置** | 用户裁定：**保持 setter、具名收窄**，从 8.1 覆盖面里显式划出。8.1 仍剩两个消费点未落 |

### H.1 那条自证循环，值得看一眼是怎么成立的

判据本来只有一条宽松子串扫描：详情页内任一 class 含 `comment` 的元素，整段文本包含本次正文即判确认。
**而我们自己刚写进去的正文就在页面上** —— 富文本编辑器的 `textContent` 是活的。
编辑器但凡落在某个这样的容器里（**或某个这样的容器把它包在内** —— 后者才是真正的泄漏口，
`text(祖先)` 会把编辑器那份一起收进来），扫描读到的就是自己写的东西，从此恒真：
**一条根本没发出去的评论会被回报成已确认。**
交接原文说「今天挡住它的只是一个没有任何用例断言过的 DOM 嵌套巧合」—— 这句话是准确的。

修法不是调阈值，是把两条**独立**证据都要回来。剔除污染源只剔「与编辑器有包含关系」的那些，
细粒度评论条目与编辑器互不包含，证据本身不受影响。

### 2.8：任务注释里的前提早就不成立了

原注释写「命令层仍把任何带错误的写一律记成『未开始』」。实读代码：命令层从
`aidcp-edge 746fdcd`（2026-07-26）起就是「写命令 + 错误码不在『未开始』小名单里 ⇒ 不确定」，
**比那条登记还早三天**。也就是说「云端读到的仍是未开始」这句**从来没有对过**。

★ **教训**：登记「未落」之前也要实读一遍。上一波的印象会过期，而过期的登记会让下一个人
去修一件已经修好的事 —— 或者更糟，绕开真正的缺口。

**真缺口在旁边**：命令层把一小撮错误码当成「未开始 ＝ 上游可安全重投」（取消 / 提交窗口不可用），
而指针出口在转译「按下已派发」的失败时**原样继承底层 CDP 错误码**。两者一撞，一次已经派发出去的
提交就可能戴上「可安全重投」的帽子 —— 错误文案写得再明确也没用，云端读的是相位字段。
修法：那张码表提成**单一事实源**，命令层的相位映射与指针出口的转译**都按引用取用**
（同一判断落在两处实现里迟早分叉），已派发的失败碰上表内的码就改带一个可诊断的 CDP 码。

## 9.2 这一轮的方法论收获

- ★ **「不存在 X」这类合约，先去问 X 有没有正当实例。** 1.6 字面要的是「生产路径不存在
  『升级 + 次数为 1』」，而滚动步就是这个组合、且属主已具名裁定正确（那一支根本没有校验重试循环，
  1 就是它真实的尝试次数）。照字面写会把一条正确行为判红。改成
  **「凡是经过校验重试循环得出的升级结论，回报的次数必须是实测出来的」**，滚动步作具名例外并
  正面钉住它的形状 —— 例外要有用例，否则下一个人会「顺手统一」掉它。
- ★ **合约测试要带一条「完整性腿」。** 1.6 只驱动四条点击步 + 输入步的话，新增一条白名单步骤
  而不给它写用例时，整套用例仍然全绿、合约却在新分支上静默缺席。加了一条：从被测源码解析白名单
  键集合、与驱动集合逐条对齐。**并且单独验了「正则失配时它必须响亮失败」** —— 那正是 §9.2 记的
  「切出空串 ⇒ 空集 == 空集恒真」那类陷阱，不验一次就不知道自己有没有踩上。
- ★ **反方向的变异也要做。** 「attempts 硬写成 1」和「attempts 硬写成上限」是同一种病的两个方向，
  只验前者的话，把次数钉死在 3 的实现照样全绿。
- **本轮 5 + 3 + 2 共 10 次变异逐条实测，Rust 那几次运行输出里都有 `Compiling`**（§5 那条仍然管用）。

## 9.3 全量与既有 flake

- 主干全量在本树上跑了两次：**第一次 2911 例 1 红、第二次 2911 例 0 红**。
  红的那条正是 §5 点名的既有 flake（会话恢复「缓存会话的传输已死时走重建」，满负载下 EPIPE）。
  同日在**干净主干**上跑基线是 0 红，单跑该文件必过。**照 §5 的指示没有动它**
  （加重试或标允许失败会让唯一的现场证据消失）。
- `cargo test --locked` / `cargo fmt --check` / `clippy -D warnings` / `gate:native` /
  `typecheck` / `test:acceptance`（31 例）全绿。

## 9.4 下一步

> **⚠ 第 1 项里的「话题」已在 §10 做完。最新状态以 §10 为准。**

1. **8.1 剩下的两个消费点**（定时设置已划出，不再算）：**话题 / 提及候选**（需一套「候选项定位 +
   点击 + 回读」的分片能力）、**遗留步骤路径的输入步**（结构上必须把整条步骤循环搬进引擎，
   不能只截一个步骤 —— 这条门槛比听起来高）。
2. **H.2 仍然阻塞、别硬做**：它要的是「降级痕迹到得了人眼前」，而那条通路整体不存在
   （§9.3 弃守项②，引擎子进程的错误输出没有任何消费端）。须单独起 change。
3. **另立 change：三道闸推广到其余命令面**（见 2.5 末段）。
4. **另立 change ×2：云端编排那 7 项**（见 2.4）。
5. **攒批收尾**：台账 + 仪式类一次做完（6.1–6.7 / 7.x 那批）。

---

# 10. 第四轮（2026-08-01 深夜）—— 8.1 的话题消费点

`aidcp-edge ab31c57`（已在 origin/master）。拟人化线 **49/69 → 50/69**。

## 10.1 它不是「打字方式」的问题，是一条活的假成功

原以为这条只是「把整段赋值换成逐字打」。实读之后发现判据本身在说谎：

注入路由的做法是——把整段正文 `before + ' #话题'` **一次性重新赋值**，点完候选之后，
拿「正文里搜得到这几个字」当确认。**而它读回的正是自己刚写进去的那串。**
候选点没点上、平台受没受理，判据都为真。

- **后果不是「没贴上就报错」，是「没贴上也报成功」**：稿子发出去时话题只是一串普通文字，
  拿不到话题带来的分发，回执上一切正常，云端也看不出来。
- **这条路径是活的**：云端对每条稿子的**每一个 tag** 都下发一条这样的指令。

★ **可复用的判断法**：一个后置判据，先问它读的那份东西**是不是我们自己刚写进去的**。
是的话，它证明的是「输入发生了」，不是「平台受理了」——这两件事在写动作上永远要分开。
本轮 §9 的 H.1（评论到达确认）与这一条是**同一种病的两个实例**，都在同一天被找出来。

## 10.2 修法：整套移植退役实现，不自创

退役 TS 实现早就解决过，而且是**实机校准**的：逐字打触发串 → 轮询建议下拉 →
**只点**「文本精确匹配的候选」或「新建话题」那一项 → 真实鼠标事件（页面侧 `.click()`
实测**不提交**那个待定 span）→ 要求正文里出现真 token `a.tiptap-topic[data-topic]`，
剔掉隐藏后缀，**精确相等**而非子串（子串会让已存在的 `#考研数学` 替 `考研` 顶包）。

选择器、两个 4s 窗口、判据，一律照搬，**没有一处是在桩上现编的**。

**三条值得记住的设计约束**（都写进了代码注释）：

1. **认不出目标候选就不点。** 随便点一个的代价是**不可逆**——稿子上贴了一个无关话题，
   之后没有任何一步会去撤它。诚实的做法是不点、如实回未确认，正文里留一串裸 `#关键词`
   （与退役实现同行为），云端算尽力而为、**稿子照发**。
2. **打字之后没有安全取消点，这是有意的。** 本路径唯一的清场原语是**整字段清空**，
   而那一刻正文里躺着整篇稿子，清它等于毁稿。所以取消只在打字**之前**生效；
   之后把两个有界窗口跑完再让位（≤8s），且**每个出口都回「不确定」、绝不回「未开始」**。
3. **只截 `topic` 一种。** `mention` / `location` / `collection` 仍走注入路由——
   它们**没有任何实机校准过的 token 判据可移植**（退役实现对这三支用的也是通用锚点 + 页面子串，
   同样自证）。只把打字搬进引擎、判据仍留自证，**等于把同一个谎言搬进更新的代码**。
   ★ 这条取舍值得单独记：**做一半比不做更坏的场合，就是「判据仍会说谎」的场合。**

## 10.3 归因验证（4 次变异，逐条实测）

① 点完候选即判确认 → 「没变成真 token」那条红；② 真 token 判据退回正文子串 → 分片用例红；
③ 精确相等放宽成子串 → 「`#考研数学` 不得让 `考研` 判成已贴上」红；
④ 候选放宽成「取下拉第一项」→ 「全是无关词时不许点」红。Rust 两次输出里都有 `Compiling`。

假页面的落点特意与本文件其余落点拉开距离（编辑器 240,300 / 提交 320,460 / 候选 612,188）——
指针原语记「上一次落点」是进程级全局，落点撞车会让轨迹塌成单帧、把帧数断言变成抖动（§9.5-7）。
另：落点带 ±3px 抖动，所以假页面认「点没点过」按**距离**认，不按逐字相等。

## 10.4 全量

`cargo test --locked` / `gate:native` / `typecheck` / `test:acceptance`（31 例）/
`npm test` **2913 例 0 红**（这一轮没碰上 §5 那条既有 flake）。

## 10.5 下一步

1. **8.1 只剩「遗留步骤路径的输入步」**。门槛比听起来高：结构上必须把整条步骤循环搬进引擎，
   不能只截一个步骤。
2. **H.2 仍然阻塞**（见 §9.4-2）。
3. **另立 change：三道闸推广到其余命令面** / **云端编排那 7 项 ×2**（见 2.5 / 2.4）。
4. **攒批收尾**：台账 + 仪式类（6.1–6.7 / 7.x）。

**新登记的真机项**：backlog 簇 **123.32**（话题选择器今天是否还成立 —— 漂了会全线贴不上，
而且**不会有任何报警**）、**123.33**（「认不出候选就不点」的触发频率）、
**123.34**（另外三支候选的 token 判据标定，那是把它们也搬进引擎的前置）。

---

# 11. 第五轮（2026-08-01 收尾）—— 攒批收口，以及一个必须先看的发现

拟人化线 **50/69 → 65/69**。控制仓 only（无 edge 改动）。

## 11.1 ★★ 最重要的一件：spec delta 与实装大面积对不上

按 §8.1 那条纪律（「归档前把每条具名偏离对着 delta 读一遍」）实做了一遍，**结果比预期严重**：

**`specs/` 里有 8 条 MUST 与实装相反。** 照原样归档就会把它们并进主 spec。

原因是系统性的，不是本 change 独有：**delta 是 propose 当时按「意图」写的**，
而实装期跨了好几轮裁定 —— 验证码协助拟人化（2.9）、注入路由点击助手（3.7）、
锚点暂存与晋升（5.4 / 5.5）、定时设置、遗留步骤路径，**先后被显式弃守或收窄，
而 delta 一次都没跟着改**。

**没有任何东西会拦**：`openspec validate --strict` 只校验 delta 的内部结构、从不比对实现；
归档流程也不问。逐条清单与建议改法已写进 `tasks.md` 的
**「归档前置 — spec delta 与实装的逐条对账」**一节（含两条尚未读完、需要收口时补的）。

- **改法不是把 MUST 删掉。** 弃守的是「本 change 交付这件事」，不是「这件事不该做」。
  正确形态照迁移主线 3.2 的口径：**规格只能写「已实现」那一列** ——
  未交付的部分要么从 delta 摘出去另立，要么把要求收窄到实际成立的范围，并留下承接方。
- ★ **同批其余几个 change 归档前都该做一遍同样的对账**（它们的 delta 也都是 propose 期写的）。
  这条建议的成本很低、漏掉的代价是主 spec 长期失真。

## 11.2 攒批收口做了什么

- **6.1–6.7 全部收口，数字取自合并后的树。** 6.7 原本点名缺的那一半（各波门禁都只在**该波自己的
  分支树**上跑过）已经补上：另开了一个**分离头指针的只读校验 worktree**（`ab31c57`，独立 `npm ci`），
  在合并后的树上重跑全套。实测：`cargo test --locked` **23 个二进制 / 349 条 / 0 红**（库内单测 179）、
  `cargo fmt --check` 与 `clippy -D warnings` 单独实跑通过、`test:acceptance` **31/31 / 0 跳过**、
  `typecheck` 通过、`npm test` **2913 / 2912 绿 / 0 红 / 1 跳过**、
  `build:dist` `reachable=83 removed=68 legacy_page_rules=absent page_rule_fragments_guarded=18 source_maps=absent`。
- **7.1 / 7.8 / 7.10–7.15 收口**。7.10–7.15 是「登记为范围外 + design.md Non-Goals 具名交接」，
  实读 design 的交接表，六项逐条在表内、三栏俱全，故勾选；**勾选只到「已登记 + 已交接」，
  不含「承接方已开工」**（其中四项承接方仍是「需新立 change」）。
- **design.md D1 的债还了。** 第三波带着一条「具名推翻 D1」的裁定挂账至今（台账记着推翻、
  design 正文写着相反的决定）。现在 D1 正文改成现行结论 + 三条理由，原文降级为引用块留作追溯。
- **8.1 收口（具名收窄）**：最后那个消费点「遗留步骤路径的输入步」**不做**，理由三条且可核对 ——
  ① 它缺的是反检测质量不是会说谎的判据（属主已加过「真可编辑判据」闸，而输入步的业务结果
  本来就是「字在框里」）；② 与 2.9 / 3.7 / 定时设置**同源**；③ v1 有序步骤是**已弃用**的兼容路径，
  控制仓 CLAUDE.md §2 明写「勿在遗留路径上改代码」，而 §8 约定的接线方式在这一支上
  **做不到只截一个步骤**，等于在退场路径上新建一套实现。可达性也实测过（云端规则支产出的
  输入步 actionId 不在边缘白名单里，必落 `no_target`）。
  **⚠️ 这是一次范围判断，要推翻只改 tasks.md 那一条即可。**
  顺带登记了一条与该取舍无关的真残留：该支「写完立刻回读、命中即收工」有**回滚窗口**（§9.5-9）。

## 11.3 剩下 4 条

`2.10`（验证码回执诚实性合约测试，实现点在他人单写区、须与属主对齐）、
`5.6`（Native 命令面后置校验盘点）、`5.11`（现存后置判据逐条标达标 / 不达标）、
`H.2`（指针原语静默降级 —— **仍然阻塞**，须先有「降级痕迹到得了人眼前」那条通路）。

**归档前还压着 §11.1 那一节**（8 条 delta 对账 + 2 条待读完），那是硬前置。

**新登记的真机项**：backlog 簇 **123.29**（评论提交后平台清不清空评论框 —— **本簇优先级最高**，
它不确认，H.1 的收益就只剩「不再自证」，代价却是全量回执降级）、**123.30**（轮询窗口够不够 /
新原因码频次）；定时设置那条并进了既有的 **123.12**，没另开条目。


---

# 12. 第六轮（2026-08-01 收尾）—— 对账推广到其余五条线

承 §11.1。把「delta 与实装逐条对账」推到本批其余 5 个活跃 change。**先说结论：确实是通病，
但不是均匀分布 —— 集中在有弃守裁定的那两条线上。**

## 12.1 分诊法（可复用）

驱动判据不是通读，是**先找裁定**：`grep` 每个 change 的 `tasks.md` 找
「显式弃守 / 具名弃守 / 具名收窄 / 已弃守 / 裁定…不做」，再拿这些条目去查
**delta 里有没有一条 Requirement 在断言它们**。一条弃守裁定往往一次性作废整条 Requirement，
所以这个方向查得又快又准。

| change | Requirement 数 | 弃守类标记 | 结果 |
| --- | --- | --- | --- |
| `enforce-native-engine-artifact-gates` | 12 | **12** | **2 条命中，已修** |
| `native-page-engine-production-cutover` | 20 | 4 | 逐条核过 —— **delta 干净**，1 条建议属主复核 |
| `restore-native-xiaohongshu-action-honesty` | 17 | 2 | 重点条核过 —— **不构成命中**，1 条建议属主复核 |
| `harden-native-engine-runtime-contracts` | 9 | 0 | **只做了标记扫描，未逐条读** |
| `restore-native-facebook-residual-parity` | 15 | 0 | **只做了标记扫描，未逐条读** |

## 12.2 修掉的两条（都在 `enforce-native-engine-artifact-gates`）

1. **`edge-desktop-packaging` 的「双平台打包目标」MODIFIED —— 整段摘出。**
   那次 MODIFY 只加了一样东西：平台相关资源**按目标平台解析暂存目录** + 跨平台失败时报错
   须写明「目标 / 主机 / 实际解析目录」—— 也就是任务 **9.1–9.3**，而那一整节
   2026-07-31 已**显式弃守**。留着它，归档会把三条没实装的 MUST 并进主 spec，
   **而主 spec 里那条要求本来是干净的**。
   ⚠️ 摘出时特别标注了一条：**MODIFY = 整块替换**，将来要恢复必须把主 spec 那条的
   全部条款与 scenario 逐条抄回，只写新增段会**静默删掉**已有的双架构与 nsis 要求。
2. **`native-engine-artifact-gates` 的「退役路径覆盖不得冒充生产覆盖」—— 整条摘出。**
   整条对应 **8.1–8.3**，已显式弃守（做的是测试信号分层、属工程整洁）。
   **摘出时写清了「弃守 ≠ 这件事不存在」**：拿退役路径用例给 Native 行为充覆盖这个坑是真的、
   本批已经踩过（夹具编码了引擎不再走的分支＝死码喂绿），只是**它靠人读、不靠这条规格**。

**⚠️ 这两处是跨属主编辑**（`enforce-native-engine-artifact-gates` 当前无人开工，
但它不是本 session 认领的 change）。方向是**只减不增**、且防的是一次坏归档，
但仍须该 change 的属主**追认**。

## 12.3 两条建议属主复核（本轮不代改）

- **`native-page-engine-production-cutover`**：`Existing Xiaohongshu safety and behavior contracts
  MUST survive migration` 里点名了 `no-fake-success behavior`。而**这一整批修复 change 的存在
  本身就说明它没完全 survive**。它们正在被修，所以这条不是「断言了没做的事」，
  而是**口径问题**：归档时是按「迁移当时」读还是按「修完之后」读，须属主定。
  另：该 change 的 3.2 整条显式弃守，`tasks.md` 里已自带一条写得很清楚的归档红线
  （「规格只能写『已实现』那一列」）—— 实核之后 **delta 里并没有对应的定位类 Requirement**，
  那条红线防的是任务清单被读成能力，delta 本身是干净的。
- **`restore-native-xiaohongshu-action-honesty`**：`Unmeasured Xiaohongshu compatibility branches
  are removed rather than left claiming success` **不构成命中** —— 它的 scenario 是 either/or
  （「要么按实测位移回报、要么分支不存在」），而滚动步已改成按实测回报（2.8 已勾）。
  但有一件相关的事该属主读一下：被引擎特化**截走的那批死分支**（评论提交 / 发布字段填写 /
  三条翻页滚动段 / 评论滚动 / 现在加上话题）**至今一条没删**，它们现在测的是引擎已不再走的分支。

## 12.4 ⚠️ 本节改动的 sha 是 `07ab6265`，而那条提交说明与它无关

**收尾时又踩了一次 §5 记的那个坑**：本节的改动（两处 delta 摘出 + 本节正文）
`git add` 之后、`git commit` 之前，被**另一个并发 session 的全量暂存**一并提交走了，
落在 `07ab6265`（提交说明是「HANDOFF 更正部署口径」，与本节毫无关系）。

- **内容没坏、没丢**：逐条核过，两份 delta 的摘出说明与本节正文都在 `origin/main` 上。
- **丢的是提交说明**：本节的完整理由（为什么摘、摘的边界、MODIFY 整块替换的警告）
  原本写在提交信息里，现在只存在于**文件本身**。
- **这正是 §5 那条应对的价值所在** —— 「把『为什么』写进文件本身的注释，不要只写在提交说明里」。
  本次两处摘出的理由都是以说明块的形式**写在 delta 文件里**的，所以实质一条没少。
- **回写台账时注意**：这批改动的 sha 是 `07ab6265`，**不要按提交说明去找**。

## 12.5 剩下两条线也读完了（承上一版「未逐条读」的那一半）

上一版说这两条只做了标记扫描。**现已逐条读完，而「0 标记 ≠ 0 漂移」被坐实了一半：**

### `harden-native-engine-runtime-contracts`（9 条）—— **无漂移**

9 条逐条对过实装，全部对得上（命令清单验证契约 / 命令词表由引擎类型穷举 / 提交窗口单一事实源 /
死引擎不得挡住自己的恢复 / 重连绑定准入实例 / 重连重试留在预算内 / 页面规则取根与解码诚实 /
Facebook 评论提交预算算一次并传输；外加 `pluggable-browser-provider` 那条 MODIFIED
——重连须重解析端点 + 目标选择须带分身身份证据，都已实装）。

**唯一一处值得记的**：「死引擎不得挡住自己的恢复」那条有 4 个 scenario，其中
「引擎已退出后下发结束会话」那一个**没有回归用例**（任务 5.3 未勾）。
但实装是在的（收尾移进 finally + 缓存句柄存活判据），**所以这是测试缺口、不是 delta 漂移**，
且它已经作为未勾任务被追踪着。

### `restore-native-facebook-residual-parity`（15 条）—— **1 条命中，已修**

**`platform-browse-surface` 的「Comment migration is receipt-driven and fail-closed」MODIFIED
里有两段是零开工的**：**迁移闩的生命周期**（有界超时 / 会话结束 / 断连三路自清，且每一路都要
向运营报「已批准未送达」）与**闩的消费相关性准入**（不相干的回执 MUST NOT 进入解闩路径）。

这两段对应的工作已于 2026-07-31 **整体摘出本 change**，落到
`docs/cloud-orchestration-residuals-descoped-2026-07-31.md` 的 A 组 —— 判据只有一条：
**Rust 迁移碰过它没有**（这两条是云端编排缺陷，与 Native 迁移无关）。任务 6.5 的收窄注释
记的就是这件事。**零开工、待正式立项**，留在 delta 里就会把两条谁也没做的 MUST 并进主 spec。

已连同两条 scenario 一起摘出，原地留说明块写清「摘出 ≠ 弃守」（缺陷仍在、立论仍成立）
以及 **MODIFY = 整块替换**的警告：将来承接方写回时必须连同那时主 spec 的全部条款一起重述。

**其余 14 条不构成漂移，但要分清两种「delta 走在实装前面」**：
这个 change 才 43/61，`comment-interaction` 的「未观测的互动指标不是低于阈值」、
反应计数见证、列表面到底分类等好几条，实装确实还没做完 —— **但那是「任务没做完」，
不是「裁定作废了它还留着」**。前者归档前自然会被未勾任务挡住，后者不会。
**对账只该修后者。**

## 12.6 这一轮对账的总账

| 线 | Requirement | 结果 |
| --- | --- | --- |
| `restore-native-actuation-humanization-and-locating`（本 session 主战场） | 18 | **13 条命中，全修** |
| `enforce-native-engine-artifact-gates` | 12 | **2 条命中，全修** |
| `restore-native-facebook-residual-parity` | 15 | **1 条命中，已修** |
| `native-page-engine-production-cutover` | 20 | 0 命中（1 条口径问题交属主） |
| `restore-native-xiaohongshu-action-honesty` | 17 | 0 命中（1 条相关提醒交属主） |
| `harden-native-engine-runtime-contracts` | 9 | 0 命中 |
| **合计** | **91** | **16 条命中** |

★ **命中率与「有没有弃守裁定」高度相关**：三条有裁定的线全部中招（13 / 2 / 1），
三条没裁定的线一条没有。**这就是分诊该用的方向** —— 先找裁定，再查 delta 有没有还在断言它。

★ **另一条同样重要**：**「delta 走在实装前面」有两种，只有一种该修** ——
「裁定作废了它、delta 还留着」必须修（归档会把它并进主 spec，而且没人会再回来看）；
「任务只是还没做完」不该动（未勾任务本身就会挡住归档）。分不清这两种，
要么修漏、要么把还要做的事误删。

---

# 13. 第七轮 —— 拟人化线 **已归档**，以及现在该做什么

## 13.1 结果

`restore-native-actuation-humanization-and-locating` **69/69 → 已 archive**
（`2026-08-01-restore-native-actuation-humanization-and-locating`，控制仓 `bbabcc55`）。
三份能力并进主 spec：`native-actuation-primitives` 与 `native-locating-gates` 是**新建**，
`facebook-humanized-scroll` 增 3 条；合计 **+18 条 requirement，零修改零删除**。

本轮最后落的三件（edge `f5d7b52` / `fcb1fd2`）：

- **2.10 验证码回执诚实性。** 云端可以带一份运营画的鼠标轨迹下来，而宿主投影**手工枚举字段**、
  它不在里面 —— **被静默丢弃**（无日志无记录）；同时回执里的回放模式是**两条返回路径上的硬编码常量**。
  运营在后台画完轨迹、回执报「合成路径」，中间没有任何一处说得清那份轨迹去哪了。
  现在丢弃留痕（点名 incident 与原因），回放模式由「本次实际走哪条路」派生。
  **不转发轨迹本身是对的、没改**（引擎参数结构体拒绝未知字段，多塞一个会让整条命令变成非法请求）。
  ★ **这条卡了很久的真实原因值得记**：它挂着「须与 `src/main.ts` 的属主对齐」，
  而**那个属主 change 前一天已经归档** —— 单写区主张随之失效、没有属主可对齐。
  **一个卡在「等某方」的任务，如果没人回头查那一方还在不在，会永远卡着。**
- **5.6 / 5.11 后置校验逐面盘点。** 做成**登记表 + 常驻门禁**而不是散文清单：
  写命令集合从引擎自己的命令类型派生（全部 − `may_write` 的只读名单），与登记表做**恰好相等**断言。
  **实测 42 条写命令：22 达标 / 3 不达标 / 1 不适用 / 16 从没人读过。**
  最后那个数字在今天之前是不可见的 —— 而规格此前隐含地声称这个问题已经有答案。
  `unread` 是**只降不升且不留空位**的棘轮（留空位是棘轮最经典的失效方式）。
- **H.2 具名交接**（形态与 7.10–7.15 一致，**不是弃守**）。详见 13.3。

## 13.2 归档为什么现在才可以做

不是因为任务数满了，是因为**两个归档前置各自以自己的方式解除了**：

- 第 7 项（协助回执那条 delta）当时判为「**不是漂移，是任务没做完**」，处置是**不动它**、
  由未勾任务挡住归档 —— 2.10 一落地它就成立了。
- 第 13 项（「每条写命令都校验」）当时因盘点不存在而被限缩成「只按已核过的面读」——
  5.6/5.11 把盘点做出来之后，规格改成「**按盘点强制**」。

★ **这验证了对账时定的那条分法**：「delta 走在实装前面」有两种，
**只有「裁定作废了它、delta 还留着」该修**；「任务只是没做完」不该动，它会自己解决。

## 13.3 H.2 交接的实质（别读成弃守）

缺口是真的：预算收紧时指针轨迹会**无声降级**（约 480ms 以下掉出 15 帧拟人下限、
约 63ms 塌成单帧瞬移），无标记、无记账、无诊断。

**它需要的不是「加一行记账」，而是「那一行到得了人眼前」，而那条通路整体不存在。**
链路查到底：引擎是宿主起的子进程，错误输出既不继承也不落盘；宿主只收进一个 2048 字符滚动尾缓冲；
该缓冲**只在构造进程级失败时**才挂出。而**降级是一条成功路径** —— 命令正常返回、进程正常退出
⇒ 那一步永不触发 ⇒ 记账行被挤掉、随进程退出丢弃。**没有任何人看得到它。**
在这个前提下先加记账，写出来的用例是在断言一个运营侧读不到的字符串确实存在。

**同一条通路同时卡着逐字输入的降级记账**（§9.3 弃守项②）。所以它不是一个缺陷，
**是一类缺陷的共同前置** —— 这正是它值得单独立项的理由。
完整正文在 `docs/edge-honesty-gap-inventory.md` 的 **E12**（已更新为「已判：转出」）。

## 13.4 现在该做什么（按价值排序）

1. **另立 change：诊断通路本体**（解锁 H.2 + 逐字输入降级记账两处）。两条可行改法都不是一行：
   走协议加一条诊断记录（跨边界四处同步 + 一个消费端 + 牵动就绪握手），
   或宿主把子进程错误输出转进日志（给纯协议客户端注入日志出口并一路穿下来）。
2. **另立 change：三道闸推广到其余命令面。** **现在它有数据了** ——
   `native/page-engine/command-postconditions.json` 直接给出工作量：
   16 条未读要读、3 条不达标要修。这是覆盖面工作、不是架构工作（缝与桩都现成）。
3. **另立 change ×2：云端编排那 7 项**（见 §2.4；文档 `docs/cloud-orchestration-residuals-descoped-2026-07-31.md`）。
   **其中一部分现在是硬前置**：Facebook 残余对齐的 delta 里那两段（迁移闩生命周期 / 消费相关性准入）
   已在 §12.5 摘出，等这条 change 承接。
4. **其余五条线的收口**：FB 残余对齐 43/61、小红书动作诚实性 40/56、产物门禁 43/54、
   运行时契约 40/48、生产切换 45/49。**归档前都要做 §12.1 那套 delta 对账**
   （运行时契约已核过、干净；另外两条已修；剩下两条只做过标记扫描，见 §12.5 末段）。
5. **出包 → 一次真机 session**：backlog 簇 122 / 123 / 124 / 125 / 126 合计 **75 条**，
   共同前置只有一个 —— 重打一次客户端安装包。**小红书真机验收自 2026-07-22 迁移以来一次都没做过。**
   最该盯的三条：**123.29**（评论提交后平台清不清空输入框 —— 不确认的话全量评论回执降级）、
   **123.32**（话题选择器是否还成立 —— 漂了会全线贴不上且**零报警**）、
   **123.26**（多帧指针轨迹会不会划走反应浮层，这批**唯一有理由怀疑会新增失败**的路径）。

## 13.5 本轮新踩 / 复现的坑

- ★ **卡在「等某方」的任务要回头查那一方还在不在。** 2.10 等的属主 change 已经归档了。
- ★ **控制仓的暂存又被并发 session 的全量提交带走一次**（§12.4）。内容没坏，
  但提交说明彻底丢了。**唯一救得回来的做法是把「为什么」写进文件本身** —— 本轮两处 delta 摘出
  都是以说明块写在 spec 文件里的，所以实质一条没少。
- ★ **门禁写完先让它抓一次自己**：后置校验盘点的门禁首跑就抓到我自己表里一条
  `below_bar` 的理由只描述现状、没说清差在哪 —— 那读起来和 `confirmed` 没区别。
- **归档的 exit code 会骗人**（memory 里已有）：判据是输出里有没有 `failed for header` / `Aborted`。
  沙箱演练几秒钟就能跑完，真跑前过一遍。
- **归档提交必须用路径限定的 `git add -A`**，否则只提交新增、删除永远挂在工作区。
  验收判据：`git diff --cached --name-status` 应大量是 `R`（重命名），一堆孤立 `A` 就是漏了。
---

# 14. 第八轮 —— 立项四条、清账十六条

> **计数一律以 `openspec list` / `openspec list --specs` 为准**，本节数字是写作当天的快照。
> 本轮**没有写一行产品代码** —— 全部是立项与清账。五条实装线的进度一格没动。

## 14.1 §13.4 的 1–4 项都做了

**1–3 项（立项四条 change，全部 `validate --strict` 通过）**

| change | 规模 | 一句话 |
| --- | --- | --- |
| `surface-native-engine-diagnostics` | 35 项 | 诊断通路本体（解锁 E12 + 逐字输入降级记账） |
| `extend-native-postcondition-coverage` | 37 项 | 后置校验盘点的 16 条未读 + 3 条不达标 |
| `bound-facebook-comment-migration-latch` | 31 项 | 云端编排 A：迁移闩生命周期 |
| `unify-facebook-comment-budget-source` | 31 项 | 云端编排 B：评论预算单一事实源 |

**4 项（清账）**：16 条 ✓ Complete 全部归档，**+22 条 requirement 并进主 spec、3 条改名、零删除**。
活跃 change 41 → 29。

## 14.2 立项时坐实的、与 handoff 原文不同的三件事

**① 引擎已经在写诊断了，只是没人收得到。** 原以为要「加记账」，实读是 `native/page-engine/src/` 下
**15 处 `eprintln!`、11 类具名标签**，其中一批写在成功路径上（含逐字输入的降级记账，早就写好了）。
宿主把 stderr 收进 2048 字符滚动尾缓冲，**只在构造进程级失败对象时**挂出 ⇒ 命令正常返回的那些行随进程丢弃。
连 `session_open_failed` / `request_rejected` 这两条**失败**诊断也看不到（它们回错误响应但进程活着）。

**而末端已经存在**：Electron 逐行读核心 stdout/stderr → UI 活动流 + `edge.log`，未命中已知前缀的行原样落盘。
**断点只有「引擎子进程 → 核心进程」这一跳**。所以 §13.4 说的两条改法里，「宿主转发」这条比预想便宜得多，
且不必碰就绪握手（未就绪时任何非就绪记录都会判协议非法并终止引擎 —— 而引擎起不来正是最需要诊断的时刻）。

**② 「三道闸推广到其余命令面」这个说法会把范围放大一倍。** 规格明写：
「一个面是否合规是它自己后置条件的属性，不是它写在哪个模块里的属性；共享编排存在并已接第一条真实命令，
但一条自带诚实后置条件的命令同样合规」。所以要做的是**每条写命令都有够强的后置条件**，
不是把命令统一改走 `run_locating_gates`。后者是一次没有规格依据的大范围重写。

**③ 云端评论预算那条立论要重述。** 摘出文档说云端「只把正文传给预算函数」⇒ 算短了 ——
**这条已于 `aidcp-cloud 9013a5f`（2026-07-29）修复，比那份文档还早两天**。今天两份公式取值一致。
剩下的是**四个常量 + 一条组串口径在两个仓各写一份、且没有任何机械手段能发现分叉**。
立论从「修一个正在发生的缺陷」变成「消除一个只能靠人记住的约束」，优先级相应下调。

## 14.3 顺带发现的一处盘点表自我瓦解路径（已写进 change）

后置校验盘点的门禁给 unread 上了单调棘轮（只许降、预算恰等于实际条数、不留空位），
**但 below_bar 没有任何数量约束**。于是有一条完全合法的洗白路径：把一条 unread 改成 below_bar、
附一句「差在哪」⇒ 预算依法下降、门禁全绿、**而那条命令的假成功风险一点没变**。
现存 3 条 below_bar 已经停在那个出口上，且没有一条带「谁来消除、什么时候」。
**棘轮只锁了一个方向的出口，另一个方向敞着。**

## 14.4 ★ 攒批归档的真正风险不是「delta 对不上实装」，是「delta 对不上 delta」

这是本轮最值钱的一条，与 §11.1 是**两件不同的事**，别合并记忆。

16 条里有 **7 个能力被不止一条待归档 change 改**。而**每一条 delta 都是照当前主 spec 写的** ——
写它的那一轮看不到同批另一条尚未归档的修改。三处撞车，两类后果：

- **有意取代**（2 处）：只需定归档顺序。`simplify-active-browser-takeover` 是 BREAKING、
  明写移除浏览器出口探测与展示，确实取代 `invalidate-stale-proxy-runtime-evidence`。
  **顺序反了**，主 spec 会停在「要求分别展示浏览器出口与本机出口」——而那个界面已经不存在了。
- **不是取代，是底稿旧了**（1 处，**必须改 delta**）：`simplify-` 的范围里根本没有「手动启动是否重检」这件事，
  但它的 MODIFIED 全文按主 spec 写，会把 `refresh-proxy-preflight-on-manual-start` 的
  「单环境显式点击启动 SHALL 作废旧结果并重检」整段连同 4 条 scenario **静默删掉**。
  两者语义其实不冲突（Active 走接管不做预检 / Inactive 显式点击重检，分属不同分支），
  已把该条 requirement 改写成「以 `refresh-` 的版本为底稿 + 叠加 Active 接管改动」。

**还有一类：顺序对了也不够。** `recover-facebook-scroll-after-no-movement` 上一轮已登记
「前一条必须先归档」，但它的 delta 仍写作 `ADDED` 且**新旧要求名不同**，按序归档的结果不是取代，
而是在同一能力里留下**两条互相矛盾的 MUST**（一条说 `idle_recover_nudge` 是唯一被授权前台化的意图，
另一条说证实无位移后可以前台化）。已改为 `RENAMED` + `MODIFIED`，并把前一条里仍然成立的
按理由枚举的背景态清单并入正文。

**机械判据（攒批前扫一次，很便宜）**：
① 列出每个能力被哪些待归档 change 改，>1 的逐对比对；
② 每条 `MODIFIED` 的 requirement 名必须在主 spec 里**逐行精确存在**，不存在的只能由同批的
`ADDED` / `RENAMED` 解释，否则就是底稿错了。
**这两条 `validate --strict` 一条都抓不到**（它只校验 delta 内部结构）。

## 14.5 ★ RENAMED 的写法有个会让归档中止的坑

能归档的写法，FROM/TO 的反引号里要带**完整的 `### Requirement: ` 前缀**：

```
- FROM: `### Requirement: <旧名>`
- TO: `### Requirement: <新名>`
```

写成裸名字（不带前缀）会让归档报
`MODIFIED failed for header "..." - not found` + `Aborted`，**而 exit code 仍是 0**。

**这不是本轮新引入的**：`configure-facebook-mode-numeric-policies` 早就是裸名字写法，
也就是说**它此前根本archive不了**，只是没人试过。两处都已修。

## 14.6 下一步

1. **五条实装线收口**（本轮一格没动）：FB 残余对齐 43/61、小红书动作诚实性 40/56、
   产物门禁 43/54、运行时契约 40/48、生产切换 45/49。归档前都要做 §12.1 的 delta 对账 **+ §14.4 的跨 delta 对账**。
2. **新立的四条 change 择一开工**。依赖关系只有一条：`bound-facebook-comment-migration-latch`
   是 FB 残余对齐收口的硬前置（那条线的 delta 里迁移闩生命周期两段已在 §12.5 摘出，等它承接）。
3. **出包 → 一次真机 session**（只有用户能做）。backlog 簇 122/123/124/125/126 合计 75 条共用这一个前置；
   本轮新立的四条又各自往 backlog 加了真机项。**小红书真机验收自 2026-07-22 迁移以来一次都没做过。**

---

# 15. 第八轮续 —— 生产切换线 45/49 → **49/49**

> 与 §14 同一轮，接着做的是 §13.4 第 4 项（五条线收口）。**只动了这一条线**，另外四条一格没动。

## 15.1 落地的四件（`aidcp-edge` `1ea3cb1` / `8bafe28` / `a65a28d`；控制仓 `a4d8eb41` / `2077e336` / `7926975a`）

- **3.3 文件输入原语**：实读发现**它已经成立**，不是改到成立的。两平台共用同一份文件校验、
  单次原子 CDP 调用、有界诚实回读。「拟人化边界」在本原语上**不适用**（不是用户手势、CDP 无第二条路），
  按「这一格是空的，不是漏的」如实记。
- **9.1 Rust 全量闸**：fmt / clippy `-D warnings` / **350 例 0 失败** / release 构建全过。
  跨目标一栏如实记：本机只装了 `aarch64-apple-darwin`，其余目标**不作任何断言**。
- **4.6 契约测试**：见 15.2。
- **6.5 发布安全夹具**：见 15.3。

## 15.2 ★ 4.6 的真问题不是「补几个测」，是「没人说得清 every 是多少」

先做盘点、后补测。判据两侧：Rust 侧数 `NativeCommand::` 变体；TS 侧数命令名，**并剔除三类不算行为测试的文件**
（回执声明对账 / 登记表 / 节奏消费）。剔除这一步是关键 —— 不剔的话 `notification_back_home` 看起来「有覆盖」。

**三条不该补测**：`browse_next` / `browse_scroll` / `plan_execute`。判据不是「文档写了 @deprecated」，
是**云端根本发不出来** —— `command-bridge.ts` 的 `createEnvelope` 出口逐条列过，没有它们。
给它们补测＝在遗留路径上改代码。**按「具名排除 + 理由」处理，不按「已覆盖」处理。**

**三条零覆盖的补掉了**（16 例，`test/native-page-engine/xhs-navigation-command-contracts.test.ts`）：
`notification_back_home`（此前只有声明对账）、`note_close`（**全仓零命中**）、`profile_open`（只有宿主路由层）。
前两条是**出口**命令 —— 假成功不体现在自己身上，体现在它之后的每一条命令上。
`profile_open` 多一态：**精确目标绑定**，点错人不会让本条失败，只会让后续关注 / 读粉丝数记到别人账上；
「跳到了主页」与「跳到了那个人的主页」**必须是两条用例**。

**一条具名例外：`feed_refresh`（小红书侧）—— 补不了测，不是忘了测。** 它点完**无条件回确认**，
不取批次基线、不校验换没换（`xhs-command-router.js:456-460`）。唯一写得出的用例是「点了就成功」，
**那等于把缺陷锁进测试**。已登记为 `docs/edge-honesty-gap-inventory.md` **E13**，
属主 `restore-native-xiaohongshu-action-honesty`（**先查了它还在不在**，按 §13.5 的教训）。

## 15.3 6.5 补的是发布红线缺的那一半

既有 `AC-PUB-*` 守「未获授权绝不发布」；**「即使已授权也只发该发的那份内容」此前零验收用例**。

**为什么这半边全压在宿主**：引擎侧只按**扩展名**放行，而扩展名是**宿主按下载字节嗅探后自己写的**。
所以引擎那道闸的强度**上限就是宿主这一段** —— 而宿主这一段一条用例都没有。
新增 7 例 `AC-PUB-N01..N07`（验收套件 31 → 38），五条不变量：只走 https（挡住「把本机任意文件发出去」）/
不跟随重定向（否则一次 302 绕开前者）/ 体积查两次（声明值会撒谎）/ 扩展名只由字节决定、认不出就拒 /
任一不通过则引擎侧一条命令都不下发。

**如实记的缺口**：引擎侧 `validate_publish_file` 自身仍**零测试**（`pub(crate)`，要测须动 `engine.rs` 热点文件）。

## 15.4 方法上值得带走的两条

- **变异检验共做 9 次，每次都记下「是哪一条用例抓住的」**，且源码事后按 sha 还原（不用 `git checkout`）。
  9 次全部命中预期用例 —— 说明断言承重位置和设计一致，不是靠端到端那条兜底。
- **夹具不真实会让用例测的是夹具**：note id 用 `note-a` 这种占位，路由的 `[A-Za-z0-9]+` 正则在连字符处截断、
  两张卡被去重成一张。是红着发现的，**不是读代码想到的**。

## 15.5 下一步

1. **`native-page-engine-production-cutover` 已 ✓ Complete，但 MUST NOT 就这么归档。** 它是迁移主线、
   有显式弃守裁定，其 tasks.md 里自带一条归档红线警告（照原文归档＝主规格声称系统具备「从未实现」那一列的能力）。
   归档前置＝§12.1 的整份 delta 通读 + §14.4 的跨 delta 对账。
2. 其余四条线收口：FB 残余对齐 43/61、小红书动作诚实性 40/56、产物门禁 43/54、运行时契约 40/48。
3. §14 新立的四条 change 择一开工。
4. 出包 → 真机（只有用户能做）。
