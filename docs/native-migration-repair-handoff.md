# Native 引擎迁移修复 — 交接

> 状态时间：**2026-07-30**（第五波收口）。
> **接手先读 `docs/native-migration-repair-scope.md`** —— 本轮范围已被用户收缩为「只做这七条线」，
> 并已定下四条口径（第三道闸不做 / 边缘诚实性要做 / 跨平台打包按 case / 真机不单独排期）。
> 本文件是整批背景与逐波流水，**范围与优先级以那份为准**。
>
> 状态时间：**2026-07-30**（原第四波收口时写下）。接手时先按下面「起手校验」跑一遍，**正文里的所有计数都是快照、会滞后**。
> 本文档面向「换一个 session 从零接手这批工作」的场景，不是设计文档。上位背景见
> `openspec/changes/restore-native-*/proposal.md` 与各自的 `oracle.md`。

---

## 1. 这批工作是什么

2026-07-22~23 把 `aidcp-edge` 的页面自动化从 TypeScript 迁到 Rust Native Page Engine。迁移的等价性判据定在
**命令信封**（消息名 + 参数形状）上，而真正的行为契约在状态机、时间预算、后置校验里——这些不在那张消息表上，
于是整体蒸发。小红书侧约 11200 行 TS 被 290 行页内脚本取代，没有任何机制提示少了什么。

复盘用了两条独立路径交叉核实（正向扫来源 + 机制反推），产出 6 个承接 change。本批工作就是把它们实装。

**为什么问题一边倒集中在 Facebook**：边缘侧唯一一行逐命令回执诊断被「仅 Facebook」的判断包着，小红书跑完整轮
浏览闭环，日志里只有启动与失败两行。**没证据 → 没人发现 → 没 change → 翻记录也翻不到。** 这是自我强化的盲区：
Facebook 侧十几个提交修回，小红书侧零个修复 change，且小红书真机验收从迁移到现在**一次都没做过**。

---

## 2. 起手校验（先跑，别信正文数字）

```bash
cd /Users/baitianxing/codes/aidcp
openspec list | grep -E "restore-native|harden-native|enforce-native"

git -C /Users/baitianxing/codes/aidcp branch --show-current        # 必须是 main
git -C /Users/baitianxing/codes/aidcp-edge branch --show-current   # 必须是 master
git -C /Users/baitianxing/codes/aidcp-edge log --oneline -1        # 期望 = 第四波的头（见 §3）
```

Rust 工具链**不在默认 PATH**，跑 cargo 前先：

```bash
export PATH="$HOME/.rustup/toolchains/1.97.1-aarch64-apple-darwin/bin:$PATH"
```

边缘仓全套门禁（本机唯一真门禁，见 §6）：

```bash
cd /Users/baitianxing/codes/aidcp-edge
npm run typecheck && npm run test:acceptance && npm test && npm run gate:native && npm run build:dist
```

---

## 3. 已落地的状态

> **第五波起点更新（2026-07-30）**：edge master 已推进到 **`7f9ea7f`**。其间他轨合入了
> `confirm-facebook-feed-exhaustion-structurally`（`2dfaba2`），第五波自身目前只有一个提交（`7f9ea7f`，修 §4.5 的门禁抖动）。
> **正文下面这句 `c7c9ea8` 是第四波收口时的快照，勿当现状用。**

**edge master = `c7c9ea8`**（第三波头是 `a05bee9`）。**第四波（2026-07-29~30）新增 22 个提交**，
两条流各自在 worktree 上开发、串行 rebase 后 ff 合入主干、已推送。
**未部署**（这两条流是 edge-only，没有 ECS 面）、**未出安装包**、**未做任何真机写动作**。

> **重打包才生效**：这 22 个提交的主体是**编进 Rust 二进制的引擎 / 输入原语 / 注入分片** +
> 客户端核心 + 外壳投影。**仅 push 对运营机零效果**，必须重打桌面客户端包。

### 3.1 第四波 22 个提交（按流分组，均已在 `origin/master`）

`restore-native-xiaohongshu-session-guards`（12 个）：

| sha | 内容 |
| --- | --- |
| `3236d23` | 验证码键入取证：从推断改为实测 |
| `ac99d64` | 周期观测纳入生命周期托管（含宿主三处接线） |
| `95af401` | 运行期身份持续校验 + 重立链 |
| `197edae` | 复验补两条缺口（否定式断言改正向） |
| `d033504` | 身份闸六处审查缺口 |
| `bfaa33d` | 探针抛异常 ≠ 页面事实（四个消费点 + 清空第三态） |
| `7433a24` | 被取消的重立可重新开闸 |
| `c01bdcb` | 每个恢复入口要么量身份、要么停手 |
| `0966c43` | 外部看到的 = 正在发生的 |
| `1295122` | 每道守卫都可杀（可注入 / 可中性化） |
| `01605c8` | 订正代码对自己的描述 |
| `bed0e83` | 删掉接线处最后两处不实陈述 |

`restore-native-actuation-humanization-and-locating`（10 个）：

| sha | 内容 |
| --- | --- |
| `306410b` | 失败优先：云端时长指令的门禁用例（先红后绿） |
| `e748a56` | 消费云端下发的时长（犹豫 / 停留） |
| `1b890ea` | 小红书写动作改真实键入 |
| `25fa1c3` | 小红书翻页改共享惯性滚轮手势 |
| `92f7882` | 四处诚实缺口 + 两处合并陷阱 |
| `f652786` | 余下六处诚实缺口（含分片泄漏守卫改目录派生 11→17） |
| `17892c2` | 守住无人看的三态分支 |
| `044f103` | 守住五处折叠 |
| `4593922` | 钉住两条滚动回执语义 |
| `c7c9ea8` | 给没人看的拟人化时钟加下界 |

### 3.2 门禁实测 — **注意这批绿灯的取得位置**

**两条流的绿灯全部是「合回主干之前」在各自 worktree 上取得的；主干目标提交 `c7c9ea8` 上的重跑没有证据。**
两条 change 的对应任务（会话守卫 7.2/7.3/7.4）因此**故意没勾**。接手第五波的第一件事应是在主干上重跑一遍。

| 流 | 实测（合并前，各自 worktree） |
| --- | --- |
| 会话守卫 | `npm test` 2741 / 2740 绿 / 0 红 / 1 跳过；验收红线 30/30；类型检查通过；`cargo test` 独立 target 连跑 **25 次 0 红** |
| 拟人化 | `npm test` 2725 / 2724 绿；验收红线 30/30；`gate:native` 18 个二进制 / 269 条；`build:dist` 分片泄漏守卫 **17** |

两个可对照的数：全量用例 **2697 → 2741**；生产可达模块 **77 → 79**（新增两个可注入模块真进了生产可达集，
不是只在测试里存在）。分片泄漏守卫 **11 → 17** 是覆盖面变了（改成按目录派生），不是新增了 6 个分片。

### 3.3 change 进度快照

**会滞后，以 `openspec list` 为准**（下表为 2026-07-30 实测勾选数；分母随任务补充而变，不是「总量变大了」）：

| change | 进度 | 备注 |
| --- | --- | --- |
| `restore-native-xiaohongshu-session-guards` | **37/60** | 第四波主战场之一（原 24/50） |
| `restore-native-actuation-humanization-and-locating` | **28/74** | 第四波主战场之一（原 13/74），仍是缺口最大 |
| `restore-native-facebook-residual-parity` | 41/74 | 非本波，另有 session 在推 |
| `restore-native-xiaohongshu-action-honesty` | 37/61 | 未动 |
| `harden-native-engine-runtime-contracts` | 30/60 | 未动 |
| `enforce-native-engine-artifact-gates` | 23/55 | 未动（但**本波越界改过它的三个文件**，见 §4.6） |

**真机项已登记**：`docs/real-machine-acceptance-backlog.md` **簇 122**（身份 + 验证码键入，17 条）与
**簇 123**（拟人化输入 + 滚动 + 节奏，23 条）。两簇是同一台机器、同一个分身，**一次真机 session 连着验**。
**小红书真机验收自 Native 迁移以来一次都没做过** —— 这两簇同时是那次迁移的第一次真机复核。

---

## 3.4 第五波（2026-07-30）已做完的

> **本轮范围已被用户收缩：只做 Native 迁移修复这七条线，其余全部划出。**
> 范围说明与已定口径见 **`docs/native-migration-repair-scope.md`**（接手先读那份）。

edge master 上本波两个提交：

| sha | 内容 | 证据 |
| --- | --- | --- |
| `7f9ea7f` | 修掉 §4.5 那条约 50% 红的门禁抖动（三条 feed-recovery 用例共用落点） | 配对交错测量**修前 7/10 红 → 修后 0/10**；另加确定性守卫（两落点靠近到 64px 内即红，已实测触发） |
| `55c9d2e` | §5 三道闸接进第一条真实命令，顺带修掉一处两态塌陷 | 该支路接线前**零 Rust 用例**，新增 3 条；中性化验证：把修复改回去，该报错的那条当场报错 |

**五道门禁已在主干目标提交上跑齐**（这是 §4.1 一直欠的）：验收 31/31、`npm test` 2792/2792、
类型检查、`gate:native`（fmt + clippy `-D warnings` + test，19 个 target）、`build:dist`。
据此补勾了会话守卫的 7.2 / 7.3 / 7.4。**顺序不能反**：不先修抖动就重跑，会把主干自身的红读成自己改坏的。

**三条对既有记录的订正**（都是实测推翻，不是措辞调整）：

1. **§4.5 那条门禁缺陷的范围被低估、危害被高估**。穷举复核找到 **6 组**坐标撞车而非 1 组，
   但对抗复验后只有一组会红 —— 小红书搜索与验证码那两组走**裸三事件**、根本不碰那个进程级全局；
   其余三组组内无帧数断言。同一塌帧分支的 `budget == 1` 那一半在此**不可达**，故改坐标即根治、无残余抖动。
2. **主干门禁有第二族抖动**，此前被并进了第一族：`publish_tests.rs` 三条**绝对墙钟死线**用例
   （不止台账点名的那一条）。当天 34 次全量红 4 次（≈12%），**全部落在有并发负载时**。属主是产物门禁 8.4。
3. **「正常负载 39 次全量从未红」不成立** —— 干净主干 10 次里红 2 次。两次都在有负载的时段，
   所以原结论多半是**空载取样**得出的，与本批反复踩的「单独跑那个文件永远全绿」是同一种自证。

**一条给属主的提醒**：那三条用例今天能过，**恰恰是因为踩中了 P3「预算不足就静默退化成瞬移」的降级路径**
（50ms 预算把帧预算算成 1）。单纯抬预算会让点击变慢、反而更容易红 —— 这两件事必须一起改。

---

## 4. 下一步：第六波（原「第五波」的残项）

> **第四波已完成**（2026-07-29~30）：会话守卫 §3 验证码键入取证 + §5 运行期身份持续校验（5.6 除外），
> 拟人化 §4 云端时长消费 + §8 输入 / 滚动两个半边。落地清单见 §3.1，未完成的部分逐条列在下面。
> **控制仓已收口**：两条 change 的 tasks.md 已回写、真机项已登记（簇 122 / 123）。

### 4.1 先做：在主干上重跑一遍门禁 — **已做（2026-07-30 第五波）**

第四波的所有绿灯都在合并前的 worktree 上取得（§3.2）。
**已先修掉 4.5 那条门禁抖动**（`aidcp-edge 7f9ea7f`，见 4.5），再在主干目标提交上把 5 条门禁跑齐，
会话守卫 7.2 / 7.3 / 7.4 据此补勾。**顺序不能反**：不先修抖动就重跑，会把主干自身的 50% 红读成自己改坏的。

### 4.2 两条流的收尾残项

**`restore-native-xiaohongshu-session-guards`（37/60）**

- **5.6 仍是【阻塞 · 待人裁定】，不要开工。** 但裁定人手上的两条前提**在当前代码上已经不成立**
  （接收点不再是空实现、宿主已有两处建连后调用点），实际待裁只剩「逐动作下限值归谁」。
  另有一条新事实：显式建连**不触发**云端重连事件，所以这一步必须显式做，不存在「自然会有」的兜底。
  链条里已留具名占位（无静默兜底）。
- **1.6**：有界预算档位仍待人确认，本波未动。
- **3.4 只做了边缘半边**：云端 / 控制台半边（未知原因串的展示、键入取证的消费）**未做**，已登记为缺口。

**`restore-native-actuation-humanization-and-locating`（28/74）**

- **8.1 部分完成**：5 个消费点只落了 2 个（评论提交 + 发布字段填写）。**未落**：话题 / 提及候选、
  遗留步骤的输入步、定时设置。前两个是体量问题（要一套候选定位 + 点击 + 回读的分片能力，
  以及把整条计划循环搬进引擎）。
- **定时设置需裁定**：它是**分段日期时间控件**，退役实现用的就是原生赋值；照任务字面「逐字打」
  **会是一次功能回归**。裁定前不要动。
- ~~**§5（0/11）、§6、§7 全未动**~~ **【已推进，2026-07-30 第五波】**：§5 已 4/11 —— 5.1 / 5.2 接线落地
  （`aidcp-edge 55c9d2e`，三道闸接进第一条真实命令）、5.7 只读复核完成、5.8 承接边界已回写；
  **5.4 / 5.5 已按用户裁定显式弃守**（7.16 结案为「暂不做，待重新设计」，见 §7）。
  §6 / §7 其余条目仍未动；**7.17 仍是【待裁定】**。
- **§8 唯一一处能力回退**：富文本评论框 + 联系方式串码现在是**零派发结构性拒绝**（此前会「成功」但少发一个换行）。
  解锁前置是真机确认评论框对 Shift+Enter 的绑定 —— 真机项 123.5。**不要在桩上赌这一条**，
  赌错的后果是发出半条评论。

**跨属主追认（本波已越界，属主还没点头）**：`f652786` 改了 `enforce-native-engine-artifact-gates` 属主的
三个文件（把分片泄漏守卫从「只数 Facebook 有序清单」改成按目录派生，11 → 17），
**与该 change 5.7「不改该文件一行」的纪律直接冲突**。方向是收紧，但须属主追认。
另有 `harden-native-engine-runtime-contracts` 的两个测试文件（修一处被本波波及的**门禁失效**：
它用源码文本切片取「已路由命令集合」，本波改名导出后切出空串、对账退化成「空集 == 空集」）。

### 4.3 承第三波「再往后」的三项（仍未做）

- 运行时契约剩余：重连时重新解析端点（`engine.rs`）、会话结束收尾移进 `finally`（`browse-session.ts`）、
  身份证据判据**接线到真实附着路径**（见 §5.1 —— 2026-07-30 复核仍未接线）。
- 产物门禁 + 命令清单那 14 条声明修正：**必须同一个提交**（清单摘要被构建契约测试反向绑定到 Electron 侧的期望常量，
  改一边会让打包校验带着对不上的摘要出门）。
- **cloud 分支**（用户 2026-07-28 定：先做完 edge）。edge 侧现在只剩上面这些残项 + 真机验收，
  可以开始评估切过去。

### 4.4 第四波转出的三条后续项（P1 / P2 / P3）

这三条都**不是**第四波引入的缺陷，而是本波查清、但按范围纪律不能在本波修的。

**P1 · 任务租约拒绝的负向应答（跨仓 + 串行热点区）**

现状：身份未落定时边缘拒绝认领任务，但**不给云端任何应答** ⇒ 云端空等满认领超时（200s）才失败。
**负向应答的形状早就存在**（任务释放消息 + 原因字段；云端对已知原因**即刻** reject、不等超时；
边缘的任务协调器自己就在用这条通路），缺的只是「身份未落定」这一档原因取值。
> 曾有一句「协议上没有负向应答形状」写进代码注释 —— **那是假话，已订正**，并加了一条断言禁止它回归。

配方（四步，都很小）：
1. 两份 `protocol.ts`（**逐字一致**）的释放原因联合各加一个值，并按邻近条目风格写明：
   身份未落定 ⇒ **不可自愈**，处理办法是在浏览器里重新登录目标账号后重启本环境，云端 MUST NOT 自动重试。
2. 云端租约客户端的释放回调里，照现有「让出超时」那一段的形状加一个 `if`，归入**不可自动重试**一类。
3. 边缘身份命令闸把应答从「未接线」改成**真发出去**，并把对应用例从「MUST NOT 伪造」改成「MUST 发出且原因正确」。
4. 回归：两仓类型检查（两份协议文件的穷举表会抓漂移）+ `AC-PROTO-*`。

**边界（照抄，别扩大）**：**不**新增消息类型、**不**动动作映射、**不**改协议文档计数、
**不**进主动命令白名单。属 §7 热点单写区，**须等 `protocol.ts` 的单写窗口**。
代价基线已登记为真机项 122.13②（先量出「空等 200s」这个事实，修完才能对比）。

**P2 · 停摆 / 终局态在进程间通道丢失时缺持久红**

现状兜底日志**只顶当下一行**：下一行普通日志就把界面写回「运行中」。
性质是**继承缺陷**（终局态历来如此，本波只把新增的停摆态补到同一水平），不是本波引入。
修法在**外壳侧**：识别终态白名单措辞时，除了当次翻红，还要**合成一个运行姿态闩**落进环境句柄，
使后续日志行被覆盖层压住、红卡不被清。
**判据与合成必须落在可注入模块**，否则又是一条零覆盖边（本波刚补掉两条同类）。
现象已登记为真机项 122.13①。

**P3 · 指针原语在预算压力下静默退化成瞬移**

剩余预算不足约 **64ms** 时帧预算塌成 1 帧 ⇒ **瞬移到目标再按下**。
它**不报错、回执照样诚实**（点击确实发生了），全仓唯一会喊的是一条测试断言（「点击必须逐帧移动，
不得瞬移到目标坐标」）—— 也就是说**生产上这件事发生了没有任何人看得见**。
与拟人化流刚做完的「逐字输入降级标记 + 记账」是**同一族问题**，指针这条还没有。
**这不是第四波引入的。** 注意它与那一族共享同一个坏消息：那条降级记账目前**也到不了运营视野**
（引擎的错误输出只在进程级失败时才挂出，而降级是成功路径 ⇒ 记账行随进程退出丢弃）。
**「让降级痕迹到得了人眼前」这条通路根本不存在，须单独起 change**（两条可行改法都跨边界：
走协议加一条诊断记录 ⇒ 四处同步 + 一个消费端；或宿主把子进程错误输出转进日志 ⇒ 给纯协议客户端注入日志出口）。

### 4.5 第四波查清的一条门禁缺陷 — **第五波已修（`aidcp-edge 7f9ea7f`）**

> **收敛结论**（原始诊断保留在下面，供追溯）：取修法②，但**改常量做不到** —— 三条用例共用同一个夹具函数，
> 落点已改成必填参数、三条各传各的。**配对交错测量（同机同时段两棵树逐轮交替）修前 7/10 红 → 修后 0/10 红**；
> 另加一条确定性守卫，两个落点被拉近到 64px 内即当场红。
> **穷举复核把范围改了**：全 crate 共 **6 组**坐标撞车，对抗复验后只有 (540,330) 会红
> （小红书搜索与验证码那两组走**裸三事件**、根本不碰那个全局；其余三组组内无帧数断言）。
> 同一塌帧分支的 `budget == 1` 那一半在这里**不可达**（Facebook 点击出口传 `u64::MAX`），故无残余抖动。
> **但主干门禁仍未全绿**：另有第二族（`publish_tests.rs` 三条绝对墙钟死线用例，主干 10 次红 2 次），
> 属主 `enforce-native-engine-artifact-gates` **8.4**，未动。

`native/page-engine/tests/fake_cdp.rs` 里三条 Facebook feed-recovery 用例
（`facebook_feed_recovery_uses_one_trusted_cdp_click_before_returning_cards` /
`facebook_watchdog_feed_recovery_foregrounds_once_without_duplicate_activation` /
`facebook_feed_recovery_click_without_home_postcondition_is_ambiguous`）
**共用同一个落点坐标 (540, 330)**，而输入原语里的「上一次点击落点」是**进程级全局**、
被并发测试互相污染：**谁后跑，起点就已经在目标上 ⇒ 距离为零 ⇒ 路径塌成单帧 ⇒
「不得瞬移」断言当场红。**

- **实测频率**：主干自身 8 次全量**红 4 次（约 50%）**，本波分支同率 ⇒ **这条门禁在主干上就是坏的。**
- **⚠️ 单独跑该文件永远全绿** —— 所以历史上多次「无抖动」的结论都是这么自证出来的、**不成立**。
  同一坑本波还中过一次变体：只抬命令死线而没抬会话超时，等于空操作（真预算被会话默认值卡住），
  当时「连跑 6 次 + 8 次全绿」只是抽样太小。**两个上限都要抬，只抬一个等于没抬。**
- **三个候选修法**（主控倾向**第二个**）：
  ① 给引擎加一个具名的测试用重置入口 —— 最小，但污染生产 API；
  ② **让这几条用例各用不同的落点坐标** —— 纯测试改动、不碰生产、不污染 API；
  ③ 把「上一次落点」改成按会话隔离 —— 架构最正确，但要改输入原语签名与全部调用点。
- **生产语义不需要改**：一个引擎进程只驱动一个浏览器、命令串行，
  「光标已经在那儿就不再移动」是对的（两步点赞的第二步必须留在「控件 → 浮层」走廊内）。

### 4.6 并行安排

第五波的活仍然压在同几个热文件上（引擎主入口 / 输入原语 / 宿主装配 / 宿主会话 / 两份协议文件），
**必须一个 change 一个 worktree、集成串行**（§9）。
**P1 单独排队**：它动两份 `protocol.ts`，是 CLAUDE.md §7 点名的热点单写区，不与他人并行。

---

## 5. 绝不能误读成"已完成"的几件事

这批工作反复踩的就是「看着有、其实没通电」。下面每条都要按原样理解：

> **2026-07-30 复核**：下面各条逐条实读代码核对过。
> **第 3 条已被第四波接线，已改写**；第 4 条**补了一条新事实**；第 7 条**措辞订正**（结论不变）。
> 其余四条**原样成立**：第 1 条 —— 那个只读判据函数在整个引擎里**零调用方**（只有定义与自测）；
> 第 2 条 —— 暂存区仍恒空，且模块注释明写「本轮只造原语、尚未接进任何平台命令」；
> 第 5 条 —— 受闸动作名映射仍只有四条；第 6 条 —— 会话启动的首屏扫描仍直接调执行体、绕过停手闸。

1. **端点身份证据判据已落成但未接线。** 判据与失败语义写在端点选择模块里、有单测覆盖，
   但**没有接到真实附着路径**——那需要引擎分发层 + 宿主传入的分身身份。**MUST NOT 当成已生效的防护。**
   在它接线之前，同机多环境并行时端口被回收复用，仍可能附着到别的环境的浏览器上。

2. **锚点暂存区结构就位但在生产上可证为空。** 当前每个定位器都是编译进二进制的固定选择器，没有任何不确定来源，
   所以暂存区永远收不到东西。有一条测试专门把这件事钉死，**防止被读成"定位自愈已恢复"**。

3. **周期观测的宿主订阅已接线**（第四波 `ac99d64` 完成，本条由"未接线"改写）。三处落点实读确认：
   ① **执行器故障隔离处停观测** —— 刻意**不**挂在冷待机成功路径上：有活跃租约或复用外部浏览器时冷待机会被
   直接拒绝、而连接此时已是终态，挂在待机路径上就会漏，探针会对着一条已死的连接空轮询到进程退出。
   **别顺手把它挪进待机入口**（代码里已就地写明理由）；
   ② **冷待机进出对称**（进停、唤醒恢复，两侧幂等）；
   ③ **重连事件恢复观测与身份校验** —— 待机期不抢（那时浏览器还没重建）、暂停态不重开身份校验
   （它一判失效就走重立链、会把浏览重新拉起来，那是对"暂停"的违背）。
   **仍要注意**：这一条现在是"已生效的防护"，但**只在桩上验过**（真机项 122.12）。

4. **提交窗口预算表同时是准入白名单。** 不在表里的标签会被判成契约违规并否决窗口，而拿不到窗口的写入不派发。
   引擎新增一处窗口却漏改宿主那张表，后果**不是"少一层保护"，而是那处写入全部拒发**（回执诚实，但功能停摆）。
   第三波的 `7b8d556`（接窗口）与 `a05bee9`（加标签）**必须同批部署，单独回滚任一个都会让小红书写入停摆**。
   **第四波新增一条（预算打穿的后果与上面这条不是一回事，别混）**：小红书评论提交窗口的预算已从写死值
   改成**派生自命令墙钟上限**（引擎侧与宿主侧同一提交，另加编译期恒等式 + 宿主门禁断言；镜像门禁的正则
   改成能解析常量引用并回查引擎侧，解析不出即响亮失败）。
   **实测订正**：预算打穿**不会**让写入被拒发，而是窗口**静默过期** —— 过期之后没有任何接口会拒绝任何东西，
   它只是不再挡抢占 ⇒ 抢占重新落在提交那一刻 ⇒ 一条**可能已经发出去**的评论被当成没发生 ⇒ 上游重投
   ⇒ **重复评论**。（真正的"拒发"只发生在标签不在白名单那一种情形，也就是本条正文说的那件事。）
   若当初手抄数字，主干把命令上限从 30s 抬到 45s 时这处漂移**零文本冲突、不会有任何提示**。

5. **小红书评论点赞仍无提交前阻断复检。** 提交前闸只接了点赞 / 收藏 / 关注 / 评论提交四条。
   补法：在引擎侧那张受闸动作名映射里补一条。

6. **会话启动的首次扫描不经停手闸。** 它不走云端命令入口、也没有可回执的信封。
   后果是会话启动瞬间若正停在登录墙上会多滚一次；首个周期探针之后的所有命令都受闸。

7. **`facebook-consent-structural-detect` 这个 change 指向的实装文件在一座孤岛上。**
   `src/facebook/consent.ts` 还在仓里，切换后真正生效的同意条探测在页面规则里。
   该 change 起草于切换之前，**照原文实装等于加固一段永不运行的代码**。已在其 `tasks.md` 顶部写了必读订正。
   **（2026-07-30 措辞订正）** 原文写"`src/` 内除自身外零引用"**不准确**：它有三个引用方
   （Facebook 会话 / 评论执行器 / 信息流读取器，其中两处是类型引用）。
   但从核心入口 `src/main.ts` 出发做 import 闭包（含动态 import，共 **99** 个模块），
   `consent.ts` **不在其中，那三个引用方自己也都不在** —— 它们整体是一座与核心入口断开的孤岛。
   **结论不变**，但判据得这么说：**"没人引用"是错的，"从入口到不了"才是对的。**

---

## 6. CI 已上主干，但它跑不起来

`aidcp-edge` 新增了 `.github/workflows/checks.yml`（push / PR 触发，跑 TypeScript 门禁 + Rust 格式 / 静态检查 / 测试）。
它是随本批合并进主干的，**而且确实被触发了**。

**但每一次 run 都在 3~5 秒 failure，原因是 GitHub 账号计费被拒**
（"recent account payments have failed or your spending limit needs to be increased"）。这是账号级的，
任何 runner 都起不来——仓里原有的出包流水线同样在失败。

**在计费恢复之前，边缘仓唯一真门禁是本机手动跑的那几条**，而大家合并用的统一脚本 `scripts/land-change`
至今**一行 Rust 都不跑**。近两万行 Rust 与全部构建脚本仍然不在任何自动流程里。

**别把"CI 已配好"当成已覆盖。** 计费恢复后要做两件事：① 确认工作流真能跑完并通过；
② 再决定本机门禁要不要按需接进合并脚本（用户已同意"持续集成先上主干"，第二步待定）。

---

## 7. 两条已裁定 + 三条待裁定

### 已裁定（用户 2026-07-29）

- **同意条歧义：维持"认不准就停手"。** 曾提出按 DOM 包含关系先去重再判歧义（依据：候选采集含带标签的普通容器，
  嵌套时同一个按钮会被数两遍）——**未被采纳**，因为那只是读代码推出的假设、从未在真机上观察到。
  已承接为真机观察项（`facebook-consent-structural-detect` 任务 3.3.1）：真机上真碰到"认不准"时先 dump 一次 DOM 坐实，
  **取证前不得改判据**。
- **持续集成先上主干。** 已做（见 §6）。

### 待裁定（阻塞对应任务，不要绕过）

- `restore-native-xiaohongshu-session-guards` **5.6**：身份翻转 / 重连后"重注入连接级节奏快照"这一步在 Native 形态下归谁。
  **仍阻塞、不要开工**，但 2026-07-30 实读代码后**待裁范围已缩小**，且裁定人手上的两条前提**已不成立**：
  ① "接收点是空实现"—— 不成立：接收点真的消费降速档位（并明写了"不清离页停留锚点、否则就是秒退"的边界）；
  ② "宿主没有落点"—— 不成立：宿主已有三处调用（会话预算下发 / 云端重连 / 唤醒）。
  **实际待裁只剩一件事：逐动作的时间下限值归谁**（最小间隔那一层在 Native 形态下整体缺失，
  故其余动作的下限区间存下来就是死字段，代码里已具名登记为缺口）。
  **第三条新事实**：`cloud.reconnected` 只在客户端**自动重连循环**里发出，
  身份重立链走的是**显式建连** ⇒ **不触发**该事件 ⇒ 这一步**必须显式做，不存在"自然会有"的兜底**。
  链条里已按此留了具名占位（无静默兜底）。
- ~~`restore-native-actuation-humanization-and-locating` **7.16**~~ **【已裁定 2026-07-30，用户：暂不做，待重新设计】**
  裁定口径：**当前引擎根本没有「学」这一环节** —— 没有任何非确定性的定位来源，也就没有什么可暂存 / 可晋升 / 可反污染的；
  这一层很复杂，需要重新设计之后再谈。**5.4 / 5.5 显式弃守**（不做、不勾，已在 tasks.md 就地标注「不是待办」），
  本 change 的「定位三道闸」**只落两道**（后置校验、有界重试与升级）。结论已写回 design.md 的「待裁定」小节。
  **暂缓期间守两条**：① 代码里那套暂存 / 晋升实现与钉住「暂存区恒为空」的守卫用例保留原状，
  但 **MUST NOT 因它存在就把第三道闸读成已生效**；② 新增接线一律显式传只读模式，让空转是**声明出来的**。
  真要做须先回答「要不要重新引入非确定性定位来源」这个前提，再出设计、另起 change（CLAUDE.md §3）。
- 同 change **7.17**：可换的页面来源与执行层两个接口在迁移中消失，相关任务的验证方式取决于裁定结果。

---

## 8. 这批工作反复踩到的坑（接手务必知道）

### 8.1 夹具红了，先问"它编码的是旧行为还是实现有问题"

本批至少四次遇到：测试变红不是因为实现坏了，而是因为**夹具把被修掉的缺陷形态写死成了断言**。

- 三条 Facebook 用例把点击写死成"恰好三个鼠标事件"。多帧轨迹是十九次移动 + 按下 + 抬起。
  照它改实现 = 把"瞬移再点"这个机器特征原样种回去。已改为断言形状不变量（必须逐帧、按下抬起各一次且成对、
  落点在有界抖动内），helper 是 `assert_humanized_single_click`。
- 一条小红书用例的点赞控件不在互动栏结构内——正是新代码要拒绝的情形。改夹具为真机结构。

### 8.2 死码会把检查喂绿——**已发生三次**

- 一条按源码文本计数的检查要求某入口守卫恰好出现 2 次，失败文案写着"被移除的路由不得再出现"，
  而那 2 次里有 1 次就在恒假死码里。
- 一条新用例断言"两条页面命令路由的抑制分支都会回执"，第二条同样在死码里——
  也就是说**当初那半个修复落在了永不运行的代码上**。
- 一条平台驱动测试断言小红书驱动必须引用退役的浮层监测体模块。照它改会把退役模块拖回生产产物——
  实测那个无调用点的工厂成员正因值导入让四个退役模块一直随安装包发出（生产可达 81→77）。

**判据**：否定式的检查（"某某不得出现"）尤其危险，它可能正被它要禁止的东西维持在绿色。

### 8.3 并行编辑同一工作区 = 静默覆盖

同一个 worktree 里两个 agent 改同一个文件会互相覆盖，git 不会替你合并未提交的改动。
本批的做法是**给每条流一份互不重叠的文件白名单**，撞到别人文件的任务一律记进"待办"而不是硬改，集成时串行补。

### 8.4 集成拖得越久越贵

分叉 6 个提交、才两天，就有 **6 个文件冲突**，全在最热的路径上（引擎主入口 / 宿主会话 / 宿主装配 /
FB 信息流与短视频 / 页面规则分片 / 共享测试夹具）。这类文件冲突解错**不会报错，只会静默丢一块行为**。
**解冲突必须按行为对齐，不能按文本凑**——本批两处典型：

- 短视频：主干加了轴向导航，本流换了滚轮手势 → 合成"保留轴向结构 + 里面的滚轮换成手势"，不是二选一。
- 恒假死码：主干正好在里面改过东西 → 照删，它本来就不执行。

### 8.5 页面规则不是 Node 环境

`native/page-engine/src/*.js` 与 `facebook-router/*.js` 是构建期异或混淆后编进二进制、运行时注入页面执行的。
不能用 TypeScript、不能 import、不能依赖 Node API。改完需重新构建才生效。

**另外**：所谓"规则已加密"只挡扫读级别——异或密钥在已构建产物里直接搜得到。别基于错误前提把更敏感的东西塞进这条通道。

### 8.6 进程级共享状态让并行测试互相污染 —— 而"单独跑全绿"正是它的伪装

第四波查清的门禁缺陷（详情与三个候选修法见 §4.5）留下三条通用教训：

- **"单独跑那个文件全绿"证明不了任何事**。抖动来自同进程里**别的**用例留下的状态；
  单文件跑没有那个别的用例，于是永远绿。历史上多次"已确认无抖动"就是这么自证出来的。
- **判断某条门禁是不是自己弄坏的，先在干净主干上重跑同样次数**。第四波这条实测是
  **主干自身 8 次红 4 次**、本波分支同率 —— 结论因此是"主干本来就坏"，而不是"我改红了"。
- **抖动类修复只能用频率数据当证据，不能用中性化（突变）验证**。它修的不是确定性行为。
  反过来，凡是宣布"修好了"的抖动，**必须给出修前 / 修后两组次数与红灯数**；
  同一波里还中过一次变体：只抬命令死线而没抬会话超时 = 空操作，**两个上限都要抬，只抬一个等于没抬**。

---

## 9. 开发方式：换回一个 change 一个 worktree

用户 2026-07-29 同意从"六个 change 挤一条分支"换回项目本来的规范（`docs/parallel-dev-worktrees.md`、`CLAUDE.md` §7）：
**一个 session = 一个 change = 一条分支 = 一个 worktree，四者同名**，worktree 放 `../<repo>.wt/<change-name>`。

理由就是 §8.4 亲眼看到的。worktree 分不掉冲突，但它让冲突**在合并时暴露**，而不是在共用工作区里被悄悄覆盖。

**但要清楚**：剩下的几条 change 仍然共用引擎主入口那几个热文件，所以**集成必须串行**，
worktree 只是让每次冲突小一点、看得见。

分支 `native-migration-repair` 已合入主干、使命结束，其 worktree
（`/Users/baitianxing/codes/aidcp-edge.wt/native-migration-repair`）可以清掉。

---

## 10. 硬约束（照抄自 `CLAUDE.md`，别在这批工作里破例）

- **四个 canonical checkout 的分支指针永远停在各自默认分支**（`aidcp`=`main`，edge / cloud / console=`master`）。
  要分支隔离就另开 worktree。**绝不在 canonical checkout 里 checkout feature 或 release 分支。**
- **部署只从主 checkout 的 eligible ref 走，绝不从任何 worktree 部署。** 本批全程未部署。
- **`ol` 部署只有用户明确要求才做**，且必须从发布分支。`dev` 是默认目标。
- **桌面安装包默认不打。** 只有用户明确要求"打安装包 / 出包 / 发版"时才执行。
  edge 代码改动的默认收尾只到提交 / 推送。
- **force-push / 非 fast-forward / 推到非默认 protected 分支仍需先确认。**
  （本批为 rebase 后同步特性分支做过一次带 lease 的 force-push，已向用户报备。）
- **文档 / 提交 / tasks.md 里不写任何密码、token、私钥内容**，只记路径、服务位置、命令用法、配置读取方式。
- **红线**：MUST NOT 静默假成功；失败判定必须是结构性的；按实测回报不得回报请求值；
  「读不到」与「没有」是两态，不得压成一态。

---

## 11. 相关文档

- 6 个 change：`openspec/changes/restore-native-xiaohongshu-action-honesty/`、`restore-native-xiaohongshu-session-guards/`、
  `restore-native-actuation-humanization-and-locating/`、`harden-native-engine-runtime-contracts/`、
  `enforce-native-engine-artifact-gates/`、`restore-native-facebook-residual-parity/`
  —— 其中四个带 `oracle.md`（退役 TypeScript 实现的逐条摘录，含"不可照抄"的告警），**实装前先读**。
- 真机验收项：`docs/real-machine-acceptance-backlog.md`
- 并行开发操作手册：`docs/parallel-dev-worktrees.md`
- 部署口径：`docs/deployment-environments.md`
