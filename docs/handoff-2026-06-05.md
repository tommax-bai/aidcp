# 换机续作交接文档（2026-06-05）

> 目的：周末更换新开发机后，按本文档恢复 `aidcp` 三仓环境，并从当前开发进展无缝续作。
>
> **路径说明（2026-06-11 换机完成）**：三仓为同级目录，全文统一用相对写法
> `.`（本仓）、`../aidcp-edge`、`../aidcp-cloud`；`aidcp-edge` / `aidcp-cloud` 可能尚未在当前机器 clone。
> 部署 / SSH / ECS 口径不变。

## 先看这条：部署架构铁律

> **cloud 只部署在云端 ECS `121.89.85.150`，本地永远不要再起 cloud；本地只跑 edge，edge 连接 ECS cloud：`ws://121.89.85.150:8787`。**
>
> **ECS 上 `/opt/aidcp/cloud` 由 `systemd` 的 `aidcp-cloud.service` 托管；`8787` 对外监听；PostgreSQL 同机 `127.0.0.1:5432`，数据库 `aidcp` 已就绪并由 cloud 直连。**
>
> **同机另有 `isales` 独立运行，任何 ECS 操作绝不能碰 `isales`。**

## 三仓结构与本地路径

### 1) aidcp（文档总览仓）
- 本地路径：`.`
- 仓库地址：`git@github.com:tommax-bai/aidcp.git`
- 职责：只存 `docs/`

### 2) aidcp-edge（边缘端）
- 本地路径：`../aidcp-edge`
- 仓库地址：`git@github.com:tommax-bai/aidcp-edge.git`
- 职责：CDP / 浏览 / 拟人化 / 反检测

### 3) aidcp-cloud（云端）
- 本地路径：`../aidcp-cloud`
- 仓库地址：`git@github.com:tommax-bai/aidcp-cloud.git`
- 职责：协议 / 编排 / 发布 / 飞书 Bot

## 换机后初始化要求

- 三个仓库都要重新 `git clone`
- 三个仓库都要重新安装依赖
- `aidcp-cloud` 本机 `pnpm` 会被 supply-chain policy 拦截
- 因此本地若只做代码级验证，启动 cloud 时使用：

```bash
./node_modules/.bin/tsx --env-file=.env src/server.ts
```

- 但**正式运行口径不变：本地不要起 cloud，只跑 ECS 上的 cloud**
- ECS 环境使用 `npm`

## 换机前必做清单

1. 检查三仓 `git status`，确认是否有未提交或未 push 的改动
2. 处理 `aidcp-edge` 的 `git stash`
   - 当前已确认存在：`stash@{0}: wip: ws-reconnect-backoff 待修状态机`
   - `stash` 不会随 `git clone` 带到新机器
   - 建议换机前把它转成分支或 commit 后 push
   - 如果不处理，换机后这份改动会丢失
3. 把私钥 `~/codes/isales-4.pem` 拷到新机器
4. 在新机器执行 `chmod 600 ~/codes/isales-4.pem`
5. 在新机器验证能否 SSH 登录 ECS：

```bash
ssh -i ~/codes/isales-4.pem root@121.89.85.150
```

## ECS SSH 连接方式

- 登录命令：

```bash
ssh -i ~/codes/isales-4.pem root@121.89.85.150
```

- 注意：
  - 默认 key 会被拒绝
  - 必须显式指定 `~/codes/isales-4.pem`
  - 私钥文件权限必须是 `600`
- 文档中只记录路径和用途，不记录私钥内容
- 详细说明见：`aidcp-cloud/docs/deployment-ecs.md` 的 **ECS SSH 连接方式** 一节
- 对应提交：`57837cc`

### rsync 部署示例

```bash
rsync -av -e 'ssh -i ~/codes/isales-4.pem' --exclude .env --exclude node_modules --exclude .git <本地src/等> root@121.89.85.150:/opt/aidcp/cloud/
```

## ECS 当前部署状态（截至 2026-06-05）

> **更新（2026-06-25，最新）**：ECS 现役 cloud **`c1e00b0`**（= cloud master HEAD）。本次部署只新增一处：
> 飞书评论人审卡显示**真实笔记标题**（《…》）替代裸 noteId——两处人可见面同修（待审核卡「笔记」字段 +
> 审批后确认卡「标题」字段，后者经 callback `payload.title` 流到 `cards.ts` 的 `**标题**`，即用户截到的
> 「标题 / 评论·笔记 <id>」）。标题经 `CommentApprovalGate.getNoteTitle`（复用 dispatcher 既有 `getNoteData`）
> 解析、取不到诚实回落 noteId；审批信号 / 收发契约 / parse 全不变。
> **部署基线已是 `755baa9`（engagement-restraint，ECS 探针实测 `src/risk/action-cooldown.ts` + `COMMENT_MIN_LIKES=1000`
> 已 live，backup `20260625-233507`——故纠正"engagement-restraint 部署 gated"的旧说法：实为已部署）**，
> 本次 delta 仅 `c1e00b0` 单 commit（4 源文件外科手术 rsync，未全树同步、未碰 `publish-multi-image` 未提交 WIP）。
> 备份 `/opt/aidcp/cloud.bak.20260625-235448.tar.gz`；healthcheck 全过（`active`／NRestarts=0／`8787` 监听／
> 飞书长连接 onReady／PG 各 store 就绪；isales 未受影响）。`AIDCP_COMMENT_APPROVAL=true` 已开，改动即时可见。
> **macOS 部署坑**：本机自带 openrsync，`rsync -avR <abs>/./path` 的 `/./` 锚点**不生效**（会把整条绝对路径
> 套在目标下建垃圾树），改用**逐文件显式目标路径**（`rsync <src> host:/opt/aidcp/cloud/<rel>`）。下方 06-16 块起为历史快照。
>
> **更新（2026-06-16）**：ECS 现役 cloud `c14d2fc`（在 `873ec05` 指令级节奏基础上按实测调参）：
> 详情页停留下限 1200/2600→**2500/5000ms**、返回改 read 量级（治"停留仍偏快 ~1.2s"）；ContentCurator
> prompt 放松（默认 pass、仅明显广告/导流/纯空洞才 close、正文空≠低质）。备份
> `/opt/aidcp/cloud.bak.20260616-183354.tar.gz`；healthcheck 全过。配套 edge `6d69c84`（fallback floor 同步）。
> 下方 `873ec05` 为同日早先快照。
>
> **edge `f8712f5`（需重启本地 edge 生效）**：修正"正文抽空（标题+刚刚）"根因——CDP 真机验证发现
> 正文 `#detail-desc` 比 engage-bar 晚渲染、过早抽取回退到含标题+时间的容器。修：抽取前
> `waitForNoteBody()` 等 `#detail-desc` 出文本；body 选择器收紧为 `#detail-desc`/`.desc`（去掉松散
> `.note-content`/`[class*=content]`）。这是"停留算不准 + curator 偏严"的总根因，修后两者自然变准。
>
> **更新（2026-06-16）**：ECS 部署到 cloud `873ec05`（指令级节奏 Command Pacing）：
> 云端按已上报内容 + 风控状态算 `dwellMs`/`thinkMs` 中心值随决策指令（`navigation.back`/
> `interaction.*`/`note.open`）下发，`session.budget` 加极薄 `pacing` 默认块；治"详情页无价值秒退"。
> 回滚备份 `/opt/aidcp/cloud.bak.20260616-180059.tar.gz`（+ 既有 `.env.bak.20260616`）；
> healthcheck 通过（`active`／`8787` 监听／飞书长连接 onReady／PG 锚点缓存就绪／`NRestarts=0`；isales 未受影响）。
> 配套 **edge 侧 `5278eb7` 已 push 未上线、需重启本地 edge 生效**：消费时间指令、`jitterAround` 抖动、
> 详情页返回前 `ensureDetailDwell` 兜底停留。对应 openspec change `add-pacing-profile`（已 archive）。
> 下方 6/16(ed3b256)、6/12、6/5 记录为历史快照。

> **更新（2026-06-16）**：ECS 重新部署到 cloud `ed3b256`，覆盖一轮浏览循环 bug 修复：
> ① 开帖按帖子 `noteId` 寻址（修"判定 AI 卡却开成邻座 NPD/C罗"的 stale-index，`d446fa0`）；
> ② 消除 `content.no_valuable` 双发 scroll（翻页减半、恢复"连续 N 次转搜索"阈值，`7d652f0`）；
> ③ 关注顺序倒置修复（不关注直接返回／关注先点关注、等 follow 回执再返回；失败兜底排除 follow）
> ＋"已访问"标记从"判定时"后移到"确认打开后"＋清理死事件 `note.entered`（`ed3b256`）。
> 回滚备份 `/opt/aidcp/cloud.bak.20260616-154230.tar.gz` + `/opt/aidcp/cloud/.env.bak.20260616`；
> healthcheck 通过（`active` ／ `8787` 监听 ／ 飞书长连接已建立 ／ PG `select 1`）。
> 配套 **edge 侧已 push 未上线、需重启本地 edge 生效**：首屏轮询防死锁（`0a78363`）、
> 按 `noteId` 开帖 ＋ feed 滚动步长 800→500（`edbcf50`）。下方 6/12、6/5 记录为历史快照。
>
> **更新（2026-06-12）**：ECS 已重新部署到 cloud `6265afa`（事件驱动多 Agent + 协议 v2 + 风控/飞书 + “会话随边缘 hello 重置” fix），从 6/7 旧 src 全量追平到当时 HEAD；回滚备份 `/opt/aidcp/cloud.bak.20260612-165811.tar.gz` + `/opt/aidcp/.env.bak.20260612-165811`；migration `0004_publish_agent.sql` 已应用（`publish_pipeline_logs` 表 + `publish_log.images` 列）。下方 6/5 记录为历史快照。

- ECS 已更新到最新 cloud 提交：`ca18091`
- 本次关键修复：
  - `handler.ts` 发卡降级修复：`approvalChatId ?? FEISHU_CHAT_ID`
  - 文档补充：`docs/publish-approval-active-flow-plan.md`
- 已验证状态：
  - 服务 `active (running)`
  - 飞书长连接已建立
  - `0.0.0.0:8787` 正在监听
  - PostgreSQL `select 1` 可通
- 回滚备份已存在：
  - `/opt/aidcp/cloud.bak.20260604-1655.tar.gz`
  - `/opt/aidcp/.env.bak.20260604`

## 当前开发进展

### 1) 主动审批发卡链路

当前链路为：

`edge` 主动发 `publish.approval_request` → `cloud` 收到 → 降级使用 `FEISHU_CHAT_ID` 解析群

已确认事实：
- 本机曾报飞书错误 `230002`：`Bot 不在群`
- 根因不是代码主逻辑，而是**本机 `.env` 中配置的是过时群**
- 正确群由 `/bind` 命令注册在 ECS 同机 PostgreSQL
- cloud 跑在 ECS 且直连该数据库时，会自然拿到正确群配置并解决该问题

### 2) 飞书自动记群机制

Task Skill：`feishu-auto-chat-registry`

phase-1 当前状态：
- step1-5 已实现并验证
- 机器人进群 / 退群会自动入库 PostgreSQL
- `/bind` 可设置默认群
- 发卡流程已支持查库取群

关于 step-5 的当前结论：
- “默认群优先于 env” 的**纯逻辑测试已 PASS**
- 但真实 `handler` 实跑时，曾出现 `getDefaultChat = null`，随后走到 env 兜底
- 该问题尚未最终定性
- 需要在 ECS 环境复测后确认是数据、环境还是运行链路问题

## 换机后从这里继续

### 待办 A：工作流 A 真机联调收尾

目标：
- `edge` 改连 `ws://121.89.85.150:8787`
- 重跑主动发卡
- 用户在飞书中点击授权
- 设置 `AIDCP_REAL_PUBLISH=true`
- 真发一条测试笔记

已知前提：
- ECS 已配置 `DASHSCOPE_API_KEY`
- 正确飞书群已通过 `/bind` 注册
- cloud 已部署在 ECS 并直连同机数据库

补充：
- `edge` 后台常驻启动方式见 `aidcp-edge/docs/browser-cdp-status.md`

### 待办 B：三仓防健忘文档

每个仓库都需要补两份文档：
- `ARCHITECTURE.md`
- `DECISIONS.md`

用途：
- `ARCHITECTURE.md`：架构账本
- `DECISIONS.md`：决策日志

当前状态：
- 尚未开始

### 待办 C：WS 重连状态机修复

当前状态：
- 改动已存在于 `aidcp-edge` 的 stash 中
- stash 标识：`stash@{0}: wip: ws-reconnect-backoff 待修状态机`

已知 3 个真实 bug：
1. 重连后 `sessionId` 未更新
2. 断连队列补发时只发出 1 条
3. 初连重试存在超时问题

重要提醒：
- 这份 stash **不会**随换机后的 clone 带过去
- 换机前应优先处理：
  - 方案一：转成分支并 push
  - 方案二：整理成 commit 并 push
  - 方案三：接受丢失

## 新机器恢复建议顺序

1. clone 三仓到固定路径
2. 安装依赖
3. 拷贝 `~/codes/isales-4.pem` 并 `chmod 600`
4. 验证 SSH 登录 ECS
5. 确认 ECS 上 cloud 服务状态，不在本地启动 cloud
6. 本地只启动 edge，并把目标地址指向 `ws://121.89.85.150:8787`
7. 先恢复/处理 `aidcp-edge` 的 stash 对应工作，再继续工作流 A 联调

## 安全边界

- 不要在文档中记录任何敏感值
- 不要写 PostgreSQL 密码、token、私钥内容
- 只记录：
  - 文件路径
  - 服务位置
  - 命令用法
  - 配置读取方式

## 本次交接时的仓库观察

### aidcp
- 当前为文档总览仓
- 仅维护 `docs/`

### aidcp-cloud
- 当前 `HEAD`：`57837cc`
- 其中已知 ECS 实际部署版本：`ca18091`
- 说明：本地文档提交已继续前进，但 ECS 已确认运行的是 `ca18091`
- **更新（2026-06-12）**：ECS 已重新部署到 `6265afa`，上述 `ca18091` 为 6/5 历史值。

### aidcp-edge
- 当前存在未迁移风险项：`stash@{0}`

## 一句话续作口径

> 新机器上不要本地起 cloud；只恢复三仓、装依赖、准备 SSH 私钥、确认 ECS cloud 正常，然后本地启动 edge 直连 `ws://121.89.85.150:8787`，从工作流 A 联调和 `aidcp-edge` 的 WS 重连 stash 两条线继续。