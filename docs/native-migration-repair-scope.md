# Native 迁移修复 — 本轮范围说明（2026-07-30 用户裁定）

> **这份文档只说「我这一轮做什么、不做什么」，不对任何其他工作线下指令。**
> 各 change 的 `tasks.md` 保持原样，别的会话该怎么排还怎么排。
>
> 配套文档：`docs/native-migration-repair-handoff.md`（整批背景与逐波交接）、
> `docs/native-migration-repair-wave4-handoff.md`（第四波）、
> `docs/edge-honesty-gap-inventory.md`（边缘诚实性缺口 12 条）。

## 1. 本轮范围：只做 Native 迁移修复这一批

七条工作线，**246/447 已做，剩 201 项**（实测 2026-07-30，会滞后，以 `openspec list` 为准）：

| 工作线 | 进度 |
| --- | --- |
| `restore-native-actuation-humanization-and-locating` 拟人化与定位 | 33/76 |
| `restore-native-facebook-residual-parity` Facebook 残余对齐 | 41/74 |
| `enforce-native-engine-artifact-gates` 产物门禁 | 23/57 |
| `harden-native-engine-runtime-contracts` 运行时契约 | 30/63 |
| `restore-native-xiaohongshu-action-honesty` 小红书动作诚实化 | 37/66 |
| `restore-native-xiaohongshu-session-guards` 小红书会话守卫 | 40/60 |
| `native-page-engine-production-cutover` 迁移主线（切换） | 42/51 |

**201 项按性质拆开**（这个拆分比总数有用得多）：

| 性质 | 项数 | 说明 |
| --- | --- | --- |
| 真机验收 | 63 | **不单独排期**，由用户日常测试逐步覆盖（见 §3） |
| 收尾仪式 | 42 | 跑门禁、记提交 sha、跑 `openspec validate`、把范围外项登记出去。单条几分钟，**不做归不了档** |
| 补测试 | 15 | 给已修行为补护栏，可选择性取舍 |
| **真活（要写生产代码）** | **77** | 见 §2 |

## 2. 那 77 项真活里，风险最高的两块

**优先做这两块，约 20 项**：

1. **重连后可能连错浏览器**（运行时契约 §6）。引擎断线重连时不重新确认「我连的是不是本环境自己的浏览器」。
   同机多开时端口被系统回收再分配，**可能连到别人的号上、把动作记到别人账下**。
   判据代码已写好但**没接到真实附着路径上**——这是整批唯一一条会造成「张冠李戴」的。
2. **后置判据会错报成功**（拟人化 5.9 / 5.10 / 5.11 + 小红书动作诚实化）。宽松子串匹配、
   读回自己刚写进去的内容当证据。逐条清单见 `docs/edge-honesty-gap-inventory.md`。

其余 57 项可按线推进，不急。

## 3. 本轮已定的口径（别再重新讨论）

| 事项 | 裁定 | 出处 |
| --- | --- | --- |
| 定位第三道闸（锚点暂存 / 反污染） | **不做，需要重新设计**。当前引擎根本没有「学」这一环节 —— 没有非确定性定位来源，就没有可暂存 / 可晋升的东西 | 对应 change 的 7.16 已结案，结论写回其 design.md |
| 边缘「诚实回执 / 后置校验」缺口 | **要做**。同日曾短暂定为「按 case 处理」，随后改口径为要做，**以后者为准** | `docs/edge-honesty-gap-inventory.md` |
| 跨平台打包资源解析 | **按 case 处理**。已验证推迟是安全的：在 macOS 上打 Windows 包会**响亮失败**、不会静默发错包 | 产物门禁 9.1–9.3 |
| 真机验收 | **不单独排期**，由用户在日常测试中逐步覆盖 | `docs/real-machine-acceptance-backlog.md` 顶部 |
| 统一自动化运行模型 | **撤出计划**，用户后续自己立项；其「重叠处以本方案为准」的授权同时失效 | `docs/design/managed-automation-runtime/` |

> **真机那条要清醒**：日常测试的覆盖是**不均匀**的，冷门分支（异常态、降级路径、失败回执）可能长期没人碰。
> 清单上没勾的项＝**至今无人验证**，不因所属 change 归档就变成「验过了」。
> 小红书侧自 Native 迁移以来真机零覆盖，改成日常覆盖不会自动改善这一点。

## 4. 本轮不做的（**只是我不做，不是对别人的指令**）

用户口径：**这些多是此前 JS 侧没做完的功能，与「迁移到 Rust 引擎」这批没有关系**，不该混在这一轮排期。

云端拆仓（3 条）、验证码协助基址自证、视频号存储与熔断、发布认领改判、风控目标跟随会话、
自包含指纹浏览器运行时、Facebook 帖子身份两条、发布授权入库、发布触发与执行、
Facebook 发帖填充时限、以及几条差 1–2 项收口的。

**它们的 `tasks.md` 我一个字都没改**，立论仍然成立、缺陷仍然存在。其中三条值得下一个排期的人知道：

- **Facebook 发帖填充时限**：一天前引入的**线上回归**，正文改逐字输入后是 O(长度)，
  云端仍用固定 30s 窗口，约 175 字即中位数超时、200 字几乎必爆，dev 上每篇自动生成的 FB 帖都踩。
- **发布认领改判**：「资源暂时被占」被判成失败，且是 2 秒内烧光预算那种 —— 与本仓
  「失败判定必须是结构性的、资源暂时被占绝不算失败」这条铁律直接冲突。
- **验证码协助基址自证**：基址是进程内验不了的声明，可能**跨环境错投**。

另有两条的**立论已过期**（不是排期问题）：`facebook-join-actuation-decouple` 与
`facebook-consent-structural-detect` 的实装落点在 Native 切换后已不进生产
（前者源文件已不存在，后者文件在但被剪枝），**照原文实装等于给永不运行的代码加固**。
判据是「从核心入口到不到得了」，不是「有没有人引用」。**这只是事实登记，处置权在其属主。**

## 5. 交接给下一个 session 时要知道的

- **起手校验照 `docs/native-migration-repair-handoff.md` §2 跑**，别信正文里的计数。
- **Rust 工具链不在默认 PATH**：`export PATH="$HOME/.rustup/toolchains/1.97.1-aarch64-apple-darwin/bin:$PATH"`。
- **主干 Rust 门禁仍不是稳定绿的**：`publish_tests.rs` 三条绝对墙钟死线用例约 12% 概率红
  （当天 34 次全量红 4 次，全在有并发负载时）。属主是产物门禁 8.4。**别把它读成自己改红的。**
- **改动主体是编进 Rust 二进制的引擎 / 原语 / 分片，仅 push 对运营机零效果**，必须重打桌面客户端包才生效。
  （打包默认不做，须用户明确要求。）
- **我碰过的别人的文件**（都是登记 / 追认性质，非替其决定，但接手时应知道）：
  产物门禁与运行时契约的 tasks.md 各加了一节「跨属主改动待追认」；
  小红书动作诚实化的 tasks.md 加了一节指向诚实性清单；迁移主线的 3.2 / 3.3 下加了承接边界说明。
