## 1. Reconcile the current architecture and old assets

- [x] 1.1 Record the current `split-cloud-automation-production-runtime` dependency, Automation composition-root hotspots, branch/worktree baselines, and the exact gate that must close before production wiring. <!-- done: repo=aidcp sha=35567e2f evidence=references/current-state-inventory.md#1 validation="openspec strict valid; live split status 76/129; readiness ledger 11 blockers" deployment="n/a docs only" -->
- [x] 1.2 Inventory every managed-automation file and commit on the old `aidcp-cloud` feature branch; classify each as port unchanged, adapt to the independent process, replace, or drop, with a named reason. <!-- done: repo=aidcp sha=35567e2f evidence=references/current-state-inventory.md#2 validation="11 commits and all 63 diff files classified; automated missing-file comparison empty" deployment="n/a docs only" -->
- [x] 1.3 Inventory legacy per-account scheduling/dispatch entrypoints and in-flight evidence in `aidcp-automation`; define the complete lane-read and legacy-in-flight adapter surface before changing behavior. <!-- done: repo=aidcp sha=35567e2f evidence=references/current-state-inventory.md#3 validation="producer/dispatch census plus fail-safe LegacyAccountWorkPort frozen" deployment="n/a docs only" deviation="batch-G content scheduler is not yet wired; absence is explicitly unknown/deny" -->
- [x] 1.4 Freeze the phase-one owner map, current Automation migration ledger maximum, table manifest changes, cross-process DTO locations, and default-disabled rollout flags. <!-- done: repo=aidcp sha=35567e2f evidence=references/current-state-inventory.md#4 validation="current shared max 0105; proposed 0106-0109 allocation and four exact-false-default flags recorded" deployment="n/a docs only" -->

## 2. Freeze phase-one contracts and registries

- [x] 2.1 Port and narrow the Task, TaskRevision, CapabilityScope, ExecutionPlan, TaskRun, StepRun, Intent, Attempt, DecisionTrace, reason-code, and action-classification contracts into their final owner/shared locations; exclude ManagedPlan/Cycle/Trigger contracts. <!-- done: repo=aidcp-automation sha=7d22a22 validation="npm run typecheck 0; scope grep found no ManagedPlan/ManagedCycle/TriggerBinding/session-mode" deployment="not deployed; additive pure contracts" deviation="rewritten as phase-one Automation-owned authority instead of copying old API-owned/global contracts" -->
- [x] 2.2 Add canonical serialization, payload hash, version parsing, target validation, terminal-state guards, and customer-safe projection helpers with unit tests. <!-- done: repo=aidcp-automation sha=b27abc3 validation="managed-automation contracts 4/4; npm run typecheck 0" deployment="not deployed; additive pure helpers" -->
- [x] 2.3 Add versioned CreateTask, CancelTask, QueryTask, receipt, and error DTOs to the approved kernel/transport boundary without business execution code. <!-- done: repo=aidcp-kernel sha=34d1b94 validation="managed-task-port 3/3; npm run typecheck 0" deployment="not deployed; kernel DTO/port only; HTTP route/client remains task 4.2" -->
- [x] 2.4 Register and validate only `persona.research@1` and its bounded search→browse→assess→summarize read-only graph; prove every mutation capability is rejected. <!-- done: repo=aidcp-automation sha=0d1a048 validation="managed-automation registry 5/5; npm run typecheck 0; interaction/publish/reply/account_admin mutations all rejected" deployment="not deployed; registry has no production root call site" -->
- [x] 2.5 Add contract drift tests across API/Automation transport endpoints and prove unknown versions/capabilities fail honestly. <!-- done: repo=aidcp-transport sha=faf78a1 repo=aidcp-automation sha=de602c7 validation="transport drift/HTTP focused 10/10; real ManagedTaskCommandService HTTP slice 3/3; unknown wire version rejected before authorization/write; unknown TaskDefinition and capability versions rejected by the real Automation registry; Automation acceptance 145/145 and full 2075 pass with 4 guarded skips" deployment="not deployed; route remains unregistered in the production root" -->

## 3. Add Automation-owned persistence

- [x] 3.1 Add target-scoped migrations for task authority, task revisions, immutable plans, task runs, and account lane state using the current Automation migration ledger. <!-- done: repo=aidcp-automation sha=239bdbc validation="expand-only, migration-owner, schema-contract, boundary, schema-db-scope, and schema-ddl-owner 25/25; npm run typecheck 0" deployment="not deployed; migrations 0106-0109 are additive and feature flags remain false" -->
- [x] 3.2 Add target-scoped migrations for step runs, intents, attempts, command receipts, and decision traces; register every table in Automation ownership manifests. <!-- done: repo=aidcp-automation sha=239bdbc validation="10 new Automation tables registered; derived module census source=267 ownership=267 unresolved=0 forbidden=0; owner lists regenerated" deployment="not deployed" deviation="regeneration also reconciled pre-existing stale generated lists: api +16 and automation +12 existing manifest entries; no ownership source decision changed" -->
- [x] 3.3 Port/adapt typed stores with capability probes, target filters, CAS transitions, bounded leases, stable command dedupe, collision detection, and terminal regression protection. <!-- done: repo=aidcp-automation sha=e493afc validation="managed-automation focused contracts/registry/stores 17/17; npm run typecheck 0; boundary census source=274 ownership=274 unresolved=0 forbidden=0" deployment="not deployed; stores are not composition-root wired" deviation="rewritten for independent Automation tables and frozen phase-one contracts rather than porting monolith SQL unchanged" -->
- [x] 3.4 Add account-lane store operations for managed acquisition, legacy observation, renewal, safe release, expired-lease recovery, and kill-switch retention. <!-- done: repo=aidcp-automation sha=e493afc validation="lane unit coverage proves evidence-required legacy claim, lease CAS, expired takeover predicate, shutdown retention guard, and release retention for dispatching/submitted_unknown Attempts" deployment="not deployed; legacy producers do not consult the lane until gated task 6.4" -->
- [x] 3.5 Add store unit tests and PostgreSQL integration tests for concurrent claim, duplicate/collision, lease takeover, terminal races, target isolation, and lane release with unknown Attempts. <!-- done: repo=aidcp-automation sha=db9a224 validation="store unit tests 8/8; PostgreSQL guarded suite added and discovered 1/1 but skipped because DATABASE_URL is unset and no aidcp_test* database is available; npm run typecheck 0" deployment="not deployed" deviation="actual PostgreSQL execution remains required by task 7.3 and is not claimed here" -->
- [x] 3.6 Update schema contract and migration-order/owner gates without making a default-disabled feature look production-ready when its runtime dependency is absent. <!-- done: repo=aidcp-automation sha=239bdbc validation="KNOWN_MAX=0109 matches migration ledger; REQUIRED remains 0102; focused schema/owner suite 25/25" deployment="not deployed; missing 0106-0109 disables managed-task capability rather than the whole Automation process" -->

## 4. Build the API-to-Automation task port

- [x] 4.1 Implement Automation-owned CreateTask, CancelTask, and QueryTask services with authorization revision, account scope, registry, compiler, store, and readiness checks. <!-- done: repo=aidcp-automation sha=e9bf456 validation="command transaction/service 10/10; combined managed/schema/boundary run 48/48 after unique-race audit; npm run typecheck 0; boundary census source=282 ownership=282 unresolved=0 forbidden=0" deployment="not deployed; no route or production root call site" deviation="Create commits Task+Revision+Plan+Run+Trace+Receipt in one owner transaction; authorization remains a narrow injected projection port for task 4.3" -->
- [x] 4.2 Implement authenticated, versioned, target-bound Automation routes and transport clients with stable result-unknown behavior. <!-- done: repo=aidcp-transport sha=faf78a1 repo=aidcp-automation sha=de602c7 dependency=aidcp-kernel@2fe845b validation="transport full 46/46 and typecheck 0; Automation managed focused 35/35, typecheck 0, boundary census source=282 ownership=282 unresolved=0 forbidden=0; timeout/ambiguous/malformed write response maps once to result_unknown with the original commandId while deterministic auth/route/protocol/target outcomes remain distinct" deployment="not deployed; no composition-root registration or API owner adapter" deviation="Automation keeps its existing direct kernel pin until gated task 6.1; the new transport dependency carries the merged feature kernel transitively so E-2 acceptance fixtures and production roots stay untouched" -->
- [x] 4.3 Implement the API owner adapter that authorizes actor/account access and calls the Automation port without constructing Automation stores or calling Edge. <!-- done: repo=aidcp-api sha=19956bd branch_head=354dcc4 repo=aidcp-transport sha=e031d6a dependency=aidcp-kernel@2fe845b validation="API owner adapter 7/7; API full 508/508; acceptance 3/3; typecheck 0; npm package graph has one exact kernel and transport instance; transport full 46/46 and typecheck 0" deployment="not deployed; adapter is not registered in the API production root" deviation="API origin/master advanced during the slice and was merged without rewriting published history; transport transparently re-exports its kernel DTO types so API does not define a second contract" -->
- [x] 4.4 Add command idempotency, collision, cancellation-before/after-dispatch, cross-account query privacy, malformed response, timeout, and disabled-route tests. <!-- done: repo=aidcp-transport sha=e031d6a repo=aidcp-automation sha=4b885a3 repo=aidcp-api sha=19956bd validation="transport managed HTTP/drift 10/10 covers duplicate/collision, auth/disabled route, malformed response and timeout; Automation command/service/HTTP slice 14/14 proves queued/waiting cancellation terminates while dispatching/submitted_unknown reconciliation remains explicit; API 7/7 proves cross-account query returns common not_found, authorization outage remains unavailable, and result_unknown is returned once without retry; Automation full 2076 pass/4 guarded skips and acceptance 145/145" deployment="not deployed; no production route, worker, Edge dispatch, or fallback" -->
- [x] 4.5 Add safe task/run/reason/trace query projections that exclude secrets, raw private content, hidden model inputs, and unrelated account data. <!-- done: repo=aidcp-automation sha=e9bf456 validation="query projection test proves DecisionTrace input/evidence refs and correlation/causation IDs are absent; account-scoped task lookup precedes projection" deployment="not deployed; query route remains task 4.2" -->

## 5. Build the bounded execution runtime

- [x] 5.1 Port/adapt the immutable linear PlanCompiler and StepExecutor contracts; add deterministic plan-hash and bounds tests. <!-- done: repo=aidcp-automation sha=0a4ecbf validation="plan compiler 5/5; focused managed-automation plus boundary suite 26/26; npm run typecheck 0; boundary census source=278 ownership=278 unresolved=0 forbidden=0" deployment="not deployed; compiler and executor contracts have no production root call site" deviation="compiler is pure and service-level denial traces remain task 4.1; it hashes the frozen revision/authorization/graph/bounds artifact and rejects scope weakening" -->
- [x] 5.2 Implement the account lane arbiter over durable lane state plus a complete legacy-in-flight adapter; do not infer availability from socket mode. <!-- done: repo=aidcp-automation sha=7481dea validation="lane/store focused 17/17; npm run typecheck 0; boundary census source=283 ownership=283 imports=785 unresolved=0 forbidden=0" deployment="not deployed; arbiter has no production root call site and lane exclusion remains default-off" deviation="all nine frozen legacy producer slots are mandatory; content_schedule is represented by explicit unknown until its independent root is wired, and task 6.4 must add the legacy-side pre-dispatch checks" -->
- [x] 5.3 Implement TaskRunWorker claim, renew, step checkpoint, cancellation, retry, expiry recovery, terminalization, and graceful shutdown behavior behind a default-off gate. <!-- done: repo=aidcp-automation sha=cd19501 validation="worker/store/lane focused 23/23; worker vertical slice 6/6; full 2091 pass/4 guarded skips; acceptance 145/145; npm run typecheck 0; boundary census source=284 ownership=284 imports=795 unresolved=0 forbidden=0" deployment="not deployed; owner-local worker is exported but not constructed by the production root" deviation="expired running rows are reclaimed directly by target-scoped SKIP LOCKED claim; lease heartbeat does not advance the state CAS version; dispatching/submitted_unknown recovery never re-dispatches and shutdown retains the lane" -->
- [x] 5.4 Implement `ResearchStepExecutor` for the four registered read-only steps with stable content-reference evidence, dedupe, bounded re-drive, and distinct empty/failed/unknown outcomes. <!-- done: repo=aidcp-automation sha=c76c4c5 validation="research executor 6/6; final owner-runtime focused suite 54/54; full 2109 pass/4 guarded skips; acceptance 145/145; npm run typecheck 0" deployment="not deployed; executor is owner-local and has no production root call site" deviation="each executor call performs one atomic dispatch and leaves retry bounds to TaskRunWorker; malformed or target-drifted completion evidence becomes submitted_unknown, and duplicate stable refs are traced without increasing counters" -->
- [x] 5.5 Implement the Automation connection-runtime dispatch adapter using existing account/edge targeting and atomic command receipts; reject unsupported platform/capability combinations. <!-- done: repo=aidcp-automation sha=dab3314 validation="adapter plus connection-runtime focused suite 30/30; final owner-runtime focused suite 54/54; npm run typecheck 0; boundary census source=288 ownership=288 imports=813 unresolved=0 forbidden=0" deployment="not deployed; the atomic command channel remains an injected owner port for task 6.2 and no current Edge connection declares these capabilities" deviation="the adapter adds no WebSocket message type; it resolves exact ads-<envKey> account/platform/capability metadata, subscribes before one send, binds receipts to connection generation, and maps post-dispatch timeout or send ambiguity to submitted_unknown" -->
- [x] 5.6 Add unit and vertical-slice tests for waiting-for-lane, cross-account independence, reconnect, timeout-after-dispatch, empty result, duplicate evidence, cancellation, recovery, and kill switch. <!-- done: repo=aidcp-automation sha=77ea3af validation="lane/executor/adapter/worker/connection focused suite 54/54; worker lifecycle and vertical slice 10/10; full 2109 pass/4 guarded PostgreSQL skips; acceptance 145/145; npm run typecheck 0; boundary census source=288 ownership=288 imports=813 unresolved=0 forbidden=0" deployment="not deployed; no production construction, route registration, real Edge dispatch, or platform action" deviation="undeliverable waits are checkpointed as waiting_for_edge and release the safe lane before a bounded reconnect Attempt; a kill switch retains the lane while dispatch is unresolved, then stops at a durable checkpoint after a proved receipt" -->

## 6. Wire production roots without reviving a second runtime

- [ ] 6.1 Rebase after `split-cloud-automation-production-runtime` closes its Automation main/readiness hotspots and confirm no other session is writing the same composition-root files.
- [ ] 6.2 Wire managed task routes, stores, registry, lane arbiter, worker, executor, and shutdown into the independent Automation root with separate default-off entry/worker/lane flags.
- [ ] 6.3 Wire the API client/authorization adapter into the independent API root; keep customer UI and direct Agent tool exposure out of scope.
- [ ] 6.4 Make every identified legacy scheduling/dispatch entrypoint consult the shared account-lane adapter and emit `managed_task_lane_active` without changing disabled-path behavior.
- [ ] 6.5 Add readiness, metrics, logs, and health detail for disabled, schema-missing, target-invalid, port-unavailable, lane-waiting, claim, recovery, unknown Attempt, and shutdown states.
- [ ] 6.6 Add loopback API→Automation and Automation→connection-runtime tests proving Create→Compile→Run→Trace→Query and proving no legacy fallback or direct Edge path exists.

## 7. Validate, integrate, and prove boundaries

- [ ] 7.1 Run focused contract/store/port/runtime/lane tests and typecheck in every owning repository; record exact counts and gated tests.
- [ ] 7.2 Run owner-boundary, protocol/transport drift, target-isolation, risk honesty, unauthorized-action, acceptance, and full suites in every affected repository.
- [ ] 7.3 Run PostgreSQL CAS/claim/lease/target/lane integration against an isolated DEV-capable database and record rollback-safe schema evidence.
- [ ] 7.4 Rebase and serially integrate affected repositories using fast-forward-only history; update repo/commit/test evidence without force-push.
- [ ] 7.5 Deploy only to named DEV after `scripts/deploy-target dev --check`; verify independent API/Automation/Content health, listeners, worker readiness, disabled default, and rollback. Do not deploy OL.
- [ ] 7.6 Enable the read-only slice only after all gates pass; run a named DEV task probe and distinguish Automation receipt, Edge dispatch, read evidence, and platform observation.
- [ ] 7.7 Update handoff and task evidence, run `openspec validate add-managed-automation-task-channel-phase-1 --strict`, and archive only after all required implementation and live-validation boundaries are complete.
