## 1. aidcp-cloud — Registry shape (additive)

- [x] 1.1 Extend `src/platform/registry.ts` `PlatformRegistryEntry` with `noteActions: Record<NoteScopedAction, {supported:true}|{supported:false;reason}>` (all 7 note-scoped actions), `noteSurfaces: Record<'read_content'|'like'|'comment', Surface>`, and `capabilities: Record<'browse'|'feed_refresh', ...>`; fill XHS and Facebook entries with stage-0 values (FB surfaces all `detail`; FB collect `{false,'no_collect_concept'}`; FB comment_like/browse_images/scroll_comments `{false,'v1_unimplemented'}`; FB feed_refresh `supported:true`). <!-- aidcp-cloud 42bee48 old `capabilities: PlatformCapability[]` array dropped (zero src consumer besides browse gate); registry.test.ts updated to new shape -->
- [x] 1.2 Migrate the browse startup gate `role-dispatcher.ts:1008` from `.includes('browse')` to `.browse.supported` in the same commit; assert XHS `browse.supported===true`. <!-- aidcp-cloud 42bee48 via isOrchestrationCapabilitySupported(platform,'browse') (fail-open) -->
- [x] 1.3 Add pure `src/platform/surface.ts` with `resolveReadSurface(platform)` / `resolveCommentSurface(platform)`. <!-- aidcp-cloud 42bee48 also loopClosure + isNoteActionSupported/noteActionRefusalReason/isOrchestrationCapabilitySupported/platformFeedScrollFloorMs, all fail-open -->

## 2. aidcp-cloud — Static control flow + capability gate

- [x] 2.1 Add the private `sendNoteScopedCommand()` wrapper: `noteActions[a].supported===false ⇒ not dispatched + audited reason`; route like/collect/comment/browse_images/scroll_comments/comment_like through it as the single explicit refusal point. <!-- aidcp-cloud 42bee48 all 5 dispatch sites routed; fail-open on registry miss -->
- [x] 2.2 Drive loop-closure back-vs-scroll from `resolveReadSurface(platform)` plus a per-note `currentNoteMigratedToDetail` flag (set by cloud when it emits a migration command, reset per note); **do not read `observedSurface`** for control (`observedSurface` audit-only). <!-- aidcp-cloud 42bee48 shouldCloseWithScroll() reads static loopClosure(); flag on SessionContext, reset on note change + reset(); observedSurface not introduced in C1a (audit echo is C1b) -->
- [x] 2.3 Gate FeedScroller construction on `capabilities.feed_refresh` and generalize `facebookScrollDwellMs` into a `pacing.feedScrollDwellFloorMs` consumer. <!-- aidcp-cloud 42bee48 canRefresh() ? undefined : {enabled:false} (fail-open, doesn't override env kill-switch); facebookScrollDwellMs→feedScrollDwellMs reads platformFeedScrollFloorMs (FB 7000, XHS undefined) -->
- [x] 2.4 Inject fail-open closures `canBrowseImages()`/`canScrollComments()`/`canRefresh()` (mirror `isInteractionEligible` injection at `role-dispatcher.ts:803`); registry-miss/exception ⇒ return true (today's behavior). <!-- aidcp-cloud 42bee48 -->

## 3. aidcp-cloud — Roles consume injected closures (fail-open, honest short-circuit)

- [x] 3.1 `deep-reader.ts` short-circuits on `canBrowseImages()===false` to `reading.images_done{imagesBrowsed:0, reason:'surface_unsupported'}` (no LLM, no fabrication); `comment-reviewer.ts` short-circuits on `canScrollComments()===false` to `reading.done{commentsRead:0}`; roles MUST NOT import registry or branch on `platform==='x'`. <!-- aidcp-cloud 42bee48 both short-circuit before any note read / LLM decide; closures default to ()=>true when unset; added optional reason field to ReadingImagesDonePayload -->

## 4. Verification

- [x] 4.1 Cloud unit tests: registry all-coverage typecheck + test (every platform × every NoteScopedAction has a supported profile; every `supported:false` has a non-empty reason; noteSurfaces 3 keys present; `browse.supported` Record shape ⇒ XHS===true startup gate); capability gate (FB + `interaction.completed{actions:['like','collect']}` ⇒ only like dispatched, collect audited); short-circuit fail-open (`canBrowseImages()` miss/exception ⇒ still dispatch browse_images for XHS; ===false ⇒ `reading.images_done{imagesBrowsed:0}` and LLM not called); static routing (XHS reads `resolveReadSurface`==='detail' ⇒ always back, independent of event order). <!-- aidcp-cloud 42bee48 test/platform-registry.test.ts + test/platform-surface.test.ts + test/integration/platform-registry-shape.test.ts -->
- [x] 4.2 Adversarial-order test: `page.cards.arrived` interleaved between XHS `note.detail` and `feed.entered` ⇒ still emits `back` (proves control flow no longer depends on event ordering). <!-- aidcp-cloud 42bee48 isolated via closure reason (back_to_feed vs feed_inline_continue) since empty page.cards.arrived itself triggers a get-more scroll -->
- [x] 4.3 Run `npm run test:acceptance`, full `npm test`, `npm run typecheck`; AC-PROTO/AC-RISK stay green. <!-- aidcp-cloud 42bee48 typecheck clean; test:acceptance 50/50; full suite 1942/1942 -->
- [x] 4.4 Rebase on `origin/master`, integrate, push cloud to `master`, deploy dev, record stage-0 zero-behavior under cluster 65. <!-- aidcp-cloud cc2a146 rebased clean + ff-pushed to master + main checkout synced. **DEPLOYED dev**: C1a was already on dev via a prior fleet deploy (it is an ancestor of the deployed master); confirmed live when C1b was deployed 2026-07-14 18:02:52 (batch — C1b's --checksum dry-run showed only C1b's 7 files differed, i.e. C1a's registry.ts/surface.ts already matched dev). Both zero-behavior, healthcheck PASS (active, 8787, PG, Feishu). journalctl stage-0 observation → cluster 65 real-machine backlog. -->

<!-- NOTE: 5.1 (validate) done; openspec archive HELD for batch with C1b (C1b ADDs distinct requirements to the same platform-browse-surface capability; archive both together to keep spec-merge clean). -->


## 5. Change Record

- [x] 5.1 Task record updated with commits (cloud cc2a146), validation (`openspec validate platform-registry-shape --strict` = valid), and dev deploy (batch with C1b bf3d75a, 2026-07-14 18:02:52). <!-- openspec archive held for the next landed+deployed triage batch (with C1b); both spec deltas ADD to the same `platform-browse-surface` capability ⇒ archive together for clean spec-merge. -->
