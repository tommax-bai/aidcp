# Tasks

> **跨仓 change**：云端 + 边缘 + 控制仓文档三处。跨仓协调成本是它此前反复延后的主因（摘出文档「立项时要知道的」第 2 条），
> 所以部署顺序与「什么时候保证才成立」在 §5 单独写清，**不得在验收里假装已生效**。
> **一个数字都不改**：四个常量都是 2026-07-29 真机实测定的，本 change 只改「谁算、怎么传」。

## 0. 开工前置

- [ ] 0.1 `openspec list` 核对有无并行流正压着两份 `protocol.ts`（协议是点名单写热点，须串行）
- [ ] 0.2 实读确认现状并记录：云端四个常量 + `facebookCommentSubmitTimeoutMs` + `pacedText` 组串；边缘四个常量 + 传输余量 + 同样的组串。**确认两侧取值今天一致**（摘出文档里「云端只数正文」那条已于 `aidcp-cloud 9013a5f`（2026-07-29）修复，比文档还早两天，勿照抄旧结论）
- [ ] 0.3 实读手工 `/comment` 路径：它是否走同一个计算方。若另有下发路径，在此记录结论（接上 / 明确走兜底），**不留空**（design Open Questions）

## 1. 协议（两份 protocol.ts 逐字一致）

- [ ] 1.1 `InteractionCommentPayload` 增可选提交预算字段（毫秒），带注释写明：由云端（判定方）算出、边缘据此派生、缺席即走响亮兜底
- [ ] 1.2 两份 `src/comm/protocol.ts`（cloud / edge）**逐字一致**；靠 `npm run typecheck` 在两侧各验一次
- [ ] 1.3 确认**不触发**其余协议同步点：不新增消息类型（`MessageType` 穷举与 `docs/protocol.md` 头部计数不变）、不动动作↔消息映射、**不进主动命令路由白名单**（它是既有命令的字段，不是独立下发的新命令）。逐条在此写明核对结果
- [ ] 1.4 控制仓 `docs/protocol.md` 同步字段说明

## 2. aidcp-cloud — 计算方

- [ ] 2.1 把 `facebookCommentSubmitTimeoutMs` 算出的值随 `interaction.comment` 命令带下去；**常量与公式留在云端**（这是计算方，design D1）
- [ ] 2.2 确认组串口径不变（正文 + 换行 + 群聊码），**不趁机改公式**
- [ ] 2.3 若 0.3 发现手工 `/comment` 走另一条下发路径：按 0.3 的结论接上或记为走兜底

## 3. aidcp-edge — 派生方

- [ ] 3.1 删 `FACEBOOK_COMMENT_TIMEOUT_FLOOR_MS` / `_BASE_MS` / `_PER_CHAR_MS` / `_MAX_MS` 四个常量与那份公式
- [ ] 3.2 改为 `预算 = 收到的值 − 传输余量`；**保留** `FACEBOOK_COMMENT_RESPONSE_SLACK_MS`（它是边缘对自己回执时序的判断，与长度无关，不是公式的第二份，design D2）
- [ ] 3.3 字段缺席时回落到既有的**与长度无关**的默认命令超时，并在回执上标出「本次预算未由云端下发」。**MUST NOT** 保留任何按长度算的兜底（design D3）
- [ ] 3.4 缺席时**不得拒绝命令**（可选字段在两个部署方向上都会短暂缺席，拒绝会把部署时序问题变成评论全线停摆）

## 4. 验证

- [ ] 4.1 云端用例：带群聊码的长评论，断言下发的预算按完整串算（回归既有行为，防 2.2 被顺手改动）
- [ ] 4.2 边缘用例：收到预算 ⇒ 命令预算 = 传输值 − 余量；断言边缘**不再**按长度算（可用「同一长度、不同传输值 ⇒ 不同预算」这条断言，长度不再影响结果）
- [ ] 4.3 边缘用例：字段缺席 ⇒ 走与长度无关的默认值 **且**回执带「未下发」标注。**两条都断言**——只断言前半条会漏掉「静默兜底」这个正是要防的形态
- [ ] 4.4 边缘用例：字段缺席不导致命令被拒
- [ ] 4.5 单一声明点断言：改动预算常量时只有一处声明变化。做法是断言边缘侧**不存在**长度相关的评论预算常量（按符号名扫源码），而不是断言两侧数值相等——后者要求测试读到另一个仓的源码，做不到
- [ ] 4.6 变异检验：把 3.3 的兜底改成「按长度算」，确认 4.3 红；把 3.2 改成忽略传输值仍按长度算，确认 4.2 红。**逐条记下是哪个用例抓住的**
- [ ] 4.7 云端全量闸：`npm run test:acceptance` → `npm test` → `npm run typecheck`（含 `AC-PROTO-*` 两份 protocol.ts 不漂移）
- [ ] 4.8 边缘全量闸：`npm run test:acceptance` → `npm test` → `npm run typecheck`

## 5. 部署与「保证何时成立」

- [ ] 5.1 云端先上：加字段 + 带值。老边缘忽略未知字段、行为不变，可独立部署 dev
- [ ] 5.2 边缘随后：删公式、改派生、加兜底标注。边缘先上也安全（走兜底）
- [ ] 5.3 **如实登记**：两边都上线之前保证不成立；边缘靠出包分发，按 handoff §2.2 的口径不为此专门打包 ⇒ **随下一次出包自然生效**。验收里不得写成「已生效」
- [ ] 5.4 云端部署 dev 后按安全序列验证并追加 `<!-- <date> deployed -->`

## 6. 收口

- [ ] 6.1 提交 + 推送 `aidcp-cloud` / `aidcp-edge` master 与本仓；tasks.md 按 `<!-- <repo> <sha> 备注 -->` 回写（sha 取自**已推送**提交）
- [ ] 6.2 回去更新 `harden-native-engine-runtime-contracts` 的 tasks.md 里那段指向摘出文档的说明：改为「B 节已由 `unify-facebook-comment-budget-source` 承接」
- [ ] 6.3 更新 `docs/cloud-orchestration-residuals-descoped-2026-07-31.md` 的 B 节：标注已立项及 change 名，**并订正 B.1 里已失效的那条立论**（云端只数正文 —— 2026-07-29 已修，文档写于 07-31 仍在引用）
- [ ] 6.4 归档前按 handoff §12.1 逐条对读 delta 与实装，**至少读两遍、第二遍在修完之后**

## 7. 真机验收（不在本文件勾选）

- [ ] 7.1 登记到 `docs/real-machine-acceptance-backlog.md`：装了新包的运营机上，一条带联系方式的长评论走完提交，确认云端未误判超时、去重标记已打（共同前置＝重打一次客户端安装包，与簇 126 同批）
