# curated-create-close-returns-library

Make the close-shaped control on the desktop curated reference-creation page return directly to the inspiration library while preserving list state; keep the page's back control and close behavior elsewhere unchanged.
