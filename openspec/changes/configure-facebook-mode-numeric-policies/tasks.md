## 1. Dependencies and isolated worktrees

- [ ] 1.1 Finish, validate and integrate `environment-level-rule-mode-and-approval`, including its environment-scoped Console/customer API tasks, before implementing this change.
- [ ] 1.2 Wait for the `facebook-rule-mode-without-persona` rule-runtime hotspot, `split-cloud-automation-production-runtime` sync-read/composition hotspot and `wechat-review-residuals` slow-start projection/clamp hotspot to integrate, then rebase this change and re-audit every MODIFIED requirement against the new baseline.
- [x] 1.3 Run `./scripts/task-preflight`, confirm canonical control/Cloud/Console/Edge checkouts remain on their default branches, and create matching isolated `codex/configure-facebook-mode-numeric-policies` worktrees without touching unrelated changes.
  <!-- 2026-07-30: control preflight passed; canonical control/Cloud/Console/Edge stayed on main/master/master/master. Created clean matching Cloud/Console/Edge worktrees at 730f91004f92875934ec7236ae17214596af4365, eda6a43c7c03d0e759e26feb270f342fe3d387d5 and 7f9ea7f369890ca12ff05f878a665c05a64d83f8. Preserved the unrelated untracked Edge release artifacts. Validation: task-preflight plus branch/status/SHA checks. Deployment: not applicable. Deviations: none. -->
- [ ] 1.4 Install physical dependencies with `npm ci --prefer-offline` in each implementation worktree and record the exact baseline SHAs in this task file before code changes.

## 2. Cloud schema and numeric policy authority

- [ ] 2.1 Add an API-owner expand-only migration for independent rule/slow-start policy revision metadata with globally stable ids, typed values/day caps, per-kind global current pointers, append-only audit/idempotency/publish-gate observations, per-env positive/negative Edge capability observations, `client_environments.slow_start_policy_revision_id`, and the same-database slow-start legacy bridge; do not edit existing migrations.
- [ ] 2.2 Add a separate automation-owner expand-only migration that extends rule progress/view facts/batches with stable policy identity, definition identity and both numeric snapshots, widens `view_count` from `0..9` to `0..99`, adds the audited one-active-batch uniqueness guard, creates per-target monotonic `legacy_fill|reject_missing` rollout state plus writer heartbeats, and installs triggers that never read the API database.
- [ ] 2.3 Seed immutable API-owner legacy revisions with rule `5/2` and the exact current Facebook upper-bound matrix (`20/2/0/1/0/1/0`, `25/3/0/1/0/1/0`, `35/6/1/2/0/2/1`, `40/8/2/2/0/2/1`, `50/12/3/3/1/3/2`, `60/15/4/4/1/4/2`, `70/18/5/5/1/5/3`) while preserving all switches/anchors/outcomes; use the same migration-defined legacy rule identity in automation backfill/`legacy_fill` triggers, and make the API bridge reject missing pins after owner current becomes non-legacy while automation `reject_missing` permanently rejects incomplete rule writes.
- [ ] 2.4 Implement strict Cloud schema/range metadata and validation for rule integers `1..100` and exactly 7×7 `0..QUOTA_MAX` (`QUOTA_MAX=100000` for this schema), per-action nondecreasing daily caps; reject missing/extra days, actions, fields, decimals, decreases and all action/Prompt/template/safety inputs atomically.
- [ ] 2.5 Implement the env-scoped capability observation store/resolver first: authenticated positive/negative observations, server time, fixed 30-day positive freshness, downgrade invalidation, missing/unsupported/stale blockers, and exact cohort queries usable by later runtime/preview tasks.
- [ ] 2.6 Implement per-kind draft CAS, normalization/derived previews, immutable publish transactions, monotonic global current pointers, audit actor/request/digest, and idempotent publish replay without permitting an in-place published update or pointer rollback.
- [ ] 2.7 Implement server-side impact previews and publish-time rechecks for draft/current CAS, rule collecting/batch adoption boundaries, active slow-start pins, DEV/OL runtime schema readiness, fresh target-attested `reject_missing` rollout state with complete expected-instance coverage, target applied current/cursor/lag, and the exact affected Edge capability cohort.
- [ ] 2.8 Update schema ownership inventories, owner-scoped migration attribution, migration compatibility gates, `KNOWN_MAX_SCHEMA_VERSION`/required schema checks and config-mirror versions so API/automation bridges never cross databases, strict consumers remain off until each target's locked zero-gap transition, and new runtime never consumes a half-migrated policy.

## 3. Cloud rule-mode runtime

- [ ] 3.1 Keep persisted `definitionSchemaId`/`definitionSchemaVersion` as the fixed action-topology identity and resolve numeric policy separately; preserve readback/mismatch fail-closed behavior and never replace a stored mismatch with compiled or numeric-current identity.
- [ ] 3.2 Update rule admission/progress to adopt only the execution target's atomically applied, fresh current revision with a same-snapshot fresh positive client capability at zero progress with no active round, persist both numeric snapshots, and finish partial collecting sequences from those snapshots across restart/reconnect without requiring old rule definitions in the mirror.
- [ ] 3.3 Update batch creation and rehydration to persist/read policy revision, threshold, cadence, cycle position and `includesJoinContact`; never recompute historical or in-flight semantics from current constants.
- [ ] 3.4 Preserve atomic threshold-close, view dedupe, one-batch concurrency, action order, just-in-time risk/session/approval/target gates, honest partial outcomes and no-debt behavior for non-default thresholds/cadences.
- [ ] 3.5 Update rule configuration/runtime projections and blockers to distinguish environment enablement, API owner current, target applied current/cursor/propagation lag, account adopted revision, pending-safe-boundary adoption, definition mismatch and unavailable/stale/incompatible states without compiled numeric fallback.

## 4. Cloud slow-start and risk runtime

- [ ] 4.1 Route customer PUT, admin PUT and programmatic environment create-intent through one atomic slow-start transition service: first enable writes the Shanghai-day anchor plus compatible owner-current pin, repeated enable preserves both, disable clears both, create-intent failure rolls back environment/ownership/intent, and non-legacy enable enforces fresh client capability.
- [ ] 4.2 Replace Facebook `FB_COLD_START_PLANS` runtime use with a synchronous typed provider for the environment active pin while keeping XHS/history-path behavior and `collect`/`comment_like`/`dm_reply=0` unchanged.
- [ ] 4.3 Derive minute/hour caps from the pinned daily caps with the existing formulas and keep `effectiveQuotas = min(risk-scaled or explicit safety quota, slow-start derived cap)` for every action/window.
- [ ] 4.4 Fail closed with named blockers when an active pin, referenced revision, complete 7×7 payload, platform, binding or freshness is unknown; do not fall back to the global current revision, another platform curve or compiled Facebook numbers.
- [ ] 4.5 Update slow-start/current/active/next-enable projections and the search quota path to consume the exact pin used by `RiskController`, merge the `wechat-review-residuals` platform eligibility contract (`eligible`/`ineligibleReason`, both anchor paths, platform-wide no-clamp outside the whitelist), and preserve environment rebinding, global disable and Cloud risk-state single-writer semantics.

## 5. Cloud service split and HTTP contracts

- [ ] 5.1 Extend the existing `client_environment_automation` repeatable-read snapshot to atomically include environment gates/anchors/pins, rule enablement, both owner-current definitions, every still-referenced immutable slow-start definition, and per-env capability supported/observedAt/freshUntil; persist per-target applied current/cursor/digest/freshness, keep old rule settlement on progress/batch snapshots, remove rule-policy freshness coupling to content-schedule, and extend authenticated automation health with rollout phase/epoch, expected-instance-set digest, coverage, builds/contracts and freshness.
- [ ] 5.2 Apply policy plus capability observations atomically in automation with target/cursor/schema validation, register transport/policy snapshot freshness as the runtime gate, and add readiness/health diagnostics; stop new affected platform work after transport/policy freshness expiry, but use missing/negative/expired capability only to block subsequent non-legacy enable/adoption without interrupting an existing active pin or adopted revision.
- [ ] 5.3 Implement Panel JWT routes for complete policy read, cursor revision history, immutable revision detail, strict draft CAS, preview, idempotent publish/result lookup and cursor audit; enforce the server-side publish gate, return its observation, full write-after-read DTOs, 409 conflicts, 503/unknown dependencies and same-key/same-payload `result_unknown` reconciliation rather than `{ok:true}`.
- [ ] 5.4 Extend env-scoped customer-auth rule-mode GET/PUT responses with separate, complete owner-current/target-applied/adopted envelopes (`envKey/kind/revision/schemaVersion/complete/asOf/freshUntil`, optional digest, plus typed numbers), keep PUT body strictly `{enabled:boolean}`, preserve unbound/offline environment writes, and always allow a proven owner to disable rule mode when policy detail is unavailable.
- [ ] 5.5 Extend env-scoped customer-auth slow-start GET/PUT and create-intent responses with separate complete active/next envelopes using `days[7].dailyCaps`; reserve top-level `dayQuotas` for one bound account's same-controller current-day final quotas, while keeping write bodies strictly limited to existing intent/`{enabled:boolean}` fields, ownership checks and minimum disclosure.
- [ ] 5.6 Add compatibility coverage proving old customers may ignore additive response fields, new clients treat missing/malformed policy detail as unavailable, and neither customer-auth path exposes drafts, audit actors, other environments or numeric writes.
- [ ] 5.7 Accept `X-AIDCP-Client-Capabilities: facebook_mode_policy_projection_v1` only after authenticated owned-environment validation or inside a valid create-intent completion; record positive when present and negative when missing/invalid, feed the resolver from task 2.5, and expose exact compatible/incompatible/unknown envKey cohorts without a manual override.
- [ ] 5.8 Enforce the default-off publish gate only on `AIDCP_DEPLOY_ENV=ol` with explicit enabled=true and nonempty changeRef; require fresh DEV/OL `reject_missing` attestations plus complete API/automation expected-writer coverage, append target/build/instance/gate observations, expose visible read/preview/health reasons, associate each publish audit, and provide no Console, cross-database or recovery bypass.

## 6. Admin Console

- [ ] 6.1 Add typed `facebook-mode-policies` API clients/DTO validators for schema metadata, publish-gate observations, drafts, revision history/detail, previews, publishes, conflicts, same-key result lookup, unknown results and cursor audit without introducing a generic policy DSL.
- [ ] 6.2 Register `/mode-policies` in shared route metadata and the System navigation group for desktop and narrow layouts.
- [ ] 6.3 Build the rule policy editor with only `viewThreshold` and `joinEveryNRounds`, Cloud-provided bounds, local feedback plus final server validation, and separate draft/published/current truth.
- [ ] 6.4 Build the slow-start editor as a fixed 7-day × 7-action daily-cap matrix with nondecreasing validation and read-only Cloud-derived minute/hour previews; do not render `collect`, `comment_like`, `dm_reply`, action, Prompt or safety controls.
- [ ] 6.5 Build impact preview, visible server publish-gate status, publish confirmation, audit/history comparison and “create new draft from historical values” flows with CAS/409 handling, non-optimistic pending states, complete readback and `result_unknown` reconciliation; provide no UI control to enable the release gate.
- [ ] 6.6 Extend the environment page with read-only owner-current/target-applied/cursor/lag/adopted and slow-start current/active/next revision states plus safe-boundary copy; do not add environment/customer numeric or version selectors.
- [ ] 6.7 Update Content Schedule to show owner-current→target-applied propagation, target-applied→account-adopted safe-boundary state, adopted dynamic threshold/cadence and named unavailable blockers while keeping policy editing out of the account row.
- [ ] 6.8 Add Console tests for non-default values, strict 7×7 rendering, loading/unknown/503/409/timeout states, failed-write recovery, audit pagination, route/navigation accessibility, no forbidden controls and late-response scope isolation.

## 7. Edge customer UI

- [ ] 7.1 Extend shared customer-auth HTTP, main/preload IPC and renderer DTO validation for complete owner/applied/adopted rule envelopes and active/next slow-start envelopes; send the fixed projection capability header on authenticated environment calls without adding a numeric write, Native action or Protocol v2 payload.
- [ ] 7.2 Replace the copied Facebook seven-day HTML table and fixed rule description with Cloud-driven rendering, including the missing `search` column and revision/current/adopted/active labels.
- [ ] 7.3 Validate each policy atomically by envKey/kind/revision/schemaVersion/complete/typed payload/range/asOf/freshUntil and validate digest when present; render malformed or missing details as unavailable while preserving independently known mode-switch truth.
- [ ] 7.4 Add bounded policy refresh/invalidation on environment selection, page entry, successful write, revision change and freshness expiry; cache only whole envKey+revision values and prevent late A/revision responses from changing B/newer state.
- [ ] 7.5 Add stale last-good/asOf presentation, old-Cloud additive compatibility, client-downgrade negative-observation and non-default policy UI tests, and assert the renderer no longer contains authoritative fallback `5/2` or Facebook day-cap numbers.
- [ ] 7.6 Run focused Edge tests plus the full Edge test/typecheck gates and record that source/DEV validation does not update installed clients; do not build an installer without explicit release authorization.

## 8. Validation and gated rollout

- [ ] 8.1 Add owner-scoped migration/store tests for exact legacy seeding including D3/D4 `join_group=1`, stable cross-owner legacy identity, no cross-database trigger/read, API pin fill/clear while legacy, API current-CAS versus old-enable serialization, automation `legacy_fill` backfill, phase-switch versus old-INSERT serialization, monotonic `reject_missing`, incomplete-instance/expired-heartbeat rejection, locked catch-up census, idempotency, strict keys/ranges/nondecreasing caps, CAS rollback and `view_count` above 9.
- [ ] 8.2 Add rule acceptance/integration tests for non-default thresholds/cadences, definition-identity mismatch, partial progress and active-batch publication without historical rule definitions in the mirror, restart, concurrent close, immutable `includesJoinContact`, rebinding and all unknown/stale fail-closed paths.
- [ ] 8.3 Add slow-start/risk/search tests for active pin preservation, close/reopen adoption, all seven days/actions, fixed-zero actions, derived minute/hour formulas, stricter-risk precedence, global disable and missing-policy fail-closed behavior.
- [ ] 8.4 Add sync-read tests for one-snapshot reference/definition/capability atomicity, positive-to-negative downgrade, missing/unsupported/stale adoption blockers, separation of transport/policy freshness from capability freshness, continued existing pin/adopted execution under a fresh snapshot with negative/expired capability, owner-current versus target-applied propagation lag, same-cursor drift, target mismatch, freshness expiry, monolith local authority and independent API/automation delivery.
- [ ] 8.5 Run Cloud risk/rule acceptance suites, full Cloud tests and typecheck; run full Console tests/build/typecheck; retain exact exit codes and investigate any truncated output before claiming success.
- [ ] 8.6 Re-run `openspec validate configure-facebook-mode-numeric-policies --strict`, update completed checklist items with repo commit SHAs/validation/deviations, and keep Protocol v2/Native action contracts unchanged.
- [ ] 8.7 Following deployment preflight and backup rules, deploy the API-owner and automation-owner expand migrations/bridges plus policy-aware API/rule dual-writers to DEV first; verify stable legacy behavior and keep every automation target in `legacy_fill`, strict consumers off and publish gate disabled. OL writer rollout remains a separate explicit release authorization.
- [ ] 8.8 After separately authorized OL compatibility rollout, independently freeze each owner's authoritative DEV/OL desired/restartable writer inventory and require every listed instance to report an allowed policy-aware build/contract within the existing heartbeat TTL with old restartable artifacts removed; in automation only, lock each target phase, wait out old rule writes, run idempotent catch-up/census to zero gaps, atomically switch to permanent `reject_missing`, project the fresh attestation to API, combine it there with independent API-writer coverage, and only then enable strict consumers.
- [ ] 8.9 Validate non-default values in DEV only through draft/preview, code-level tests or an isolated fixture that cannot advance shared `facebook_mode_policy_current`; do not perform a DEV-only publish against the shared DEV/OL business database.
- [ ] 8.10 Keep the publish gate disabled until a separately authorized compatible Edge installer has proven positive capability coverage and DEV/OL runtime readiness includes fresh permanent `reject_missing` attestations plus complete API/automation expected-writer coverage; then obtain explicit global-behavior-change authorization, deploy OL API with a nonempty changeRef and gate enabled, publish once, verify owner current plus each target applied current/cursor/lag, redeploy gate-off and verify the durable gate observation without treating code/config acceptance as a successful Facebook action.
- [ ] 8.11 Add rollback-floor readiness and tests: migrations never roll back and pre-policy runtime is permanently forbidden; any legacy-valued recovery revision must use the same separately authorized short-lived OL gate and normal publish checks, after which the gate closes and only policy-aware compatible code remains eligible regardless of reference drain.
