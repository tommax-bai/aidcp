## 1. Preconditions

- [ ] 1.1 Confirm `platform-abstraction-layer` is archived with xhs zero-regression.
- [ ] 1.2 Confirm `facebook-browser-env-and-login` has recorded passing F1 and F2 Phase-0 gates. This change explicitly revises the env-change gate (its task 7.5 permits proceeding "or the design is revised"): F3 multi-day low-frequency stability observation is NOT a blocker for feature work — the team pushes features fast and batches all long-duration stability observation into a final completion pass (see section 8). F1 (server-confirmation mechanism) IS required and can be unblocked immediately by having the disposable account publish its own test post (a self-owned post removes permission-gate variables and isolates verification of the mechanism itself). F2 (login/checkpoint honest-stop) is already passed.
- [ ] 1.3 Decide whether target configuration UI is in scope; if yes, open `aidcp-console` same-name worktree, otherwise keep config to cloud/API/storage.

## 2. Cloud Target Config and Cron

- [x] 2.1 Add per-account Facebook comment target config storage/API: a keyword list + an allowed-container list (operator's own / joined Pages and Groups) keyed by account. Empty keywords OR empty containers fail closed (honest no-op). (Design revised 2026-07-07: keyword+container search replaces the earlier fixed-URL list.)
  <!-- aidcp-cloud 5a557d2: FacebookCommentConfigStore (account_facebook_comment_config table, JSONB keyword/container arrays) + migrations/0034; sanitize/invalid/retired/ghost-account rejects; effectiveConfigFor() is the single fail-closed point (enabled only when BOTH lists non-empty). Panel API GET+PUT /api/accounts/:id/facebook-comment-config (GET before wildcard; JWT-protected). 8 store tests; full suite 1438/1438. -->
  <!-- aidcp-console e13626d: accounts page platform column (Tag+filter) + xhs-only profile-link gate (no broken FB link); FacebookSearchConfig modal (keyword + container tag editors) shown only for platform==='facebook', GET prefill + PUT save + invalidate(['accounts']); FacebookCommentConfig DTO. 2 component tests; full suite 58 pass/1 skip; build ok. Consumption by runFacebookTargetedTask is 2.2 remaining. -->
  <!-- OPEN: what exactly a 'container' identifier is (Page slug / numeric group id / URL) must be pinned when edge 4.1 in-container search is implemented; UI currently accepts free-text tags. -->
- [x] 2.2 Route Facebook automatic comments through the EXISTING two comment entry points instead of a new cron: the schedule-driven comment action (`commentEnabled`/`commentDailyCap`) and the Feishu `/comment` command. Both already resolve the per-account platform profile via the account store; add a `facebook` entry to `PLATFORM_REGISTRY` and let both entry points reach the Facebook targeted-comment execution for `accounts.platform='facebook'`. Execution (`runFacebookTargetedTask`): pick a keyword at random from the account config, search ONLY within one configured container, pick a candidate post from the in-container results (bounded), then compose + validate (+ approval). MUST NOT whole-site search. Before dispatch check account status, kill switch (`AIDCP_FB_COMMENT_AUTO`), config non-empty (fail closed), risk gate (`canDo('comment')`), and the existing daily cap (`commentDailyCap`/`commentedTodayCount`).
  <!-- aidcp-cloud fd92ffb PARTIAL: added facebook PLATFORM_REGISTRY entry + FB_COMMENT_PROFILE (commentProfileForPlatform('facebook') no longer throws); CommentScheduler.triggerManual/triggerTargeted now honestly reject facebook (never fall back to the xhs search flow) until FB targeted execution lands. Remaining: runFacebookTargetedTask (random keyword -> in-container search -> bounded candidate extraction -> compose+validate+approve), kill switch (AIDCP_FB_COMMENT_AUTO), config + daily-cap dispatch gating. -->
  <!-- aidcp-cloud 8f942e0 PARTIAL (cloud shadow-first orchestration, physically posts nothing): runFacebookTargetedTask in CommentScheduler routes both entry points to a single-convergence gate chain — kill switch (AIDCP_FB_COMMENT_AUTO default off) -> config fail-closed (keywords AND containers non-empty) -> random keyword+container -> compose (unattended, no approval) -> validateFacebookComment (reject-only) -> SHADOW stops (audit, no submit/record) -> real path adds canDo + daily cap (AIDCP_FB_COMMENT_DAILY_CAP default 2) -> real posting NOT wired (honest not_wired, never posts). Never uses onCommentTakeoverStart (2.4). Un-injected FB deps keep honest-reject (zero-regression). 9 orchestration tests. REMAINING: real posting = edge FB comment capability (4.x) + protocol payload fields (search.execute container / note.open url) + bounded timeout + silent-drop fix + F1-gated (separate real-machine follow-up). -->
  <!-- DONE aidcp-cloud 757a8fc + aidcp-edge 9c3ee01: real posting wired (still fail-closed, physically posts nothing unless AIDCP_FB_COMMENT_AUTO=true, default off). runFacebookTargetedTask real path (after the unchanged kill-switch/config/canDo/daily-cap gate chain) now dispatches through buildFacebookEdgeSteps: search.execute{container} -> pick first un-deduped candidate permalink -> note.open{url} -> interaction.comment; each step bounded-timeout (FACEBOOK_STEP_TIMEOUT_MS=28s, no watchdog on this path). Container search stays in-container only (edge validates allowed FB URL, never whole-site). 7 real-send orchestration tests + 9 edge-steps tests. Cloud full 1558/1558. -->
- [x] 2.3 Add shadow mode that runs selection/composition/validators without posting, recording risk, marking cooldown, or deduping as posted.
  <!-- aidcp-cloud 8f942e0: shadow (AIDCP_FB_COMMENT_SHADOW) short-circuits IN THE CLOUD RUNNER before any submit command — audits shadow_ok, never submits/records/marks-cooldown/dedupes. Tested: shadow never emits an edge command. (Real-browse shadow — actually opening FB to check candidate relevance without posting — is a real-machine follow-up with the edge capability.) -->
  <!-- DONE aidcp-cloud 757a8fc: shadow short-circuit preserved AHEAD of the real-send path (only reached when AIDCP_FB_COMMENT_AUTO && !shadow); real-send tests assert shadow never dispatches any edge command (posted == []). -->


- [ ] 2.4 Ensure Facebook scheduled accounts are never added to xhs/manual comment quota-bypass collections.
- [x] 2.5 Provision account platform at handshake insert-time so brand-new Facebook accounts are not deadlocked: `ensureAccount` accepts the edge-declared platform and writes it only when INSERTing a new row (never overwriting an existing row's platform), refreshing the platform cache on the same write path so a rejected first handshake cannot poison the cache. Operator escape hatch (pre-insert a `platform='facebook'` row via psql before first connect; correcting an existing row still requires psql + cloud restart) goes in the runbook, not code.
  <!-- aidcp-cloud fd92ffb: ensureAccount(accountId, platform?) INSERT ... platform ON CONFLICT DO NOTHING RETURNING platform; cache refreshed only on new-row insert (existing rows untouched). Threaded platform through connection-runtime ensureAccount call + server wiring. Regression test in connection-runtime.test.ts locks: brand-new facebook account first handshake no longer platform_mismatch. -->

- [ ] 2.6 On login-required / checkpoint / temporarily-blocked outcomes, raise an alert via the existing alert store plus a Feishu card (reuse the store-then-Feishu, cooldown-deduped pattern used for captcha coordination) and skip that account on subsequent triggers until an operator re-logs-in and resumes it; recovery is manual `/resume` (or next preflight probe pass) in v1.
- [ ] 2.7 Persist one audit row per comment trigger (outcome + reason; shadow runs write to the same table with a shadow flag) so shadow results and silent stalls are queryable rather than journalctl-only; raise an alert after N consecutive blocking outcomes (login_required / quota_denied / no_targets / compose_skipped) so an unattended stall has bounded detection latency.
  <!-- aidcp-cloud 8f942e0 PARTIAL: FacebookCommentAuditStore (facebook_comment_audit table + migration 0035) — best-effort per-trigger row (accountId/outcome/reason/shadow/keyword/container/text_length; NO comment body, length only). runFacebookTargetedTask writes a row for every outcome (shadow_ok/no_targets/compose_skipped/quota_denied/not_wired). 2 store tests (insert + swallow-on-error). REMAINING: consecutive-blocking-outcome alerter (mirror pacing-saturation-alerter / captcha-coordinator store-then-Feishu). -->

- [x] 2.8 Add a platform gate at session start so a Facebook account (whose registry entry does not declare `browse`) never starts the xhs browse role loop: gate `canStartSession` on the platform registry capabilities and refuse with a named reason instead of spinning up a zombie session and watchdog.
  <!-- aidcp-cloud fd92ffb: canStartSession refuses (platform_no_browse) when platformRegistryEntry(accountPlatform).capabilities excludes 'browse'; placed before the persona gate so FB is not mislabeled needs_persona_setup, and NOT via onSessionRejected (avoids a false persona alert). Platform threaded via DispatcherBuildContext.platform -> RoleDispatcher.accountPlatform. Test locks: facebook dispatcher emits no feed.entered{session_start}; xhs still starts. -->


## 3. Composition and Validators

- [ ] 3.1 Extract shared compose/cleanup helper if needed while keeping xhs approval behavior unchanged.
- [x] 3.2 Implement Facebook deterministic validators for URLs/domains/contact info/@mentions/length/spam phrases/relevance/empty text.
  <!-- aidcp-cloud 6269a18: src/comment-agent/facebook-comment-validators.ts — pure reject-only validateFacebookComment(text, ctx) returning first failing reason (empty/low_signal/too_short/too_long/contains_url/contains_contact/contains_mention/spam_phrase/weak_relevance). Length from FB_COMMENT_PROFILE; relevance gated on provided targetKeywords (substring match, CJK-safe). Not yet wired into an execution path (runFacebookTargetedTask is 2.2 remaining). -->
- [x] 3.3 Add tests for validator reject matrix and for no auto-fix/template fallback after rejection.
  <!-- aidcp-cloud 6269a18: test/comment-agent/facebook-comment-validators.test.ts — 11 tests across the full reject matrix + explicit no-auto-fix guarantee (a rejected input never returns { ok:true } with modified/postable text). full suite 1430/1430. -->


## 4. Edge Facebook Browse and Comment

- [x] 4.1 Implement Facebook in-container keyword search capability: open a configured container (Page/Group), run its in-container search for a given keyword, and extract bounded candidate posts from the results. MUST NOT perform whole-site Facebook search.
  <!-- aidcp-edge 9c3ee01: FacebookCommentExecutor.searchInContainer (src/facebook/comment-executor.ts). Container must be a whitelist-allowed FB URL (isUrlAllowedByTargetDescriptor); builds an in-container search URL (group -> /groups/<id>/search/?q=, page -> /<page>/search/?q=), bounded scroll-coax + collectFacebookPageStructure to extract <=maxCandidates candidate permalinks. Non-member/pending/question-gated -> permission_gated; unclassifiable container -> permission_gated. NEVER whole-site. Container semantics PINNED = operator-pasted FB URL. -->
- [x] 4.2 Implement Facebook comment editor input and pre-submit checkpoint/login detection.
  <!-- aidcp-edge 9c3ee01: submitComment upgrades editor-probe to production — F1 patch #1 scroll-to lazy comment editor (focusEditorWithScroll, bounded), controlled dispatchKeystrokes input, submit-control discovery. Fresh overlay.probeNow() before and mid-flow (blockingReason) -> login_required / blocked_by_captcha honest stops before any click; captcha mid-flow clears editor without submitting. -->
- [x] 4.3 Implement server-confirmed verification using F1-approved permalink/id or delayed requery strategy.
  <!-- aidcp-edge 9c3ee01: F1 patch #2 buildScopedVerifyJs — after submit + Page.reload, confirmation is scoped to (own numeric identity author link) AND (target-post comment region) AND (text fragment). Replaces the phase-0 probe's full-page indexOf (which false-passed on the draft still in the editor / optimistic render). Own id unknown -> never submits (identity_unknown); confirmation miss -> verification_ambiguous, never optimistic. -->
- [x] 4.4 Return honest non-success reasons for no target, editor failure, blocked state, submit failure, and ambiguous verification.
  <!-- aidcp-edge 9c3ee01: FacebookCommentStepReason enum (permission_gated/login_required/blocked_by_captcha/no_candidates/not_facebook/open_failed/identity_unknown/editor_not_found/submit_control_not_found/submit_control_disabled/marker_not_accepted/verification_ambiguous/nav_error). FacebookCommentHandler mirrors xhs reply contracts (page.cards/note.detail/action.completed) and — silent-drop fix — replies capability_unsupported for any whitelisted command the platform can't serve (never log-and-drop). 16 executor + 9 handler tests. -->

## 5. Risk, Cooldown, and Accounting

- [x] 5.1 Wire automatic Facebook comment success to `interaction.occurred`/`RiskController.record('comment')` only after verified `ok:true`.
  <!-- aidcp-cloud 757a8fc + aidcp-edge 9c3ee01: on server-confirmed success edge sends action.completed{action:'comment', ok:true}; the EXISTING handler.ts auto-path emits interaction.occurred{comment} -> server.ts RiskController.record('comment'). FB accounts are never in manualCommentAccounts (2.4), so the record fires. runFacebookTargetedTask NEVER calls record() itself (no double count). Non-success returns (ambiguous/failed/blocked) send ok:false -> no interaction.occurred{comment} -> no risk record. -->
- [x] 5.2 Reuse the existing global comment cooldown for Facebook automatic comments through the shared pipeline (no Facebook-specific long cooldown — accounts run one platform per environment, so per-account isolation suffices); the cooldown timestamp is still marked only after verified success.
  <!-- aidcp-cloud 757a8fc: FB automatic comments run on the scheduled/command path, whose effective rate control is the risk-quota comment burst caps (minute=1, hour=4, day) + the per-account daily cap (AIDCP_FB_COMMENT_DAILY_CAP, default 2), both checked before dispatch; the success-based rate signal (RiskController.record) is marked only after verified ok:true (5.1). No FB-specific long cooldown (design decision). NOTE: ActionCooldownGate is wired only to the autonomous browse-loop dispatcher, not the command/scheduled path (pre-existing; xhs /comment does not consult it either) — out of scope here. Anti-double-post uses the dedup ledger marked at DISPATCH (see 5.4 / §5.4 below), which is deliberately attempt-based (decoupled from success) so a verification false-negative never re-comments the same target; that same mark makes facebookCommentedToday count the attempt (conservative toward the daily cap). -->
- [x] 5.3 Add tests proving shadow, failed submit, validator rejection, and ambiguous verification do not record risk or cooldown.
  <!-- aidcp-cloud 757a8fc: shadow-first block asserts shadow/no_targets/compose_skipped/quota_denied never dispatch (posted == []); real-send block asserts verification_ambiguous / open-failed / login_required / all-deduped audit non-'commented' outcomes and (ambiguous) never emit action.completed{comment,ok:true} -> no interaction.occurred{comment} success record. AC-RISK-* acceptance already locks record()==false past quota and no self-escalation. -->
  <!-- ANTI-DOUBLE-POST (§5.4): dedup ledger is marked at DISPATCH (before submit), decoupled from success — a verification false-negative or network jitter never re-comments the same permalink; tests assert the mark happens on attempt (ambiguous still marked) but NOT before open success / not for already-deduped candidates. -->


## 6. Console/UI If Included

- [x] 6.1 Add target management UI/API integration only if required for operations in this change.
  <!-- aidcp-console e13626d: accounts page split by platform (platform column + filter) and a Facebook-only "配置搜索词" entry (keyword + container config via panel API). Operator decision 2026-07-07: config lives in console UI + panel API. -->
- [x] 6.2 Run console focused tests, full tests, typecheck, and build if console is touched.
  <!-- aidcp-console e13626d: FacebookSearchConfig.test.tsx passes; full vitest 58 pass/1 skip; tsc --noEmit clean; vite build ok. -->


## 7. Rollout and Validation

- [x] 7.1 Run cloud focused tests for cron, validators, kill switch, platform account enumeration, risk, and cooldown.
  <!-- aidcp-cloud 757a8fc: comment-scheduler.test.ts (shadow-first + real-send blocks, 29 tests) + facebook-edge-steps.test.ts (9) + facebook-comment-validators + facebook-comment-audit-store all pass; full cloud suite 1558/1558, acceptance 45/45, typecheck clean. -->
- [x] 7.2 Run edge focused tests for Facebook browse/comment/verification, then edge full tests, acceptance, and typecheck.
  <!-- aidcp-edge 9c3ee01: test/facebook/comment-executor.test.ts (16) + comment-handler.test.ts (9) + platform/driver + AC-PROTO-08 pass; full edge suite 743/743, acceptance 14/14, typecheck clean. -->
- [ ] 7.3 Run a short shadow sanity pass on one disposable account (hours, not days — enough to confirm audit rows, validator reject rate, target relevance, and that the login/checkpoint alert loop fires); record findings without secrets. Multi-day shadow observation is deferred to section 8.
  <!-- REAL-MACHINE PENDING (registered in docs/real-machine-acceptance-backlog.md): needs cloud master redeploy to dev + AIDCP_FB_COMMENT_SHADOW=true on a disposable FB account. Login/checkpoint alert loop is 2.6/2.7 (not yet built) — shadow sanity can still confirm audit rows + validator reject rate + candidate relevance. -->
- [ ] 7.4 Enable real posting on one disposable account with a 1-2/day cap (the existing global comment cooldown applies unchanged) and confirm one full verified-success path end to end. Multi-day real-posting observation is deferred to section 8.
  <!-- REAL-MACHINE PENDING / BLOCKING GATE: before flipping AIDCP_FB_COMMENT_AUTO=true, re-confirm F1 on the PRODUCTION executor (submitComment scoped verify) on a disposable account — the phase-0 gated-submit probe used full-page indexOf; the production executor uses own-identity+target-post scoped verify and must be proven on a real machine first. Also requires per-profile proxy (§8.4) — China-IP no-proxy write ops are checkpoint-risky. -->
  <!-- 2026-07-09 ARMED on dev (operator authorized real post): disposable AdsPower profile k1ebny3j / FB account 61591753702668 (nickname FBProbe) verified logged-in (real numeric id read live: profile.php?id=61591753702668; edge halted first on an in-place identity hydrate-race, then connected via AIDCP_ACCOUNT_ID override with the verified real id — hydrate-race registered as an edge follow-up in backlog 簇14). Edge running from master worktree (latest executor + container-name), connected to dev, FB comment handler registered, platform gate confirmed (platform_no_browse — no zombie xhs session). Dev cloud: AIDCP_FB_COMMENT_AUTO=true + AIDCP_FB_COMMENT_DAILY_CAP=1; persona bound (Puerto Rico history persona), config keyword ["Puerto Rico"] + container the operator-chosen joined group https://www.facebook.com/groups/1109299026882957 ("Puerto Rico Y Sus Encantos e Historia"). NOT YET FIRED: no clean immediate autonomous trigger exists (Feishu /comment can't be sent by the agent; panel HTTP triggers need a JWT; the schedule fires at a per-account hashed minute up to ~1h out) — and a first irreversible post by a first-run executor is safer watched. TRIGGER = operator runs Feishu `/comment FBProbe` (cap enforces exactly one), or enable the content schedule for hands-off. On fire, watch facebook_comment_audit for the outcome; F1-production re-confirm (BLOCKING) is satisfied by observing a `commented` outcome that only server-confirms via own-identity+target-post scoped verify. -->
  <!-- 2026-07-09 dev deploy (container-name + latest FB): cloud master 018ce5f live on dev (rsync src, deps unchanged, healthcheck green — service active NRestarts=0, 8787+8090, Feishu onReady, PG select 1, isales all active); console master 8c26990 deployed to /opt/aidcp/console (http 200). Backups cloud.bak.20260709-002620.tar.gz + .env.bak.20260709-003539 + console.bak.20260709-002710.tar.gz. See change facebook-container-display-name (group name, never id). -->
- [x] 7.5 Commit sibling repo work, record commit SHAs and validation/probe/deployment notes in this `tasks.md`.
  <!-- aidcp-cloud master 24ef9d4 (rebased onto 148b558 calendar-day-quotas, clean); aidcp-console master e13626d. -->
  <!-- dev deploy 2026-07-07: cloud master 24ef9d4 live on dev (121.89.85.150) — rebased + re-ran typecheck/acceptance 44/44/full 1443/1443 before ff-merge; ECS probe found a concurrent session had rsynced the FB files at 19:26 but NOT restarted, so the completing step was systemctl restart aidcp-cloud.service (content already byte-identical to 24ef9d4 per rsync -c dry-run, so no re-rsync). Backups cloud.bak.20260707-192945.tar.gz + .env.bak + console.bak. Healthcheck: service active/NRestarts=0/restart 19:32:52, 8787+8090 listening, Feishu WSClient onReady, PG select 1, accounts platform still all xiaohongshu (4/4, zero-regression), account_facebook_comment_config table auto-created (0 rows, fail-closed), panel /api/version 200, /facebook-comment-config route 401 (wired), isales-api/engine/scheduler/worker all active (red line intact). console e13626d already deployed to /opt/aidcp/console at 18:23 (FB "配置搜索词" build live). FB comment EXECUTION still unwired (2.2) → whole feature fail-closed; change stays ACTIVE, not archived. -->
  <!-- 2026-07-08 real-send landed: aidcp-cloud master 757a8fc (buildFacebookEdgeSteps + runFacebookTargetedTask real path), aidcp-edge master 9c3ee01 (FacebookCommentExecutor + handler + silent-drop fix), both ff-pushed. Control repo docs/protocol.md (container/url) + this tasks.md on main. STILL fail-closed: AIDCP_FB_COMMENT_AUTO default off → physically posts nothing. -->
  <!-- 2026-07-08 deployed: cloud master 757a8fc live on dev (121.89.85.150) via clean `git archive origin/master` snapshot rsync (src, --exclude .env/node_modules/.git, no --delete). Deps unchanged (all 6 already present incl. satori/@resvg — this deploy also brought dev in sync with textcard-cover-form + FB shadow, since dev was behind at ~24ef9d4). Backup cloud.bak.20260708-184330.tar.gz + .env.bak.20260708-184330. Healthcheck PASS: service active NRestarts=0, 8787+8090 listening, Feishu WSClient onReady, PG select 1, accounts platform xiaohongshu×4 (zero-regression, none mis-flipped), account_facebook_comment_config + facebook_comment_audit tables self-built, AIDCP_FB_COMMENT_AUTO absent in .env (fail-closed, physically posts nothing), isales-api/engine/scheduler/worker all active (red line intact). Edge is a local installer (not on ECS) — operator must repackage aidcp-edge to use FB comment execution on a real machine. Real posting still gated on BLOCKING real-machine gates (backlog 簇 14): F1 re-confirm on production executor + per-profile proxy. -->

- [ ] 7.6 Run `openspec validate facebook-scheduled-comment --strict`.

## 8. Deferred Stability Completion (batched at the end)

Long-duration stability work is intentionally deferred out of the fast feature push and completed together once the feature functions end to end. None of these gate feature landing; all MUST be done before scaling beyond one disposable account or raising caps.

- [ ] 8.1 F3-style multi-day low-frequency AdsPower/Facebook environment stability observation (>=3 counted calendar days), recorded without secrets.
- [ ] 8.2 Multi-day real-posting observation on the disposable account (sustained 1-2/day) to confirm no delayed checkpoints, verification drift, or alert-noise regressions.
- [ ] 8.3 Cooldown / daily-count persistence across restarts (currently in-memory) — required before multi-account restart-burst scenarios.
- [ ] 8.4 Per-profile proxy/egress management and egress-stability observation before productionization.
- [ ] 8.5 Re-evaluate caps and the Scale-out boundary checklist (proposal) using the accumulated stability evidence.
