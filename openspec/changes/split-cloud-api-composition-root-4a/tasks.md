## 1. Admission, scoped census, and root blocker ledger

- [x] 1.1 Run `scripts/task-preflight`; create isolated control, Cloud, kernel, transport, API, automation, and content worktrees/branches for this change, preserving canonical defaults and unrelated files.
  <!-- 2026-07-26: preflight PASS. Created codex/split-cloud-api-composition-root-4a worktrees for control, aidcp-cloud, aidcp-kernel, aidcp-transport, aidcp-api, aidcp-automation and aidcp-content from the exact 3b default SHAs. Every code worktree has a physical non-symlink node_modules directory. Canonical repos remain on main/master and clean; preserved control-only user files output/pdf/aidcp_progress_report_2026-07-16_2026-07-26.pdf and tmp/pdfs/. -->
- [x] 1.2 Re-run two independent gates against the implementation SHA and `boundaries/module-ownership.json`: (a) a source-derived consumer census proving the scoped 20-group/55-slot surface—16/50 automation→API, 3/4 API→automation, 1/1 API→content—and excluding owner-local `listPendingApprovalIds` / `pendingPublishPreviewForRecord`, `claimExecutionTarget`, chat resolve/bind and 3b approval; (b) an independent-root blocker ledger enumerating every remaining 4b mirror and PersonaGenerator-external content-owner dependency. Neither gate may infer full-root boot from scoped closure.
  <!-- 2026-07-26 EVIDENCE WITHDRAWN / TASK REOPENED: the earlier 19-group/45-slot census (16/41, 2/3, 1/1) counted server-direct calls but missed nine publish-log methods consumed transitively by PublishDispatcher/ScheduledPublishReconciler/scheduler, and omitted the API→automation applyPublishUiUpdate command. Its 3/3 inventory test is not accepted evidence for this task. Replace it with a source-derived 20/55 census plus a separately source-derived independent-root blocker ledger; preserve the old artifact only as superseded history until the replacement evidence lands. -->
  <!-- 2026-07-26 replacement evidence at aidcp-cloud@b94f6ad: source-derived census = 20 groups / 55 method slots / 55 real production AST consumers / 2 reviewed alternates, split 16/50 automation→API + 3/4 API→automation + 1/1 API→content. Owner-local, 3b and 4b exclusions remain absent. The separately generated independent-root ledger has 51 expected blockers: 14 4b mirrors + 4 operator commands + 7 content dependencies + 26 composition-root constructions. `composition-root:require-empty` exits 1 by design; this proves scoped closure only and explicitly does not claim independent boot. -->
- [x] 1.3 Capture baseline Cloud focused/acceptance/full/typecheck/boundary results and API/automation/content hand-written-root failures separately; record package pins, source-sync and DEV monolith topology without claiming independent boot.
  <!-- 2026-07-26 baseline at Cloud 67941e495ad52c22b08e4a85ef530245b2fac517: acceptance 127/127, full 3479 total / 3468 pass / 0 fail / 11 skip, typecheck PASS, boundary census 485 source = 485 ownership and 0 cross-layer edges. Read-only split sync reports exact managed source, migration and pins for api 107, automation 208, content 83, kernel 91 and transport 37; its only expected nonzero residuals are handwritten api/automation/content roots. Pins: kernel 94fd279b356b447baee2e3eaddec222e343fb4c7 and transport f9a7276a07d4184295f92383080e7d1856f63262. Current extracted-repo whole-tree typechecks remain nonzero: api/automation fail across handwritten roots and legacy fixtures/imports; content also has pre-existing handwritten-root and quality DTO drift, so those results are recorded separately rather than weakening Cloud gates. DEV evidence remains the §10.9 monolith at Cloud 67941e4 with only 8787/8090 live; independent 8092/8093/8094 boot is not claimed. -->

## 2. Kernel contracts and wire discipline

- [x] 2.1 Add pure kernel contracts for AccountRoster, AccountOwnership and AccountRuntime authority, preserving ownership three-state results and replacing synchronous nickname get-then-set with owner-side idempotent `recordNickname`; do not expose unused `claimExecutionTarget`.
- [x] 2.2 Add the nineteen-method automation PublishLog port: the original ten methods plus `updateStatus`, `updatePostId`, `markScheduled`, `markImagesAttached`, `listDueScheduled`, `deferScheduledReconcile`, `confirmScheduledPublished`, `getMostRecentPublishTime`, and `recentPublishedContents`; retain content-version CAS, exclude owner-local `listPendingApprovalIds` / `pendingPublishPreviewForRecord`, and keep all 3b approval revision types/routes untouched.
- [x] 2.3 Add pure InteractionAuth, InteractionApiWrites and ReplyConfig resolver contracts; remove caller `Queryable`/PG types from purge methods and preserve real row counts plus audit inserted/duplicate.
- [x] 2.4 Add AccountPersona, EnvironmentHandshake, CommentApprovalPolicy, NotificationContacts, FirstPostProgress and AutomationConfigCommands contracts plus the three API-owned offboard ledger primitives `reconcileActiveOffboardSnapshot`, `claimPendingMaterializations`, and `recordMaterializationReceipt`.
- [x] 2.5 Add one exhaustive structured notification `deliver` command/result union; keep `resolveCardChatId`, `resolveAccountChatId`, and `bindBotChat` API-local, and prove the kernel layer imports no Feishu SDK, SQL, HTTP, or owner implementation.
- [x] 2.6 Define the 4a envelope, version/error unions, target validation inputs and read/write unknown-result distinctions; keep `AIDCP_API_INTERNAL_TOKEN`, `AIDCP_AUTOMATION_INTERNAL_TOKEN`, and `AIDCP_CONTENT_INTERNAL_TOKEN` separate from each other and from the existing 3b approval token.
- [x] 2.7 Add the one-method `EdgeResumeCommandPort` request/receipt/error contract with stable commandId, real resumed count, same-process owner dedupe and explicit result-unknown semantics; bind it to the exact `AIDCP_AUTOMATION_INTERNAL_TOKEN`.
- [x] 2.8 Add narrow `FacebookScopeCommandPort` contracts for `importTargets` and `replaceTargetScopes` with stable commandId, owner receipt/dedupe, target/version validation, and result-unknown semantics.
- [x] 2.9 Reuse or narrow the pure `PersonaGeneratorPort.generate` contract for API→content calls, retaining idempotency key/diversity seed and a generation-result-unknown outcome without exposing generic LLM authority.
- [x] 2.10 Add the one-method `PublishUiUpdateCommandPort.applyPublishUiUpdate` contract with stable commandId, accountId, typed preview/state update, owner receipt/dedupe/collision and explicit result-unknown semantics; bind it to `AIDCP_AUTOMATION_INTERNAL_TOKEN`.
  <!-- 2026-07-26: aidcp-cloud@b94f6ad and aidcp-kernel@bb132db deliver the pure 20-group/55-slot DTO/port surface. Kernel build, typecheck and the `API_DIRECT_PORT_INVENTORY` 20-group export probe pass; no SQL, HTTP, SDK or owner implementation entered the shared contract package. Publish UI state includes `factVersion` and the receipt distinguishes `stale`. -->

## 3. API owner adapters and internal server

- [x] 3.1 Implement API-local AccountRoster/Ownership/Runtime adapters over the existing account owner store, with target checks, three-state ownership, idempotent nickname observation and no automation pool.
- [x] 3.2 Implement the PublishLog and Edge publish command adapters over the API owner log/handlers; preserve all nineteen admitted publish methods, content-version conflicts, legitimate nulls and write-after-ack-loss unknown results, without exposing `listPendingApprovalIds` or `pendingPublishPreviewForRecord`.
- [x] 3.3 Implement InteractionAuth/ApiWrites/ReplyConfig adapters using only the API pool; make purge methods open owner transactions, keep audit `eventId` idempotency and return exact row/result truth.
- [x] 3.4 Implement persona authority, environment registration, approval policy, notification contacts, first-post progress, asynchronous config command, and the three offboard ledger adapters; trigger persona auto-fill only after owner-side environment registration succeeds.
- [x] 3.5 Implement the structured notification adapter using API-owned chat routing, account display truth, card builders and messenger; preserve approval-card failure, warn-and-continue notification and `delivery_result_unknown` distinctions.
- [x] 3.6 Implement versioned route/client modules for every admitted group, all using bearer authentication and local-target verification before owner handlers; missing token/URL/target must not fall back to a local API store or pool.
- [x] 3.7 Register 4a routes capability-by-capability on the API internal server so one optional owner capability cannot disable unrelated routes; add source guards proving no public panel/customer route exposes them.
- [x] 3.8 Wire the API command face to call `accountState.resume` first and the remote Edge resume client second; return active+failed/unknown partial truth without rollback or optimistic resumed count.
- [x] 3.9 Wire API panel Facebook mutations through the remote automation command client and `AccountPersona.generate` through the remote content generator client; never hold an API owner transaction across either call, never auto-retry unknown writes/generation, and never reuse the 3b token.
- [x] 3.10 Keep `pendingPublishPreviewForRecord` API owner-local; after relevant owner mutations, build the typed preview/state update locally and call remote `applyPublishUiUpdate`, preserving no-record, owner-read failure, applied/duplicate/collision and `publish_ui_update_result_unknown` without a reverse preview-read fallback.
  <!-- 2026-07-26: aidcp-cloud@b94f6ad implements the API owners, capability-local ingress and API-only Feishu composition. Review closed target/body mismatch, notification-union and capability-coupling defects. After an owner edit/reject commits, UI delivery remains an observable one-way outcome: it is not retried, does not roll back, and cannot rewrite the exact owner mutation result to unknown. Extracted aidcp-api SHA/validation is recorded under 6.3. -->

## 4. Automation clients and paired outbound owners

- [x] 4.1 Wire AccountRoster/Ownership/Runtime clients into the D1-admitted automation projection, risk ownership, connection runtime and account command consumer graph; remove direct `AccountStore`/API-pool access from that graph, while keeping every remaining root construction and 4b synchronous mirror explicit in the blocker ledger and making no independent-boot claim.
- [x] 4.2 Wire all nineteen PublishLog methods and Edge publish command clients into dispatcher, scheduled reconciler, scheduler, usage snapshot, delegated executor and Edge WS handlers; continue using 3b approval authority/decision/trigger without duplicate routes/state and keep pending scan on 3b `listPendingDispatch`.
- [x] 4.3 Wire InteractionAuth/ApiWrites/ReplyConfig clients; preserve auth fail-closed, outbox cursor stop/replay, audit event idempotency and real purge counts without passing an automation PG client across HTTP.
- [x] 4.4 Wire persona, environment, policy, notification-contact, first-post, and asynchronous config clients only at confirmed automation consumers; move offboard orchestration into automation so it reads local `activeWechatOffboards`, reconciles/claims via API, calls local `materializeEnvironmentOffboard`, and writes a CAS receipt without holding a transaction across the network.
- [x] 4.5 In the automation service-mode branch and hand-written automation root, wire only the structured notification `deliver` client for admitted notification consumers and do not construct or receive Feishu SDK/card builders, `BotChatStore`, API messenger, or chat resolver/bind authority; API/monolith owner segments may retain those local capabilities. Add an exhaustive kind guard and mode-local source tests.
- [x] 4.6 Extend the automation-owned `FacebookGroupOpsPort` with `importTargets` and `replaceTargetScopes`, register both as versioned/target-bound routes protected by `AIDCP_AUTOMATION_INTERNAL_TOKEN`, and use one refresh-before-reject helper backed by AccountRoster before the owner write transaction.
- [x] 4.7 Register the versioned/target-bound `AIDCP_AUTOMATION_INTERNAL_TOKEN` Edge resume route on the automation internal server and wire it only to the installed local `WsServer`; dedupe equivalent commandId receipts and never translate a missing server/timeout into resumed=0.
- [x] 4.8 Add composition-root source tests proving every D1-admitted automation consumer uses only the HTTP/client port with no API pool/store reachability and Edge resume remains a command; separately generate a blocker ledger that may remain nonempty and exhaustively enumerates unresolved 4b read/synchronous mirrors plus PersonaGenerator-external content-owner dependencies. Do not use a require-empty/full-root gate for 4a acceptance.
- [x] 4.9 Register the versioned/target-bound `AIDCP_CONTENT_INTERNAL_TOKEN` persona generation route on the content internal server, backed only by the content-owned `PersonaGenerator`; keep persistence and account validation in API.
- [x] 4.10 Register the versioned/target-bound `AIDCP_AUTOMATION_INTERNAL_TOKEN` Publish UI update route on the automation internal server; apply only to the local UI projection, dedupe equivalent commandId receipts, reject collisions, and never translate timeout/ack loss into “not applied”.
  <!-- 2026-07-26: aidcp-cloud@b94f6ad rewires the admitted consumers and adds the three paired command owners plus automation-owned offboard reconciliation. Automation mode has no Feishu SDK/cards/chat-owner constructors and only calls structured `deliver`. Publish UI compares `(accountId, recordId)` fact versions and returns `stale` without applying delayed older facts. PersonaGenerator now receives a content-owned codec rather than an API-owned Soul loader. The 51-item blocker ledger preserves every unresolved root/4b/content dependency; extracted-repo SHAs are recorded under 6.4/6.5. -->

## 5. Fact-source verification

- [x] 5.1 Add direct HTTP tests for all 20 D1 groups/55 slots in automation→API, API→automation, and API→content directions: success, legitimate empty/null, owner rejection, malformed response, wrong version, wrong target, wrong bearer, timeout/unavailable, and side-effect result unknown.
- [x] 5.2 Add focused AccountRoster/projection/Facebook tests for nonempty refresh freshness, empty/error no-advance, import/replace parity, transaction ordering, no partial target/scope write, wrong bearer/target, duplicate/colliding commandId, and post-write ack loss.
- [x] 5.3 Add publish/interaction/offboard tests for all nineteen publish methods, content-version CAS, 3b revision non-regression, owner-local preview read plus one-way Publish UI update ordering/dedupe/collision/ack loss, audit replay dedupe, owner-local purge transactions, complete-snapshot gating, atomic admission claim, local materialization, CAS/idempotent receipt, `binding_missing`, and no false terminal outcome or cross-network transaction.
- [x] 5.4 Add notification tests for every structured kind, unknown kind rejection, no-chat business error, API-side card construction, warn-and-continue call sites and post-send response loss.
- [x] 5.5 Add Edge resume route/client tests for version/target/Bearer, true resumed count, duplicate commandId receipt, collision rejection, no-server/timeout result unknown, no auto-retry, API account-state-first ordering and 3b restricted-recovery non-bypass.
- [x] 5.6 Add PersonaGenerator route/client tests for valid generation, wrong bearer/target/version, content-owner invocation, no API-local generator/LLM construction, post-generation ack loss, same-process receipt reuse, no auto-regeneration, and API-only persist.
- [x] 5.7 In `aidcp-cloud`, run focused suites, `npm run test:acceptance`, full `npm test`, `npm run typecheck`, boundary/ownership census, source-derived scoped census, independent-root blocker ledger and `git diff --check`; require zero new cross-owner reads/writes and zero API pool/store reachability in the D1-admitted automation slice. The blocker ledger is expected to remain nonempty in 4a; only new or undeclared drift fails this scoped gate.
  <!-- 2026-07-26 at aidcp-cloud@b94f6ad: focused 4a HTTP/receiver/composition tests 42/42 plus expand/offboard writer-ledger gates 59/59 and PersonaGenerator/content-codec tests 34/34; acceptance 144/144; full 3593 total / 3582 pass / 0 fail / 11 skip; typecheck PASS; boundary census 502 source = 502 ownership with 0 cross-layer edges; composition census 20/55/55 with 2 reviewed alternates and 51 expected blockers; `git diff --check` PASS. Independent final review found and closed the complete-snapshot/materialization interleaving: release now applies only to holds materialized at or before the observation watermark, and a post-observation hold remains blocked. Targeted ledger/reconciler/migration 20/20 pass; remaining Cloud P0/P1/P2=0. Independent extracted-root boot is still not claimed. -->
  <!-- Deviation: migration 0081 remains an expand migration, so `client_env_revocation_holds.execution_target` stays physically nullable during rolling deployment. It never guesses dev/ol: migration aborts on the first legacy NULL row with `offboard_admission_execution_target_backfill_required`; new writers require a valid server-injected target; reconcile/claim fail loud with `offboard_admission_execution_target_unassigned` before any side effect. Receipt/snapshot tables are target-NOT-NULL. Physical `SET NOT NULL` is deferred to a separately authorized D6 contract migration. -->

## 6. Shared packages and extracted repositories

- [x] 6.1 Publish only pure D1 contracts to `aidcp-kernel`; run build/typecheck, export probes and focused tests, then integrate/push the exact default-branch SHA.
  <!-- 2026-07-26: aidcp-kernel master fast-forwarded and pushed at bb132db84fe85cfdb90de5b116e596d7e6426657. Build/typecheck and the closed 20-group API_DIRECT_PORT_INVENTORY runtime export probe pass; the package contains only pure contracts/DTOs and no SQL, HTTP, SDK or owner implementation. -->
- [x] 6.2 Add only admitted 4a route/client modules, including Publish UI update, and the paired Facebook extension to `TRANSPORT_MEMBERS`; publish `aidcp-transport`, run build/typecheck/round-trip/export probes, and integrate/push the exact SHA.
  <!-- 2026-07-26: aidcp-transport master fast-forwarded and pushed at 874d5564276bbd1c929c2b0b0b6c5f0cbc9f3053, exactly pinning kernel bb132db. Six admitted transport modules passed 28/28 route/client round-trip tests, build/typecheck and the 20-group runtime export probe. -->
- [x] 6.3 Sync API owner adapters/server wiring plus automation/content clients into `aidcp-api`, including the owner-local publish preview producer and outbound UI command; update exact kernel/transport pins. Focused, strict 4a slice, package/export and typecheck gates MUST pass; run the whole-repo suite and record any exact blocker-ledger failures without treating them as scoped-gate success or hiding new 4a failures, then integrate/push.
  <!-- 2026-07-26: aidcp-api master fast-forwarded and pushed at 4a0f5604c6361bb2dd4c0513e43536d223c73f65, pinning kernel bb132db and transport 874d556. The dedicated API root owns one API pool, all 16 authority groups, API-only Feishu and 3b approval authority/writer/outbox; its Facebook panel writes use the paired target command and Edge publish uses real owner handlers. Independent review found and closed three missing authority/3b wiring defects plus a duplicate preview delivery; final P0/P1/P2=0. Focused root 5/5, API/offboard execution 14/14, acceptance 3/3, full 457/457, typecheck/build/package dry-run/diff-check pass. Managed source and 54 migrations match Cloud b94f6ad; runtime asset src/soul/soul.yaml is packaged and now explicitly covered by the control sync asset census. The two hand-written roots and seven repository-local tests remain intentional derived differences, not managed drift. -->
- [x] 6.4 Sync automation-owned client/root wiring and Publish UI update receiver into `aidcp-automation` while retaining its local `src/transport` source and avoiding a second transport package instance. Focused, strict 4a slice, package/export and typecheck gates MUST pass; run the whole-repo suite and record any exact blocker-ledger failures without treating them as scoped-gate success or hiding new 4a failures, then integrate/push.
  <!-- 2026-07-26: aidcp-automation master fast-forwarded and pushed at f21bd74f446de0b7c6b2d4d340b3e71fea81d6c5, pinning kernel bb132db and retaining local transport without a second aidcp-transport package. Review rejected the first Cloud-shaped root because static API/content imports made it unloadable; the final derived root loads and compiles, constructs 16 API client groups plus three automation command receivers, owner-local offboard reconciliation and structured deliver-only, while its executable fails closed on a 20-item derived readiness ledger instead of claiming production boot. Final focused entry/inventory/generation/boundary 19/19, typecheck/build/dist export/package dry-run and diff-check pass; boundary census is 219 source = 219 ownership, unresolved 0, forbidden 0, with mutation and subprocess tests proving forbidden edges exit nonzero. Whole suite remains honestly nonzero: 1725 total / 1699 pass / 23 fail / 3 skip; all 23 are named pre-existing derived schema/migration/frozen-fixture blockers and no new root failure remains. Independent final review: P0/P1/P2=0. -->
- [x] 6.5 Sync the content-owned PersonaGenerator route/server wiring into `aidcp-content`, update exact kernel/transport pins. Focused 4a slice, package/export, typecheck and security gates MUST pass; run the whole-repo suite, record exact PersonaGenerator-external blocker-ledger failures, and do not hide new 4a failures before integrating/pushing its exact SHA.
  <!-- 2026-07-26: aidcp-content master fast-forwarded and pushed at e9744fe5e04cb20a0b91ec86de2944bc7613f632, pinning kernel bb132db and transport 874d556 with one deduped kernel graph. Content owns its PersonaGenerator codec/receiver and internal target/version/Bearer route; curated-store absence disables only curated routes. Focused persona 9/9, filtered acceptance 12/12, full 455/455, typecheck/build/package dry-run and diff-check pass. Independent review found P0/P1/P2=0. The repo's pre-existing boundaries:census command still names a missing helper; the equivalent 85/85 ownership census found no drift and this repository tooling defect is not reported as a green script result. -->
- [x] 6.6 Run final `scripts/sync-split-repos` source/pin/migration census from the landed Cloud SHA; require zero managed drift and record expected hand-written-root differences without auto-overwriting them.
  <!-- 2026-07-26: final check from landed aidcp-cloud@b94f6ad reports zero managed source drift: api 111/111, automation 218/218, content 85/85, kernel 92/92 and transport 43/43; pins are kernel bb132db / transport 874d556 and migrations are api 54 / automation 44 / content 20. Expected nonzero output is limited to hand-written roots (api 2, automation 2 plus its declared derived composition helper, content 2) and repository-local test adaptations (api 7 extra, automation 7 adapted + 1 extra, content 3 extra). The sync helper now treats API src/soul/soul.yaml as a required byte-for-byte runtime asset and the automation-only composition helper as a declared non-Cloud root; neither can be silently pruned. -->

## 7. DEV monolith and closeout

- [x] 7.1 Rebase/integrate/push Cloud only after all source/package gates pass; read deployment guidance, run `scripts/deploy-target dev --check`, back up and deploy the clean eligible default checkout, and restart only the documented DEV Cloud unit.
  <!-- 2026-07-27: after kernel bb132db, transport 874d556, API 4a0f560, automation f21bd74 and content e9744fe landed, aidcp-cloud master fast-forwarded/pushed at b94f6ad7d8ac935562e88feafefeeafd5c4e2941. Preflight and deploy-target dev check passed; clean committed files only were synced to DEV after backups /opt/aidcp/cloud.bak.20260727-030000.tar.gz and /opt/aidcp/cloud/.env.bak.20260727-030000. Package metadata changed, so full ECS npm ci completed. Pre-migration API owner holds were 0; status showed only reviewed expand 0081 pending, which applied in 18ms. All three owner statuses then had pending=0 and verify missing objects=0. Only aidcp-cloud.service was stopped then started. -->
- [x] 7.2 Verify DEV monolith deploy SHA, service/restart count, 8787/8090 health, PostgreSQL/owner schema gates, Feishu state, writer lock and logs; confirm independent api/automation/content listeners remain closed and do not call this three-process verification.
  <!-- 2026-07-27 DEV monolith at Cloud b94f6ad: service active/running, NRestarts=0; 8787 plus loopback 8090/8091 listen and both health endpoints return {"ok":true}; all three owner DB SELECT 1 probes pass; enforce schema gates pass at content 0069, automation 0080 and API 0081; target=dev writer lock has exactly one holder; RiskControllerRegistry ready and Feishu WSClient onReady; no post-start error/fatal/failed log. Process env has AIDCP_DEPLOY_ENV=dev and no AIDCP_SERVICE. Independent api/automation/content units are not-found/inactive and 8092/8093/8094 are closed, so this is explicitly monolith evidence only. -->
- [x] 7.3 Update §10 with the final scoped 20-group/55-slot post-3b inventory, the source-derived census and separate independent-root blocker ledger, delivered repo SHAs/tests/DEV evidence, Facebook/Edge/Publish UI command receipts, automation-owned offboard orchestration, deliver-only notification exit, owner-local `listPendingApprovalIds` / `pendingPublishPreviewForRecord`, content-owned persona generation, removed stale “11” count, and explicit remaining 4b/content/independent-runtime boundary; do not call scoped API authority closure full-root boot.
  <!-- 2026-07-27: docs/cloud-composition-root-trisection.md §10.4 stale 4a “11” was replaced by 20/55, and new §10.10 records the directional inventory, generated 51-item Cloud ledger versus 20-item derived automation readiness ledger, receipts/factVersion/offboard watermark/Feishu/persona mechanisms, exact repo and validation SHAs, final sync residuals, DEV monolith evidence, and the explicit not-started independent-runtime/installer/live-account boundary. -->
- [ ] 7.4 Run `openspec validate split-cloud-api-composition-root-4a --strict` and `git diff --check`; update each completed task with repo SHA, validation/deployment/deviation evidence, integrate/push control `main`, and leave archive for a separately confirmed closeout.
