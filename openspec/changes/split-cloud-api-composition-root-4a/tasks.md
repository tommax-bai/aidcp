## 1. Admission, scoped census, and root blocker ledger

- [x] 1.1 Run `scripts/task-preflight`; create isolated control, Cloud, kernel, transport, API, automation, and content worktrees/branches for this change, preserving canonical defaults and unrelated files.
  <!-- 2026-07-26: preflight PASS. Created codex/split-cloud-api-composition-root-4a worktrees for control, aidcp-cloud, aidcp-kernel, aidcp-transport, aidcp-api, aidcp-automation and aidcp-content from the exact 3b default SHAs. Every code worktree has a physical non-symlink node_modules directory. Canonical repos remain on main/master and clean; preserved control-only user files output/pdf/aidcp_progress_report_2026-07-16_2026-07-26.pdf and tmp/pdfs/. -->
- [ ] 1.2 Re-run two independent gates against the implementation SHA and `boundaries/module-ownership.json`: (a) a source-derived consumer census proving the scoped 20-group/55-slot surface—16/50 automation→API, 3/4 API→automation, 1/1 API→content—and excluding owner-local `listPendingApprovalIds` / `pendingPublishPreviewForRecord`, `claimExecutionTarget`, chat resolve/bind and 3b approval; (b) an independent-root blocker ledger enumerating every remaining 4b mirror and PersonaGenerator-external content-owner dependency. Neither gate may infer full-root boot from scoped closure.
  <!-- 2026-07-26 EVIDENCE WITHDRAWN / TASK REOPENED: the earlier 19-group/45-slot census (16/41, 2/3, 1/1) counted server-direct calls but missed nine publish-log methods consumed transitively by PublishDispatcher/ScheduledPublishReconciler/scheduler, and omitted the API→automation applyPublishUiUpdate command. Its 3/3 inventory test is not accepted evidence for this task. Replace it with a source-derived 20/55 census plus a separately source-derived independent-root blocker ledger; preserve the old artifact only as superseded history until the replacement evidence lands. -->
- [x] 1.3 Capture baseline Cloud focused/acceptance/full/typecheck/boundary results and API/automation/content hand-written-root failures separately; record package pins, source-sync and DEV monolith topology without claiming independent boot.
  <!-- 2026-07-26 baseline at Cloud 67941e495ad52c22b08e4a85ef530245b2fac517: acceptance 127/127, full 3479 total / 3468 pass / 0 fail / 11 skip, typecheck PASS, boundary census 485 source = 485 ownership and 0 cross-layer edges. Read-only split sync reports exact managed source, migration and pins for api 107, automation 208, content 83, kernel 91 and transport 37; its only expected nonzero residuals are handwritten api/automation/content roots. Pins: kernel 94fd279b356b447baee2e3eaddec222e343fb4c7 and transport f9a7276a07d4184295f92383080e7d1856f63262. Current extracted-repo whole-tree typechecks remain nonzero: api/automation fail across handwritten roots and legacy fixtures/imports; content also has pre-existing handwritten-root and quality DTO drift, so those results are recorded separately rather than weakening Cloud gates. DEV evidence remains the §10.9 monolith at Cloud 67941e4 with only 8787/8090 live; independent 8092/8093/8094 boot is not claimed. -->

## 2. Kernel contracts and wire discipline

- [ ] 2.1 Add pure kernel contracts for AccountRoster, AccountOwnership and AccountRuntime authority, preserving ownership three-state results and replacing synchronous nickname get-then-set with owner-side idempotent `recordNickname`; do not expose unused `claimExecutionTarget`.
- [ ] 2.2 Add the nineteen-method automation PublishLog port: the original ten methods plus `updateStatus`, `updatePostId`, `markScheduled`, `markImagesAttached`, `listDueScheduled`, `deferScheduledReconcile`, `confirmScheduledPublished`, `getMostRecentPublishTime`, and `recentPublishedContents`; retain content-version CAS, exclude owner-local `listPendingApprovalIds` / `pendingPublishPreviewForRecord`, and keep all 3b approval revision types/routes untouched.
- [ ] 2.3 Add pure InteractionAuth, InteractionApiWrites and ReplyConfig resolver contracts; remove caller `Queryable`/PG types from purge methods and preserve real row counts plus audit inserted/duplicate.
- [ ] 2.4 Add AccountPersona, EnvironmentHandshake, CommentApprovalPolicy, NotificationContacts, FirstPostProgress and AutomationConfigCommands contracts plus the three API-owned offboard ledger primitives `reconcileActiveOffboardSnapshot`, `claimPendingMaterializations`, and `recordMaterializationReceipt`.
- [ ] 2.5 Add one exhaustive structured notification `deliver` command/result union; keep `resolveCardChatId`, `resolveAccountChatId`, and `bindBotChat` API-local, and prove the kernel layer imports no Feishu SDK, SQL, HTTP, or owner implementation.
- [ ] 2.6 Define the 4a envelope, version/error unions, target validation inputs and read/write unknown-result distinctions; keep `AIDCP_API_INTERNAL_TOKEN`, `AIDCP_AUTOMATION_INTERNAL_TOKEN`, and `AIDCP_CONTENT_INTERNAL_TOKEN` separate from each other and from the existing 3b approval token.
- [ ] 2.7 Add the one-method `EdgeResumeCommandPort` request/receipt/error contract with stable commandId, real resumed count, same-process owner dedupe and explicit result-unknown semantics; bind it to the exact `AIDCP_AUTOMATION_INTERNAL_TOKEN`.
- [ ] 2.8 Add narrow `FacebookScopeCommandPort` contracts for `importTargets` and `replaceTargetScopes` with stable commandId, owner receipt/dedupe, target/version validation, and result-unknown semantics.
- [ ] 2.9 Reuse or narrow the pure `PersonaGeneratorPort.generate` contract for API→content calls, retaining idempotency key/diversity seed and a generation-result-unknown outcome without exposing generic LLM authority.
- [ ] 2.10 Add the one-method `PublishUiUpdateCommandPort.applyPublishUiUpdate` contract with stable commandId, accountId, typed preview/state update, owner receipt/dedupe/collision and explicit result-unknown semantics; bind it to `AIDCP_AUTOMATION_INTERNAL_TOKEN`.

## 3. API owner adapters and internal server

- [ ] 3.1 Implement API-local AccountRoster/Ownership/Runtime adapters over the existing account owner store, with target checks, three-state ownership, idempotent nickname observation and no automation pool.
- [ ] 3.2 Implement the PublishLog and Edge publish command adapters over the API owner log/handlers; preserve all nineteen admitted publish methods, content-version conflicts, legitimate nulls and write-after-ack-loss unknown results, without exposing `listPendingApprovalIds` or `pendingPublishPreviewForRecord`.
- [ ] 3.3 Implement InteractionAuth/ApiWrites/ReplyConfig adapters using only the API pool; make purge methods open owner transactions, keep audit `eventId` idempotency and return exact row/result truth.
- [ ] 3.4 Implement persona authority, environment registration, approval policy, notification contacts, first-post progress, asynchronous config command, and the three offboard ledger adapters; trigger persona auto-fill only after owner-side environment registration succeeds.
- [ ] 3.5 Implement the structured notification adapter using API-owned chat routing, account display truth, card builders and messenger; preserve approval-card failure, warn-and-continue notification and `delivery_result_unknown` distinctions.
- [ ] 3.6 Implement versioned route/client modules for every admitted group, all using bearer authentication and local-target verification before owner handlers; missing token/URL/target must not fall back to a local API store or pool.
- [ ] 3.7 Register 4a routes capability-by-capability on the API internal server so one optional owner capability cannot disable unrelated routes; add source guards proving no public panel/customer route exposes them.
- [ ] 3.8 Wire the API command face to call `accountState.resume` first and the remote Edge resume client second; return active+failed/unknown partial truth without rollback or optimistic resumed count.
- [ ] 3.9 Wire API panel Facebook mutations through the remote automation command client and `AccountPersona.generate` through the remote content generator client; never hold an API owner transaction across either call, never auto-retry unknown writes/generation, and never reuse the 3b token.
- [ ] 3.10 Keep `pendingPublishPreviewForRecord` API owner-local; after relevant owner mutations, build the typed preview/state update locally and call remote `applyPublishUiUpdate`, preserving no-record, owner-read failure, applied/duplicate/collision and `publish_ui_update_result_unknown` without a reverse preview-read fallback.

## 4. Automation clients and paired outbound owners

- [ ] 4.1 Wire AccountRoster/Ownership/Runtime clients into the D1-admitted automation projection, risk ownership, connection runtime and account command consumer graph; remove direct `AccountStore`/API-pool access from that graph, while keeping every remaining root construction and 4b synchronous mirror explicit in the blocker ledger and making no independent-boot claim.
- [ ] 4.2 Wire all nineteen PublishLog methods and Edge publish command clients into dispatcher, scheduled reconciler, scheduler, usage snapshot, delegated executor and Edge WS handlers; continue using 3b approval authority/decision/trigger without duplicate routes/state and keep pending scan on 3b `listPendingDispatch`.
- [ ] 4.3 Wire InteractionAuth/ApiWrites/ReplyConfig clients; preserve auth fail-closed, outbox cursor stop/replay, audit event idempotency and real purge counts without passing an automation PG client across HTTP.
- [ ] 4.4 Wire persona, environment, policy, notification-contact, first-post, and asynchronous config clients only at confirmed automation consumers; move offboard orchestration into automation so it reads local `activeWechatOffboards`, reconciles/claims via API, calls local `materializeEnvironmentOffboard`, and writes a CAS receipt without holding a transaction across the network.
- [ ] 4.5 In the automation service-mode branch and hand-written automation root, wire only the structured notification `deliver` client for admitted notification consumers and do not construct or receive Feishu SDK/card builders, `BotChatStore`, API messenger, or chat resolver/bind authority; API/monolith owner segments may retain those local capabilities. Add an exhaustive kind guard and mode-local source tests.
- [ ] 4.6 Extend the automation-owned `FacebookGroupOpsPort` with `importTargets` and `replaceTargetScopes`, register both as versioned/target-bound routes protected by `AIDCP_AUTOMATION_INTERNAL_TOKEN`, and use one refresh-before-reject helper backed by AccountRoster before the owner write transaction.
- [ ] 4.7 Register the versioned/target-bound `AIDCP_AUTOMATION_INTERNAL_TOKEN` Edge resume route on the automation internal server and wire it only to the installed local `WsServer`; dedupe equivalent commandId receipts and never translate a missing server/timeout into resumed=0.
- [ ] 4.8 Add composition-root source tests proving every D1-admitted automation consumer uses only the HTTP/client port with no API pool/store reachability and Edge resume remains a command; separately generate a blocker ledger that may remain nonempty and exhaustively enumerates unresolved 4b read/synchronous mirrors plus PersonaGenerator-external content-owner dependencies. Do not use a require-empty/full-root gate for 4a acceptance.
- [ ] 4.9 Register the versioned/target-bound `AIDCP_CONTENT_INTERNAL_TOKEN` persona generation route on the content internal server, backed only by the content-owned `PersonaGenerator`; keep persistence and account validation in API.
- [ ] 4.10 Register the versioned/target-bound `AIDCP_AUTOMATION_INTERNAL_TOKEN` Publish UI update route on the automation internal server; apply only to the local UI projection, dedupe equivalent commandId receipts, reject collisions, and never translate timeout/ack loss into “not applied”.

## 5. Fact-source verification

- [ ] 5.1 Add direct HTTP tests for all 20 D1 groups/55 slots in automation→API, API→automation, and API→content directions: success, legitimate empty/null, owner rejection, malformed response, wrong version, wrong target, wrong bearer, timeout/unavailable, and side-effect result unknown.
- [ ] 5.2 Add focused AccountRoster/projection/Facebook tests for nonempty refresh freshness, empty/error no-advance, import/replace parity, transaction ordering, no partial target/scope write, wrong bearer/target, duplicate/colliding commandId, and post-write ack loss.
- [ ] 5.3 Add publish/interaction/offboard tests for all nineteen publish methods, content-version CAS, 3b revision non-regression, owner-local preview read plus one-way Publish UI update ordering/dedupe/collision/ack loss, audit replay dedupe, owner-local purge transactions, complete-snapshot gating, atomic admission claim, local materialization, CAS/idempotent receipt, `binding_missing`, and no false terminal outcome or cross-network transaction.
- [ ] 5.4 Add notification tests for every structured kind, unknown kind rejection, no-chat business error, API-side card construction, warn-and-continue call sites and post-send response loss.
- [ ] 5.5 Add Edge resume route/client tests for version/target/Bearer, true resumed count, duplicate commandId receipt, collision rejection, no-server/timeout result unknown, no auto-retry, API account-state-first ordering and 3b restricted-recovery non-bypass.
- [ ] 5.6 Add PersonaGenerator route/client tests for valid generation, wrong bearer/target/version, content-owner invocation, no API-local generator/LLM construction, post-generation ack loss, same-process receipt reuse, no auto-regeneration, and API-only persist.
- [ ] 5.7 In `aidcp-cloud`, run focused suites, `npm run test:acceptance`, full `npm test`, `npm run typecheck`, boundary/ownership census, source-derived scoped census, independent-root blocker ledger and `git diff --check`; require zero new cross-owner reads/writes and zero API pool/store reachability in the D1-admitted automation slice. The blocker ledger is expected to remain nonempty in 4a; only new or undeclared drift fails this scoped gate.

## 6. Shared packages and extracted repositories

- [ ] 6.1 Publish only pure D1 contracts to `aidcp-kernel`; run build/typecheck, export probes and focused tests, then integrate/push the exact default-branch SHA.
- [ ] 6.2 Add only admitted 4a route/client modules, including Publish UI update, and the paired Facebook extension to `TRANSPORT_MEMBERS`; publish `aidcp-transport`, run build/typecheck/round-trip/export probes, and integrate/push the exact SHA.
- [ ] 6.3 Sync API owner adapters/server wiring plus automation/content clients into `aidcp-api`, including the owner-local publish preview producer and outbound UI command; update exact kernel/transport pins. Focused, strict 4a slice, package/export and typecheck gates MUST pass; run the whole-repo suite and record any exact blocker-ledger failures without treating them as scoped-gate success or hiding new 4a failures, then integrate/push.
- [ ] 6.4 Sync automation-owned client/root wiring and Publish UI update receiver into `aidcp-automation` while retaining its local `src/transport` source and avoiding a second transport package instance. Focused, strict 4a slice, package/export and typecheck gates MUST pass; run the whole-repo suite and record any exact blocker-ledger failures without treating them as scoped-gate success or hiding new 4a failures, then integrate/push.
- [ ] 6.5 Sync the content-owned PersonaGenerator route/server wiring into `aidcp-content`, update exact kernel/transport pins. Focused 4a slice, package/export, typecheck and security gates MUST pass; run the whole-repo suite, record exact PersonaGenerator-external blocker-ledger failures, and do not hide new 4a failures before integrating/pushing its exact SHA.
- [ ] 6.6 Run final `scripts/sync-split-repos` source/pin/migration census from the landed Cloud SHA; require zero managed drift and record expected hand-written-root differences without auto-overwriting them.

## 7. DEV monolith and closeout

- [ ] 7.1 Rebase/integrate/push Cloud only after all source/package gates pass; read deployment guidance, run `scripts/deploy-target dev --check`, back up and deploy the clean eligible default checkout, and restart only the documented DEV Cloud unit.
- [ ] 7.2 Verify DEV monolith deploy SHA, service/restart count, 8787/8090 health, PostgreSQL/owner schema gates, Feishu state, writer lock and logs; confirm independent api/automation/content listeners remain closed and do not call this three-process verification.
- [ ] 7.3 Update §10 with the final scoped 20-group/55-slot post-3b inventory, the source-derived census and separate independent-root blocker ledger, delivered repo SHAs/tests/DEV evidence, Facebook/Edge/Publish UI command receipts, automation-owned offboard orchestration, deliver-only notification exit, owner-local `listPendingApprovalIds` / `pendingPublishPreviewForRecord`, content-owned persona generation, removed stale “11” count, and explicit remaining 4b/content/independent-runtime boundary; do not call scoped API authority closure full-root boot.
- [ ] 7.4 Run `openspec validate split-cloud-api-composition-root-4a --strict` and `git diff --check`; update each completed task with repo SHA, validation/deployment/deviation evidence, integrate/push control `main`, and leave archive for a separately confirmed closeout.
