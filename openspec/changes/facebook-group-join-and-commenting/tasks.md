## 1. Preconditions

- [x] 1.1 Ground on `origin/main`: `facebook-scheduled-comment`, `generalize-contact-info` (contact-info verbatim injection), `feed-hot-lead-auto-group-comment` (human-reviewed lead-gen lane), and `facebook-browser-environment` are archived + deployed dev. Do NOT re-implement them. <!-- aidcp control verified after fast-forward to origin/main 320c59a; implementation reuses existing FB scheduled/contact/hot-lead paths, no reimplementation -->
- [x] 1.2 Confirm per-profile proxy/egress is an OPERATIONAL precondition configured manually in the AdsPower environment config, not built here. <!-- design/proposal boundary preserved; no proxy code added in aidcp-cloud/edge/console -->
- [x] 1.3 Open `aidcp-console` same-name worktree only when the group-target management page (Phase 0) is implemented. <!-- aidcp-console.wt/facebook-group-join-and-commenting opened for Phase 0 group management page -->

## 2. Cloud — data model + assignment (Phase 0)

- [x] 2.1 Add `FacebookGroupTargetStore` (`facebook_group_target`: `group_url` PK, `group_name`, `join_gating IN unknown|instant|gated`, `priority`, `enabled`, `import_batch`; self-create + migration). Bulk import dedups on canonical URL; per-group enable/kill. <!-- aidcp-cloud c870bce9dcda1862424257188a691491e258d634: src/comment-agent/facebook-group-store.ts + panel injection; canonical URL dedupe covered by test/comment-agent/facebook-group-store.test.ts -->
- [x] 2.2 Add `FacebookGroupMembershipStore` (`facebook_group_membership`: junction + coverage columns; `UNIQUE(group_url)` one-group-one-account lock; indexes on `(account_id,status)`, `(status)`, `(account_id,status,last_commented_at)`). <!-- aidcp-cloud c870bce9dcda1862424257188a691491e258d634: membership schema/store with coverage and TTL indexes -->
- [x] 2.3 Add `facebook_group_join_audit` append-only store (clone the comment audit store; best-effort never-throws). <!-- aidcp-cloud c870bce9dcda1862424257188a691491e258d634: FacebookGroupJoinAuditStore append() swallows write errors -->
- [x] 2.4 Lazy-claim: atomic `INSERT … SELECT … ON CONFLICT (group_url) DO NOTHING RETURNING` claiming the next `unknown|instant`, `enabled` target with no membership row. Orphan reclaimer (MVP): release `assigned`/`joining` rows idle past a TTL back to the pool. <!-- aidcp-cloud c870bce9dcda1862424257188a691491e258d634: claimNext() + reclaimStaleAssignments() -->
- [x] 2.5 Panel API: import / list-with-status / per-account progress / per-group enable / assignment view. Register FB-account routes before the wildcard `GET /api/accounts/:id`. <!-- aidcp-cloud c870bce9dcda1862424257188a691491e258d634: /api/facebook/groups* routes plus /api/accounts/:id/facebook-group-progress before wildcard -->

## 3. Cloud — join rate-limit action (Phase 0, hotspot·serialize)

- [x] 3.1 Add `join_group` to `RISK_ACTIONS` + all three tiers of daily quotas + minute/hour burst caps (typecheck-enforced coupled edit). <!-- aidcp-cloud c870bce9dcda1862424257188a691491e258d634 + aidcp-console 053fa950dd7860273e7083ef9f15431b64234e8b: src/risk/types.ts, quotas.ts, cold-start planner, console enum mirror -->
- [x] 3.2 `risk_counters` CHECK constraint + idempotent DROP/ADD migration (mirror the `comment_like` retrofit) so `join_group` is countable. <!-- aidcp-cloud c870bce9dcda1862424257188a691491e258d634: pg-risk-store CHECK + migration updated; risk-pg-store tests pass -->
- [x] 3.3 Add `join_group` to the `interaction.occurred` action union so verified joins auto-record; join counts only after a judgment-confirmed join. <!-- aidcp-cloud c870bce9dcda1862424257188a691491e258d634: event-bus/protocol/handler unions updated; join_group counts only on verified clicked join receipt -->
- [x] 3.4 Ensure join and comment for one account run on the SAME scheduler instance's SAME action array (shared per-account single-flight); write out and check the worst-case aggregate daily action count. <!-- aidcp-cloud c870bce9dcda1862424257188a691491e258d634: ContentScheduler action array now includes join before comment with shared account-level inFlight; test/content-scheduler.test.ts covers join blocking comment; code comment records normal worst-case join 3/day + comment cap 8/day = 11/day -->
- [ ] 3.5 (Optional) Add `joins` to the per-session budget only if joins batch within one browser session (open decision D-单场).

## 4. Cloud — join-gate judgment role (Phase 1)

- [x] 4.1 Declare the join-gate judgment role name in the role-name enum (hotspot·serialize) and register it in the role catalog (`browse_judge`, `llmKind:'text'`) for model config. <!-- aidcp-cloud c870bce9dcda1862424257188a691491e258d634: RoleName + ROLE_CATALOG browse:facebook_group_join_judge -->
- [x] 4.2 Implement it as a command-式 role (plain class + imperative `evaluate()`, constructed by the join scheduler; NOT dispatcher-registered), template shape = the follow-decision role. Pre-click verdict {instant_join|gated_skip|already_member|ambiguous_skip}; post-click verdict {joined|pending/gated|failed}; deterministic short-circuits; fail-closed on uncertainty. <!-- aidcp-cloud c870bce9dcda1862424257188a691491e258d634: FacebookGroupJoinJudge is a plain class consumed by FacebookGroupJoinScheduler; deterministic gates and LLM fail-closed behavior covered by test/agents/facebook-group-join-judge.test.ts -->
- [x] 4.3 Record input observation + verdict + reason to audit for the console "why joined/skipped" readout and the shadow accuracy measurement. <!-- aidcp-cloud c870bce9dcda1862424257188a691491e258d634: FacebookGroupJoinJudge audit rows include observation/verdict/reason; scheduler also records real pre/post outcomes for joined/skipped measurement -->

## 5. Cloud — join scheduler + coverage scheduler (Phase 1–3)

- [x] 5.1 Add a `join` action slot to the existing content scheduler (per-account offset + weekly mask + single-flight + risk gate): one atomic join per slot; drives lazy-claim -> observe -> judge -> `group.join` -> record honest outcome to the ledger + audit. Kill switch `AIDCP_FB_GROUP_JOIN_AUTO` (default off); shadow `AIDCP_FB_GROUP_JOIN_SHADOW`. <!-- aidcp-cloud c870bce9dcda1862424257188a691491e258d634: FacebookGroupJoinScheduler is wired into ContentScheduler with default-off auto, shadow mode, lazy claim, observe/judge/group.join, ledger, and audit -->
- [x] 5.2 State machine writes: `joined` (write `joined_at`, judgment-confirmed only) / `gated`/`pending` (flip target `join_gating='gated'`, fleet-wide exclude, no dangling request) / account-transient checkpoint/login -> pause account join loop / retryable failures with attempt cap + backoff. <!-- aidcp-cloud c870bce9dcda1862424257188a691491e258d634: join scheduler writes joined/gated/pending, pauses AccountState on login/captcha, and uses markRetryableFailure with cap/backoff for transient edge failures -->
- [x] 5.3 Coverage loop: per-account daily slice over `status='joined'` groups, oldest-`last_commented_at`-first past a per-group cooldown floor + small random window; join-to-first-comment warmup interval; writes back `last_commented_at`/`comments_total` on verified comment only. <!-- aidcp-cloud c870bce9dcda1862424257188a691491e258d634: membership coverageCandidates() enforces joined-only, cooldown, warmup, oldest-first window; CommentScheduler marks coverage commented only after verified FB comment -->
- [x] 5.4 Per-account comment-source gate (allowlist): coverage-enabled accounts source containers from the joined-group ledger; keywords still from config; fail-closed (no keywords or no joined groups -> no-op). NOT a global boolean. <!-- aidcp-cloud c870bce9dcda1862424257188a691491e258d634: AIDCP_FB_GROUP_COVERAGE_ACCOUNTS is a per-account allowlist; coverage source keeps keywords from account FB config and no joined groups yields no_targets without fallback -->
- [x] 5.5 Reverse-drift protection: `joined->left` demotion requires N repeated confirmations, never a single whole-page signal; deleted/inaccessible group (nav_error on joined) is demotable. <!-- aidcp-cloud c870bce9dcda1862424257188a691491e258d634: recordCoverageLeftSignal increments left_confirmations before left demotion; nav_error can demote as permanently inaccessible -->

## 6. Cloud — contact-comment lane (Phase 3)

- [x] 6.1 Route mode-(b) contact comments through the existing human-reviewed lane (`gated-auto-comment` + `compose-approve` verbatim contact injection + Feishu approval), extended with a Facebook submit closure. Fail-closed if `accounts.contact_info` missing. <!-- aidcp-cloud c870bce9dcda1862424257188a691491e258d634 + aidcp-edge 47cf5597daabaaa44752cde1372cab132c823ff1: FB triggerManual({injectContact}) now requires contact_info, requests approval, then submits text plus groupChatCode; edge inserts contact info as one Input.insertText segment -->
- [x] 6.2 Add a Facebook validator carve-out that exempts ONLY the injected contact span (mirror how xhs excludes contact from overlap/length gates); the composed body still passes all hard validators. Do NOT weaken the unattended-path contact-forbidden invariant. <!-- aidcp-cloud c870bce9dcda1862424257188a691491e258d634: validateFacebookComment still rejects contact in unattended draft; contact lane validates body first, then appends approved groupChatCode outside the body validator -->

## 7. Edge — join action + protocol (Phase 1)

- [x] 7.1 Add `FacebookJoinExecutor.joinGroup(url)` (CDP IIFE, jsdom-stub testable): URL guard -> navigate+settle -> fresh overlay probe (login/captcha fail-closed) -> scoped group-header observation report -> click Join once only when instructed -> post-click observation report -> dismiss optional survey / do NOT submit required questionnaire. Honest outcomes; `ok=true` only on member-now. <!-- aidcp-edge 47cf5597daabaaa44752cde1372cab132c823ff1: src/facebook/join-executor.ts implements observe/click/post-observe with questionnaire fail-closed behavior; test/facebook/join-executor.test.ts covers observe-only, click-joined, questionnaire, and captcha paths -->
- [x] 7.2 FB driver gains a distinct `join` capability (never `browse`); route `group.join` through the Facebook command handler (`switch(env.type)`), never a second `onBrowseCommand` call. <!-- aidcp-edge 47cf5597daabaaa44752cde1372cab132c823ff1: Facebook driver advertises join, main registers the Facebook command handler for comment/join, and comment-handler routes group.join without browse capability -->
- [x] 7.3 Protocol four-point sync for `group.join` (two `protocol.ts` verbatim + `docs/protocol.md` count/table) + edge onMessage active-command whitelist entry (typecheck-invisible silent-drop point); regression assert the command reaches the handler. <!-- aidcp-cloud c870bce9dcda1862424257188a691491e258d634 + aidcp-edge 47cf5597daabaaa44752cde1372cab132c823ff1 + control a4d51ea2e92b5a3155d407442e26c1a50ea92d8e: protocol.ts sync, docs/protocol.md count 66/table/payload, edge active whitelist, AC-PROTO and edge-client tests pass -->
- [x] 7.4 Checkpoint/login receipts wire to pausing the account join loop directly (not the un-wired risk state machine). <!-- aidcp-cloud c870bce9dcda1862424257188a691491e258d634: FacebookGroupJoinScheduler.pauseAccount uses AccountStateManager.pause on login_required/blocked_by_captcha and leaves group retryable, covered by scheduler test -->

## 8. Console — group management (Phase 0)

- [x] 8.1 Add a "群组" management page: bulk import (paste/dedup), status list (unassigned/assigned/joined/gated-skip/failed), per-account progress, per-group enable, shadow badge. Keep status `Record<Union>` exhaustive with a gray fallback (enum-drift white-screen guard). <!-- aidcp-console 053fa950dd7860273e7083ef9f15431b64234e8b: src/pages/FacebookGroupsPage.tsx + /facebook-groups route; shadow badge currently shows honest "Shadow 未接入" until scheduler state exists -->
- [x] 8.2 "安全" quota page gains the `join_group` action column (mirror enum + label; typecheck won't catch drift). <!-- aidcp-console 053fa950dd7860273e7083ef9f15431b64234e8b: RiskAction/QuotaAction mirror + QuotasPage action order; enum tests pass -->

## 9. Rollout + verification

- [ ] 9.1 Phase 0 on dev: import 2000–5000 targets, verify dedup + atomic lazy-claim + join rate-limit config surfaces in "安全" page. No join/comment.
- [ ] 9.2 Phase 1 shadow: run the judgment role over hundreds of real targets; measure join-gate classification accuracy against a numeric threshold (with denominator) — this gates Phase 2.
- [ ] 9.3 Phase 2 single disposable account, `join_group` cap 1–3/day: verify honest-receipt → ledger closed loop, no fake ok, no dangling pending, learned gating excludes fleet.
- [ ] 9.4 Phase 3 single account: auto contextual coverage (per-account gate), then the human-reviewed contact lane on selected groups.
- [ ] 9.5 Phase 4 fleet: raise caps gradually; observe partition balance, shared-budget clamp, cross-account jitter. Register real-machine items in `docs/real-machine-acceptance-backlog.md`.
- [x] 9.6 Regression discipline: `npm run test:acceptance` -> full `npm test` -> `npm run typecheck` in edge and cloud after protocol/risk changes; AC-PROTO-*/AC-RISK-* must pass. <!-- validation for c870bce9dcda1862424257188a691491e258d634 / 47cf5597daabaaa44752cde1372cab132c823ff1 / 053fa950dd7860273e7083ef9f15431b64234e8b: edge acceptance/full/typecheck passed; cloud acceptance/full/typecheck passed; console typecheck/full test passed -->
