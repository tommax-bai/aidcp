## 1. aidcp-cloud — 曲线取用与回包

- [ ] 1.1 在客户鉴权服务的 Facebook 运行策略端口上新增**必填**的曲线取用方法（返回当前生效的总天数与逐日上限），使两个组装根漏接线时编译期即失败
- [ ] 1.2 环境慢启动读取记录补出该环境平台（与既有 ownership 查询同一 SQL 形状，不新增一次查询）
- [ ] 1.3 未绑定 / 绑定冲突分支的总天数改为由调用点传入权威值，删除内联的 7
- [ ] 1.4 慢启动读路由在 Facebook 环境回传曲线：逐日上限只含该平台能执行且按日计数的动作（浏览 / 点赞 / 评论 / 关注 / 发布 / 搜索 / 加组），并带 day 序号
- [ ] 1.5 曲线缺席按「字段整个不出现」实现：非 Facebook、平台未确认、策略不可用三种情况都不下发，且不影响其余慢启动真态返回
- [ ] 1.6 单体组装根注入曲线取用（读运行时全局策略，与配额 clamp 同一来源）

## 2. aidcp-cloud — 测试

- [ ] 2.1 单测：后台改过曲线后，回包逐格等于策略值；总天数跟随策略
- [ ] 2.2 单测：未绑定账号的 Facebook 环境总天数取权威值而非 7，且仍不返回 binding / dayQuotas
- [ ] 2.3 单测：非 Facebook 环境与策略不可用时回包整个不含曲线字段，其余真态照常
- [ ] 2.4 跑 `npm run test:acceptance` → `npm test` → `npm run typecheck`

## 3. aidcp-api — 派生仓同步与手写组装根

- [ ] 3.1 从控制仓跑派生同步（先 dry-run 对账再 apply），确认客户鉴权服务的改动已进派生仓
- [ ] 3.2 派生仓手写组装根注入曲线取用（该文件不参与自动同步，必须手改）
- [ ] 3.3 派生仓 `npm run typecheck` 通过

## 4. aidcp-edge — 客户端曲线表按数据渲染

- [ ] 4.1 页面结构：删除写死的表体数字与「与云端常量逐格同步」的注释承诺，表头补上「搜索」列
- [ ] 4.2 渲染层：按回包曲线渲染表体，行数取回包行数；数字一格不本地推算
- [ ] 4.3 缺席降级：回包无曲线时就地说明读不到，不显示任何数字，不回落上一次 / 内置 / 另一环境的曲线
- [ ] 4.4 说明文案按权威总天数表述，删除写死的「7 天」
- [ ] 4.5 环境切换时曲线随环境隔离，晚到响应不得落到当前环境

## 5. aidcp-edge — 测试

- [ ] 5.1 单测：给定回包曲线 → 渲染行数与数字逐格一致；给定 10 天曲线 → 10 行
- [ ] 5.2 单测：回包无曲线 → 不出现任何数字且给出读不到的说明
- [ ] 5.3 跑 `npm run test:acceptance` → `npm test` → `npm run typecheck`

## 6. 集成与部署

- [ ] 6.1 两仓分别 rebase 到最新默认分支、合并、推送
- [ ] 6.2 部署 dev（客户 API 由 api 进程承载，重启的必须是承载客户口的服务）
- [ ] 6.3 dev 健康检查：服务在跑、客户口可达、数据库可读
- [ ] 6.4 真机验收项登记进 backlog（客户端需出包才到运营机，本仓默认不打包）

## 7. 收口

- [ ] 7.1 tasks.md 按 sub-repo 回写 commit sha 与偏离说明
- [ ] 7.2 `openspec validate sync-slow-start-curve-to-client --strict` 通过
- [ ] 7.3 归档 change，清理 worktree 与分支
