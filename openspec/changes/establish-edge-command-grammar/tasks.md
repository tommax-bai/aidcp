# Tasks

> 本 change 与 `establish-edge-addressing-layers` 同形态：判据文档 + 规格 + 架构指针，**零源码改动**。
> 它是「新增浏览器操作时该在哪个维度加什么能力」的裁决依据（用户 2026-08-06 定为重要规则）。

## 1. 判据文档

- [x] 1.1 写 `docs/edge-command-grammar.md`：六条规则全文（含每条的「为什么」）、快照声明（2026-08-06，引用前重核）。 <!-- aidcp 8195ec3f docs/edge-command-grammar.md -->
- [x] 1.2 四个判例写足推导过程，不只给结论： <!-- aidcp 8195ec3f §4 四判例含推导 -->
  - **判例一（正）** Reels 点赞分叉：同一条点赞命令、边缘按自维护会话状态分叉两套实现、该状态漂过（11/11 进不了 Reels）、云端归账仲裁只在信息流面激活——「在哪个面」已影响云端记账，却是事后从回执读到的。按判据②④命中 ⇒ 该由下发时指定。
  - **判例二（正）** `group.open → group.join` 窗口：群组页约 7s 才渲染完 + 节奏刻意有停顿，推定与现实之间有真实窗口；加群不可逆且已付过重复加群的代价——期望进命令给了边缘说「我不在你以为的地方」的机会。
  - **判例三（反）** `page.scroll` 的 `reason?: string`：区分早已存在、没有语法落点，退化成裸字符串加注释——一致性规则要防的正是这个形态。
  - **判例四（混合）** 七条按执行载体归错类：分类被「怎么执行」污染，救援清单是补丁——类别规则的来历。
- [x] 1.3 撤回结论记入「讨论中作废」节（防重走），共五条：「云端看不到页面所以要编进命令名」（云端有推定，真理由是推定开环）；「CLI 宽容文化危险因为会留痕」（拿后果定归属，与 addressing-layers 判例二同错）；「简洁=条数少、与清晰冲突」（简洁=职责单一，`page.scroll` 是四件事的合集不是简洁）；「命令名+回执是云端的全部输入」（**已撤回的第一条经 OpenCLI 对照漏回来一次、二次被用户抓住**——写对照 / 摘要时 MUST 对照本节自查，对照文字是作废结论回流的通道）；「存量不重命名因为要出包才生效」（出包随时可做、不是障碍；真理由＝非原子升级的双名兼容窗口 + 热点串行，且是优先级判断不是做不到）。 <!-- aidcp 8195ec3f §7 六条（讨论中长到六条，超出任务写的五条） -->
- [x] 1.4 OpenCLI 对照一节：两层分离可学；无页面建模按**修正后口径**写——差异是「看」与「决定」的次序与成本（每步先看 vs 前提随命令走、执行点核验），MUST NOT 写成「云端看不到 / 命令名+回执是全部输入」；含仓库指针。 <!-- aidcp 8195ec3f §5 修正后口径 -->
- [x] 1.5 文档头部写明与两份既有产物的分工：`edge-addressing-layers` 答「归谁」，本文答「归了账号层之后命令怎么设计」，操作说明书答「每条命令的声明」——三者互为消费方，MUST NOT 互相复制。 <!-- aidcp 8195ec3f 文档头部分工表 -->
- [x] 1.6 **目标词汇蓝图**（判据文档附录，2026-08-06 用户定调后升格为核心产出）：46 条存量 + 观察命令逐条映射到目标形态——保留原名 / 改名（给出新名）/ 拆分（如 `page.scroll` 按面拆）/ 删除；各族命名约定逐族定案（手势=面.动作、互动=对象.动作、导航=目标.动作；非页面域今名基本已符，缺的是归类修正）；每条标注所属迁移批次与批次顺序。蓝图是设计产物，MUST NOT 做成第二张运行时表。 <!-- aidcp 8195ec3f §6 蓝图：4删/7改类/2拆/4改名/1新增/29保留，6批 -->
- [x] 1.7 OpenCLI 借鉴清单落进文档（design 决策七的表）：两层分离 / 命名空间化 / 只读-交互分组（=留痕维）/ 观察命令四项怎么落，加唯一不借的一项（每步快照循环）及理由。 <!-- aidcp 8195ec3f §5 借四不借一 -->

## 2. 指针与交叉登记

- [x] 2.1 `docs/architecture.md` 加一行指针（与 addressing-layers 指针并列处）。 <!-- aidcp 8195ec3f architecture.md §2.2 指针并列 -->
- [x] 2.2 核对 `close-account-layer-operation-manual` tasks 9.0 的依赖表述（「根治等本规则立起来」）指向本 change 名。 <!-- aidcp 8195ec3f close-account-layer-operation-manual tasks 9.0 已点名本 change 与批2 -->
- [x] 2.3 迁移序列登记（只登记不做）：按蓝图批次列出后续 change 序列——观察命令、七条归类根治、`page.scroll` 拆分等各归其批，批次串行走协议热点，每批直接切换（两端同批改名 → 部署云端 → 装机，无双名并行、无别名层）。落点：本文件 + `docs/edge-command-grammar.md` 蓝图节。 <!-- aidcp 8195ec3f §6.3 批次序列 -->

## 3. 验证与归档

- [x] 3.1 `openspec validate establish-edge-command-grammar --strict` 通过。 <!-- 2026-08-06 -->
- [x] 3.2 验收：未改任何存量命令名 / 类别 / 载荷；未产生新的命令清单或归属表；无 sub-repo 改动（edge/cloud 工作区实查，仅有旧备份目录与本次无关）。 <!-- 2026-08-06 -->
- [x] 3.3 控制仓 commit + push（本 change 全部产物在控制仓，无部署）。 <!-- aidcp 8195ec3f 已推送 -->
- [ ] 3.4 归档（archive 时 spec delta 并入 `openspec/specs/edge-command-grammar/`）。**待用户过目蓝图后执行**——蓝图里有逐条处置（如 `user.follow` / `comment.like` 新名、批次划分），归档前留一次改动窗口。
