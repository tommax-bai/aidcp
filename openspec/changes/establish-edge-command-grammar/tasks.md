# Tasks

> 本 change 与 `establish-edge-addressing-layers` 同形态：判据文档 + 规格 + 架构指针，**零源码改动**。
> 它是「新增浏览器操作时该在哪个维度加什么能力」的裁决依据（用户 2026-08-06 定为重要规则）。

## 1. 判据文档

- [ ] 1.1 写 `docs/edge-command-grammar.md`：六条规则全文（含每条的「为什么」）、快照声明（2026-08-06，引用前重核）。
- [ ] 1.2 四个判例写足推导过程，不只给结论：
  - **判例一（正）** Reels 点赞分叉：同一条点赞命令、边缘按自维护会话状态分叉两套实现、该状态漂过（11/11 进不了 Reels）、云端归账仲裁只在信息流面激活——「在哪个面」已影响云端记账，却是事后从回执读到的。按判据②④命中 ⇒ 该由下发时指定。
  - **判例二（正）** `group.open → group.join` 窗口：群组页约 7s 才渲染完 + 节奏刻意有停顿，推定与现实之间有真实窗口；加群不可逆且已付过重复加群的代价——期望进命令给了边缘说「我不在你以为的地方」的机会。
  - **判例三（反）** `page.scroll` 的 `reason?: string`：区分早已存在、没有语法落点，退化成裸字符串加注释——一致性规则要防的正是这个形态。
  - **判例四（混合）** 七条按执行载体归错类：分类被「怎么执行」污染，救援清单是补丁——类别规则的来历。
- [ ] 1.3 三条撤回结论记入「讨论中作废」节（防重走）：「云端看不到页面所以要编进命令名」（云端有推定，真理由是推定开环）；「CLI 宽容文化危险因为会留痕」（拿后果定归属，与 addressing-layers 判例二同错）；「简洁=条数少、与清晰冲突」（简洁=职责单一，`page.scroll` 是四件事的合集不是简洁）。
- [ ] 1.4 OpenCLI 对照一节：两层分离可学；无页面建模不可抄（决策者在场 vs 在云端）；含仓库指针。
- [ ] 1.5 文档头部写明与两份既有产物的分工：`edge-addressing-layers` 答「归谁」，本文答「归了账号层之后命令怎么设计」，操作说明书答「每条命令的声明」——三者互为消费方，MUST NOT 互相复制。

## 2. 指针与交叉登记

- [ ] 2.1 `docs/architecture.md` 加一行指针（与 addressing-layers 指针并列处）。
- [ ] 2.2 核对 `close-account-layer-operation-manual` tasks 9.0 的依赖表述（「根治等本规则立起来」）指向本 change 名。
- [ ] 2.3 后续缺口登记（只登记不做）：观察命令（问在哪个面；倾向与身份读取合并成一条「问现状」，留给实装 change 定）；七条归类根治；`page.scroll` 面区分收敛。落点：本文件 + `docs/edge-command-grammar.md` 待决节。

## 3. 验证与归档

- [ ] 3.1 `openspec validate establish-edge-command-grammar --strict` 通过。
- [ ] 3.2 验收：未改任何存量命令名 / 类别 / 载荷；未产生新的命令清单或归属表；无 sub-repo 改动。
- [ ] 3.3 控制仓 commit + push（本 change 全部产物在控制仓，无部署）。
- [ ] 3.4 归档（archive 时 spec delta 并入 `openspec/specs/edge-command-grammar/`）。
