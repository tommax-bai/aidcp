## 1. aidcp-edge — 面板空态占位（前置，已落地）

- [x] 1.1 没有过程条目时不让空的过程容器参与占位，空态说明直接接在面板标题之后，阶段预告不被裁切 <!-- aidcp-edge bdc652f 前置修复，先于本 change 立项落地 -->

## 2. aidcp-edge — 活动流接入内容工作区

- [ ] 2.1 内容工作区新增按环境的活动缓冲与 `pushActivity(entry)` / `setActivities(list)` 接口，条数有界，切换环境时清理非当前环境的展示状态
- [ ] 2.2 `renderer.js` 在 `routeActivity` 单点转发活动条目给内容工作区（保持 envId 归属判断在原处，不新增第二个订阅点）
- [ ] 2.3 切换到小红书环境时把该环境已有缓冲整体补发给内容工作区，避免刚切过去时面板空白而运行面板有内容

## 3. aidcp-edge — 面板取数模型与三态

- [ ] 3.1 面板取数模型新增 `kind: 'activity'` 分支：创作任务投影优先，无任务时用活动记录，条目只携带真实 `ts` 与句子
- [ ] 3.2 三态判定改按来源状态（创作队列 / 待审稿的读取状态），不再以数据是否为空作唯一依据
- [ ] 3.3 读取失败时面板呈现「暂时读不到当前进度」并提供重试入口，不再显示「当前没有正在进行的任务」
- [ ] 3.4 左栏文案按九档运行态取值：仅浏览循环已跑起来时才可讲成正在浏览，其余档位沿用既有诚实文案

## 4. aidcp-edge — 活动记录的呈现约束

- [ ] 4.1 活动条目渲染为「相对时间 + 句子」，不显示阶段名、不显示完成勾，使用中性标记
- [ ] 4.2 仅在浏览循环已跑起来时，最新一条带进行中标记与逐字效果；其余档位全部按历史呈现且无动效
- [ ] 4.3 相对时间按原始 `ts` 重算，重渲染不得把旧条目改写为刚刚发生
- [ ] 4.4 面板高度与横向溢出不回归：条目多时由过程区自身滚动承载，面板总高仍不超过 255px

## 5. aidcp-edge — 测试

- [ ] 5.1 用例：无创作任务 + 浏览已跑起来 + 有活动记录 → 面板展示活动条目，且不出现「当前没有正在进行的任务」
- [ ] 5.2 用例：环境已暂停 / 已停止但有活动记录 → 条目按历史呈现，最新一条不带进行中语义与动效
- [ ] 5.3 用例：创作队列读取失败 → 面板显示「暂时读不到当前进度」并可重试，不宣布没有任务
- [ ] 5.4 用例：创作任务存在时仍优先展示任务投影，活动记录不覆盖阶段链
- [ ] 5.5 用例：活动记录不产生阶段名 / 完成勾 / 完成度
- [ ] 5.6 回归：`npm run test:acceptance` → `npm test` → `npm run typecheck` 全绿

## 6. 收尾

- [ ] 6.1 提交并推送 aidcp-edge（默认分支 master），把 commit-sha 回写本 change 的 tasks.md
- [ ] 6.2 `openspec validate surface-browse-activity-in-work-card --strict` 通过
- [ ] 6.3 真机验收项（面板在真实浏览会话下的条目密度与可读性）登记到 `docs/real-machine-acceptance-backlog.md`
