# curated-inspiration-corpus

本 change（Phase 3）扩展 `curated-inspiration-corpus` 的准入语义：在既有共鸣门槛之后插入一道模型评估段，由两个独立评估角色（正文 / 评论）读全文判内容层相关性 + 丰富度。requirement 名与 `curated-inspiration-corpus` 的既有 delta 互不重叠，归档时并入同一 capability。

## ADDED Requirements

### Requirement: 两段式准入——共鸣预筛在前、模型评估在后

精选准入 SHALL 分两段：第一段为**确定性共鸣预筛**（零 LLM）——笔记按收藏地板或收藏/点赞比率、评论按评论赞数或已确认点赞；第二段为**模型评估**——仅对第一段幸存者读全文判内容层相关性与丰富度。未过第一段者 MUST NOT 进入第二段、MUST NOT 触发任何 LLM 调用（成本红线）。仅两段皆过者方落入精选语料。

#### Scenario: 共鸣不过 → 零模型调用、不纳入
- **WHEN** 一篇被观测笔记的收藏数未达地板、且收藏/点赞比率不达标
- **THEN** 第一段即弃，MUST NOT 调用 LLM 评估，MUST NOT 落精选语料

#### Scenario: 共鸣过但模型评估不过 → 不纳入
- **WHEN** 一篇笔记过了共鸣预筛，但模型评估判定其与账号领域不相关、或内容单薄、或属纯广告/标题党
- **THEN** MUST NOT 落精选语料

#### Scenario: 两段皆过 → 纳入
- **WHEN** 一篇笔记过了共鸣预筛，且模型评估判定相关且内容丰富且非广告标题党
- **THEN** 落精选语料，纳入原因标注为模型评估准入

#### Scenario: 红线反例——跳过预筛对每篇笔记都调模型（禁止）
- **WHEN** 有实现对每篇浏览到的笔记都直接调 LLM 评估、不先过共鸣预筛
- **THEN** MUST 视为违规、不予合入；模型评估 MUST 只跑在共鸣预筛幸存者上

### Requirement: 正文进精选评估角色（读全文判相关性与丰富度）

系统 SHALL 新增一个独立角色评估笔记**正文**是否适合进精选。该角色 MUST 依据笔记**全文**（而非仅标题）判定：① 与账号创作领域的相关性；② 内容丰富度（是否有具体信息/观点/可复用细节，区别于水帖）；③ 是否纯广告/标题党/蹭热点。仅相关且丰富且非广告标题党者准入。该角色 MUST 持账号绑定的模型客户端与账号人设领域，MUST 以 fire-and-forget 方式运行（评估/落库失败只记录、不抛、不阻塞浏览主路径）。

#### Scenario: 相关且扎实 → 准入
- **WHEN** 共鸣预筛幸存的一篇笔记，其全文与账号领域相关、信息扎实、非广告
- **THEN** 角色判定准入并落精选语料

#### Scenario: 标题像但正文跑题 → 拒绝
- **WHEN** 一篇笔记标题看似相关，但全文与账号领域无实质关系
- **THEN** 角色 MUST 依据全文判定不相关、不纳入（修复「只看标题层」的缺口）

#### Scenario: 高收藏但单薄/标题党 → 拒绝
- **WHEN** 一篇高收藏笔记内容单薄、或属标题党/纯广告
- **THEN** 角色判定不纳入

#### Scenario: 红线反例——仅凭标题或忽略丰富度纳入（禁止）
- **WHEN** 有实现只看标题相关、或不判内容丰富度就纳入
- **THEN** MUST 视为违规、不予合入；MUST 依据全文同时判相关性与丰富度

### Requirement: 评论进精选评估角色（独立于正文角色）

系统 SHALL 新增一个**独立于正文评估的**角色评估评论是否适合进精选。其第一段预筛为评论赞数达标**或**该评论已被机器人确认点赞；第二段由模型判定评论与账号领域的相关性及其作为「角度/口吻范例」的借鉴价值，并排除水评/广告。仅过两段者落精选语料（评论类型）。评论与正文的判定标准与提示词不同，故 MUST 为两个独立角色。喂「写评论」的评论写作语料（既有 corpus）行为 MUST 不变。

#### Scenario: 高赞优质评论 → 准入
- **WHEN** 一条评论赞数达标（或已确认点赞），且模型判定其相关且有借鉴价值、非水评
- **THEN** 落精选语料（content_type=comment）

#### Scenario: 水评/广告评论 → 拒绝
- **WHEN** 一条评论虽过预筛，但模型判定其为水评或广告、无借鉴价值
- **THEN** MUST NOT 落精选语料

#### Scenario: 评论写作语料不受影响
- **WHEN** 评论进精选评估角色运行
- **THEN** 既有「写评论」用的评论语料归档与召回行为保持不变

### Requirement: 自有收藏免模型评估直接纳入

机器人对笔记的**自有收藏**（自有收藏自动纳入路径）SHALL 绕过模型评估直接进精选——机器人主动收藏本身即一次受风控约束的策展判断，强度高于共鸣预筛。机器人点赞（弱信号、只标既有行）同样不经模型评估。

#### Scenario: 机器人收藏 → 直接纳入、不调模型
- **WHEN** 机器人对一篇笔记执行了真实收藏
- **THEN** 该笔记直接纳入精选（自有收藏自动纳入），MUST NOT 因模型评估而被拦

#### Scenario: 红线反例——对自有收藏也强制模型评估并据此拒绝（禁止）
- **WHEN** 有实现对机器人自有收藏的笔记跑模型评估、并据此拒绝纳入
- **THEN** MUST 视为违规、不予合入；自有收藏免评估直接进

### Requirement: 评估诚实——降级/解析失败不静默纳入

模型评估失败、超时、降级或输出解析失败时，系统 MUST 诚实判定**不纳入**，MUST NOT 静默纳入、MUST NOT 编造评分。评估 MUST NOT 阻塞浏览主路径（fire-and-forget）。

#### Scenario: LLM 降级 → 不纳入
- **WHEN** 模型评估调用失败或超时
- **THEN** 该候选不纳入精选，浏览主路径不受影响

#### Scenario: 输出解析失败 → 不纳入
- **WHEN** 模型返回无法解析为有效判定结果
- **THEN** 诚实判不纳入，MUST NOT 默认纳入

#### Scenario: 红线反例——降级时默认纳入（禁止）
- **WHEN** 有实现在评估失败/降级时把候选默认纳入精选
- **THEN** MUST 视为违规、不予合入；失败一律按不纳入处理
