## 1. Contract and workspace setup

- [x] 1.1 Create matching Cloud and Console worktrees with physical dependencies, record branch ownership, and confirm canonical checkouts remain on default branches.
<!-- Control `/Users/baitianxing/codes/aidcp.wt/wechat-group-scoped-reply-config`, Cloud `/Users/baitianxing/codes/aidcp-cloud.wt/wechat-group-scoped-reply-config`, Console `/Users/baitianxing/codes/aidcp-console.wt/wechat-group-scoped-reply-config`; all use `codex/wechat-group-scoped-reply-config`. Canonical branches remained `main/master/master`. Cloud `npm ci --prefer-offline` succeeded. Console has no tracked lockfile, so `npm ci` correctly failed and physical dependencies were installed with `npm install --prefer-offline`; neither dependency tree is linked. -->
- [x] 1.2 Add v2 internal/customer API schema and fixtures for config scopes, effective source, scope CRUD/publish/preview/audit, and validate them locally.
<!-- Added `docs/contracts/wechat-channels-interaction/v2` overlay with stable source/head/snapshot/effective/preview/inventory contracts and customer replyConfig source projection. `check-jsonschema --check-metaschema schemas/*.schema.json`, internal fixtures, and customer fixtures all passed. -->

## 2. Cloud scoped configuration storage

- [x] 2.1 Add an additive PostgreSQL migration for stable reply config scopes, immutable scope versions/templates/rules/profiles, scope audit, and reply-job `config_scope_id` provenance.
<!-- Cloud `migrations/0048_wechat_group_reply_config.sql`; additive tables/column/index only, legacy account config untouched. Migration contract test passed. -->
- [x] 2.2 Implement the scope store with CAS draft mutations, validation, publish, immutable lookup, list/member counts, and exact group/default resolution.
<!-- Cloud `ReplyConfigScopeStore`; immutable JSONB aggregate snapshots, row-locked CAS, publish validation, exact group/default membership and scope audit. -->
- [x] 2.3 Add migration inventory/fingerprint and `legacy|shadow|scoped` resolver modes without logging message/template bodies.
<!-- Cloud `ReplyConfigResolver`; default legacy, body-free SHA-256 behavior fingerprint, inventory conflict flag and shadow metadata-only observation. Resolver tests passed. -->

## 3. Cloud runtime and APIs

- [x] 3.1 Add internal scope management/effective-source endpoints with grants, validation, preview account membership checks, audit, and explicit legacy write deprecation behavior.
<!-- Cloud `InteractionScopeInternalApi`; scope CRUD/publish/preview/audit/effective-source/migration-inventory. `interaction-scope-internal-api.test.ts` covers list/effective, CAS and fail-closed preview membership. -->
- [x] 3.2 Update reply generation, approval, editing and sending to freeze/load `configScopeId + configVersion` while preserving legacy jobs.
<!-- Workflow freezes both fields for new scoped jobs; approval/edit/send/result paths use frozen scope snapshots, while null scope IDs continue legacy lookup. Resolver historical-job test passed. -->
- [x] 3.3 Update customer-auth replyConfig projections to use the effective resolver and expose only non-sensitive source/version state.
<!-- Customer projection now reports mode/source/status/version metadata from the resolver and never returns config bodies. Existing customer API tests remained green in the full suite. -->
- [x] 3.4 Ensure offboarding removes account data and legacy config without deleting shared scopes; add regression coverage.
<!-- Existing per-account purge remains unchanged for legacy config/runtime/content and explicitly does not delete scope heads, immutable versions or scope audit; purge regression passed. -->

## 4. Console group/default strategy management

- [x] 4.1 Add scope/effective-source DTOs and API clients from the shared contract without inventing a parallel schema.
<!-- Console mirrors the v2 contract in `interactionReplyConfig.ts`/types; API path and effective-source tests passed. -->
- [x] 4.2 Add a “视频号策略” page listing default/group scopes, account counts, versions and missing states, with scope-scoped editing/publish/audit and representative-account preview.
<!-- Added `/wechat-strategies` navigation/page and reused the validated editor in scope mode; default/group listing and stable-scope mutation tests passed. -->
- [x] 4.3 Change the account surface to show effective source and keep account runtime controls separate from shared strategy editing.
<!-- Account rows now show actual resolver mode/source, link to the strategy page and open a runtime-only drawer that works without legacy reply config. -->
- [x] 4.4 Add focused UI tests for default/group resolution, missing config, account switching, permission/version conflicts and no-side-effect preview wording.
<!-- Added scope API/page/editor coverage and retained the existing 34-case editor suite for stale switching, missing config, permissions, CAS conflicts and preview no-send honesty. -->

## 5. Validation, integration and rollout

- [x] 5.1 Run Cloud acceptance/focused tests, full tests and typecheck; run Console focused tests, serial full suite, typecheck and build.
<!-- Cloud commit `9e1380d`: focused 22/22, acceptance 59/59 (one gated E2E skipped), full 2608 passed/8 skipped, typecheck passed. Console commit `1bed7cf`: focused legacy editor 34/34 plus scope/API/page 5/5, serial full 193 passed/1 skipped before the final added scope editor test (which passed separately), typecheck and production build passed; only pre-existing jsdom/chunk-size warnings remained. -->
- [x] 5.2 Run `openspec validate wechat-group-scoped-reply-config --strict`, record repo commits/validation/deviations in this task file, and push feature branches.
<!-- Feature commits pushed: Cloud `9e1380d`, Console `1bed7cf`, Control artifacts `750d4f5`. Strict OpenSpec and all v2 JSON metaschema/fixture checks passed. Deviation: Console has no tracked lockfile, so its worktree used physical `npm install --prefer-offline` after the required `npm ci` failure. -->
- [x] 5.3 Rebase and fast-forward integrate Control, Cloud and Console default branches in dependency order, rerun required validation, and push.
<!-- Defaults advanced during development, so Control and Cloud were rebased without conflicts and without force-pushing the already-published feature refs. Fast-forward integration/push completed: Control `c21f5f0` on `main`, Cloud `4212e54` on `master`, Console `1bed7cf` on `master`. Post-integration strict OpenSpec, Cloud focused 22/22 + typecheck, and Console scope focused 5/5 + typecheck all passed. -->
- [x] 5.4 Check the named dev deployment boundary; deploy only additive eligible artifacts when the database target is isolated/safe, otherwise record the exact blocker without touching ol.
<!-- `scripts/deploy-target dev --check` passed for `121.89.85.150`. Dev/ol still share PostgreSQL, so only additive 0048 was eligible: backups `cloud.bak.20260720-110613.tar.gz`, `console.bak.20260720-110613.tar.gz`, `.env.bak.20260720-110613`, and schema dump were created; committed Cloud `4212e54` and Console `1bed7cf` synced with checksum verification; 0048 applied while legacy tables remained. Service active with NRestarts=0, ports 8787/8090/8088, public health, Console asset `index-BuTGCZcR.js`, PostgreSQL, Feishu WS onReady and all four isales services verified. Resolver intentionally remains the unset `legacy` default: scopes are still empty and the body-free live inventory shows `<default>` 1 account/1 legacy published, `tom` 2 accounts/1 legacy published, so no winner was invented and no scoped cutover was claimed. Existing 0046 legacy send-attempt schema also remained untouched because it contains destructive DDL against the shared database. `ol` was not contacted or changed. -->
- [x] 5.5 Repair and verify the dev runtime-role privileges for the additive group-scope tables after an admin-owned migration caused the strategy list API to return 500.
<!-- Root cause was reproduced on `dev`: 0048 tables were owned by `postgres` while the Cloud runtime role `aidcp` received `permission denied for table interaction_reply_config_scopes`. Cloud commit `027feb3` adds additive migration `0050`, dynamically deriving the runtime role from the existing `interaction_reply_configs` owner instead of hard-coding an environment role. Migration-contract 12/12 and typecheck passed in the feature and canonical checkouts. `master` was fast-forwarded and pushed, then only the committed 0050 file was checksum-synced to dev to avoid deploying unrelated concurrent master changes. Backup `/opt/aidcp/aidcp-wechat-scope-schema.bak.20260720-143047.sql` was created and 0050 applied as the administrative DB role. The runtime role now has DML privileges on all three scope tables; the exact `ReplyConfigScopeStore.init()+listScopes()` path returned default 1 member and group `tom` 2 members without configuration. Service remained active with NRestarts=0 and local panel/public health both returned `{"ok":true}`; no restart was needed and `ol` was not contacted or changed. -->

## 6. Immediate legacy retirement and account-table simplification

- [x] 6.1 Revise the OpenSpec and v2 contracts to remove legacy/shadow behavior, account strategy navigation and generic account actions.
<!-- Revised proposal/design/specs and v2 internal/customer overlays: scoped is the only resolution mode, migration inventory is removed, legacy jobs fail closed, cleanup is limited to five strategy tables plus config audit, and account-table actions move into named fact columns. -->
- [x] 6.2 Make Cloud scoped-only, retire account strategy APIs/migration inventory and fail closed for historical jobs without `config_scope_id`.
<!-- `ReplyConfigResolver` now has one fixed `scoped` path; account strategy routes return `410 account_reply_config_retired`, migration inventory is absent, and jobs without frozen `config_scope_id` resolve no snapshot. Focused Cloud regressions passed. -->
- [x] 6.3 Add a row-count-verifiable migration that clears only legacy account strategy data and related config audit while preserving runtime/scoped/interaction/risk data.
<!-- Cloud `0051_retire_account_reply_configs.sql` deletes the five WeChat account-strategy tables and matching config/policy/template/rule/profile audit rows only. Migration-contract coverage rejects runtime, scoped, interaction job/attempt, risk, DROP, TRUNCATE and ALTER targets. -->
- [x] 6.4 Remove account strategy lookup/navigation in Console; move status, risk and quota actions into their fact columns, keep a named WeChat runtime-control entry and remove the generic action column.
<!-- Account rows no longer fetch or link account strategy state. Operator status, risk and quota tags are keyboard/click controls in their own columns, Facebook config is attached to the platform tag, and only WeChat rows expose the named runtime control; the generic action column is gone. -->
- [x] 6.5 Run focused/full proportional Cloud and Console tests, typechecks/build, v2 fixture checks and strict OpenSpec validation; commit, integrate and push defaults.
<!-- Cloud `a7a169a`: focused 24/24 after integration, acceptance/full 2654 passed/8 skipped, typecheck passed. Console `3e30747`: changed-surface 4/4, enum safety 2/2, pre-rebase full 210 passed/1 skipped and production build passed; the three incoming default commits did not touch this change's files, and post-rebase exact tests/typecheck/build passed under a 30-second test timeout because the host load exceeded 100 and the standard five-second full run timed out broadly. V2 metaschema/internal/customer fixtures and strict OpenSpec passed. Cloud and Console defaults were fast-forwarded and pushed; Control integration is recorded by the follow-up rollout task. -->
- [x] 6.6 Back up the shared database, execute the approved test-strategy cleanup, deploy only the committed Cloud/Console artifacts to dev, and verify row counts, scoped fail-closed behavior, health and unrelated services without touching ol.
<!-- `scripts/deploy-target dev --check` selected `121.89.85.150`. Backup batch `20260720-152834` contains Cloud, Console, `.env`, full schema and restorable legacy-strategy/audit data with SHA-256 receipts. Immediately before deletion the exact guarded target was configs 2, versions 17, templates 26, rules 16, profiles 34 and config audit 18; an unrelated concurrent process had already removed the three historical null-scope reply jobs found during the initial inventory, so cleanup paused and re-baselined preserved jobs/attempts at 0 before proceeding. Committed Cloud `a7a169a` was synced/restarted first so account strategy writes were retired; the local/remote 0051 checksum matched, then a locked count assertion and 0051 deleted audit 18/templates 26/rules 16/profiles 34/versions 17/configs 2. Post-checks are all zero for the six targets while runtime controls 3, scopes/versions 0/0, jobs/attempts 0/0 and risk state/counters/interactions 11/4272/473 are unchanged. Live runtime-role resolution is fixed `scoped`: `k1eoujd8` resolves group `tom` as `group_config_missing`, and `k1esb68e` resolves default as `default_config_missing`. Console deployed asset `index-DkltVfWQ.js` comes from `e362a8a`, a later descendant containing this change's `3e30747`, so the parallel Console fix was preserved. Cloud stayed active with NRestarts=0, ports 8787/8090/8088 and local/public health passed, PostgreSQL accepted connections, Feishu WS reported onReady, and all four isales services remained active. The ol host was not contacted; the approved test-data cleanup intentionally applied to the still-shared PostgreSQL boundary. -->
