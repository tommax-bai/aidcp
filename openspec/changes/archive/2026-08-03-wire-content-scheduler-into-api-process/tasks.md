## 0. 起手：验事实、别信记载

- [x] 0.1 逐条复验本 change 的四条前提仍成立（本项目已因「登记比代码旧」栽过五次）：
  排期器类在接口仓、接口手写入口零构造、回程路由在接口仓零命中、自动化侧客户端确在调它。
  任一条已被别人修掉就当场改 proposal，别照着过期前提开工。
  <!-- 2026-08-03 实测四条全部成立：类在 aidcp-api/src/orchestrator/content-scheduler.ts；
       api 的 server.ts 与 index.ts 对 ContentScheduler 命中数均为 0；
       `schedule-feedback` 在 api/src/ 零命中；automation 侧
       src/transport/api-aux-authority-http.ts:76 确有该路由常量在被客户端使用。 -->
- [x] 0.2 摸清「环境绑定」今天的全部落点，逐个记 `文件:行`：调度器里那道跳过、
  下发前形状校验、下发时环境匹配、小时格占位的入参校验。**收尾时按这份清单逐条对**，
  别靠印象判断改全了。
  <!-- 2026-08-03 普查（基线 cloud 648fb4d）：
       ① content-scheduler.ts:456 `if (!identity.envKey) continue;`（那道跳过）
          + :29/30 身份类型 + :33 VerifiedOnlineAccountIdentity + :84 归属类型 + :488/494 传递；
       ② publish-dispatcher.ts:347-348（形状校验里的两行）；
       ③ publish-dispatcher.ts:712-713（下发时精确匹配）；
       ④ content-schedule-store.ts:540-541（入参强制非空）+ :351 建表 NOT NULL。
       **另有一处普查时没数进来、靠编译器点出**：kernel/publish-pipeline-types.ts:131
       的 `ScheduledExecutionAttribution.envKey` —— 归属类型在 kernel 里另有一份。
       ⇒ 教训：按符号 grep 会漏掉「同形状但换了名字的第二份类型」，
       所以 1.1 之后**不加强转、让编译器把剩下的点出来**这一步不是可选的。 -->
- [x] 0.3 确认小时格占位表的唯一键确实是「账号 + 动作」、环境列只是记录列
  （这是「拿掉环境不削弱幂等」的**唯一**依据，判错则整个方案不成立）。
  <!-- 2026-08-03 实读 migrations/0053：`PRIMARY KEY (account_id, action)` —— 地基成立，
       环境列不参与唯一性，拿掉它不削弱幂等。
       **但同一眼证伪了设计里「不加迁移」那句**：`env_key TEXT NOT NULL`。
       环境标识拿不到时该写的诚实值是 NULL，空字符串是个「看着像数据的假值」。
       ⇒ 本 change **必须带一条迁移**（放宽 NOT NULL，expand 类）。表属主是 api。
       已同批更正 proposal / design / 本文件 1.4，并按 §4.6 补全迁移耦合单元（见 1.4a）。
       **这正是 0.2/0.3 存在的意义**：照着「不加迁移」写下去，会写出一个空串占位。 -->

## 1. 去掉自动发帖的浏览器环境绑定（纯放松，可单独回退）

- [x] 1.1 事实源仓：删掉调度器里「解析不出环境身份就跳过发帖」那道跳过；
  相应放宽依赖接口里的身份类型（发帖不再要求非空环境标识）。
  <!-- cloud 258f41a：环境闸删除，身份类型放宽为诊断用；VerifiedOnlineAccountIdentity 整型删除（零残留引用）。 -->
- [x] 1.2 事实源仓：下发前的归属形状校验里去掉环境标识这一项；
  **部署目标必须是 dev 或 ol 且等于本机这条原样保留**——它管的是「稿子属于哪台机器」，
  两套环境共用一个数据库，删了会串数据。这一条要有独立用例钉住，别跟环境那条一起被删。
  <!-- cloud 258f41a：环境标识退出校验，部署目标判断原样保留并有独立用例钉住（见 1.6）。 -->
- [x] 1.3 事实源仓：删掉下发时「在线环境须精确匹配冻结值否则不投」整条分支；
  改为如实记录实际下发环境。
  <!-- cloud 258f41a：整条分支删除；环境不同时改为一行 info 留痕，不参与判定。 -->
- [x] 1.4 事实源仓：小时格占位不再强制要求环境标识；拿不到时写 **NULL**，
  **MUST NOT 写空字符串占位**——那是个看着像数据的假值。
  <!-- cloud 258f41a：拿不到时写 NULL；未写空串。 -->
- [x] 1.4a **本 change 带一条迁移（0.3 实读发现，原设计写错了）**：放宽 `env_key` 的 NOT NULL，
  expand 类，表属主 api。按 §4.6 那条**五处耦合单元**同批办，一处都不能少：
  ① 迁移文件本身 ② `KNOWN_MAX_SCHEMA_VERSION` ③ 该常量上方那段逐迁移追加的裁定 JSDoc
  ④ `boundaries/table-ownership.json` ⑤ schema 测试里**第二次写死**的那个常量
  （一处从目录**算**最大版本、一处**逐字写死**，改完前者会以为完事了）。
  <!-- cloud 258f41a：0109 expand。五处耦合单元实测只中 4 处——④ table-ownership 无需改（本批不新增表，该表早已登记 owner=api）。⑤ 那处「第二次写死」确实在，且它自己注释就写着「每加一条迁移都要在这里同步抬」。 -->
- [x] 1.4b 裁定 `REQUIRED_SCHEMA_VERSION` 抬不抬，并把理由写进 ③ 那段 JSDoc。
  **本 change 的判据是「新行为是否硬依赖它」**：环境标识在场时旧库照写不误，
  缺席时才会撞 NOT NULL ⇒ 是**条件性**硬依赖。倾向抬（让部署顺序被强制：先迁移后重启），
  但 MUST 写清代价——抬了之后，**没跑迁移就重启的机器会 exit 1 并每 5 秒静默重启、零告警**。
  <!-- **裁定翻转过一次，值得记**：初判「不抬」，理由是给运行期 DDL 加了同一句 DROP NOT NULL 兜底 ⇒ 硬依赖不成立。AC-SCHEMA-DDL-OWNER-01/02 当场拦下：运行时 DDL 是只减不增的棘轮，新增 DDL 的落点只能是 migrations/，断言里直接写着正确动作。撤掉兜底后硬依赖恢复成立 ⇒ **抬 REQUIRED 到 0109**。理由与代价都写进了那段 JSDoc。 -->
- [x] 1.5 用例：环境不同照常下发、历史稿件带旧环境标识仍照常下发且元数据不被改写、
  环境标识不完整不再关闭自动发帖。
  <!-- cloud 258f41a：新增 3 条（调度器 envKey=null 照常发且占位记 null / 下发换环境照常 / 归属 envKey=null 照常下发）；另把一条名字已失真的既有用例改名（原名声称 envKey=null 阻断发帖）。 -->
- [x] 1.6 **变异实测**：把 1.2 保留的部署目标判断改成恒真，确认有用例当场红。
  只跑一遍绿不算数——这条闸是本任务组唯一没被放松的那道，必须证明它还在。
  <!-- 已做：把 `if (valid && ...executionTarget === this.executionTarget)` 改成 `if (true)`，
       **恰好一条**用例红——「其它 target 的自动排期稿在授权读取和 Edge 写入前被跳过，状态保持不变」。
       ⇒ 那道闸真承重、且有用例钉着，不是「看着还在」。
       还原用 `cp` 而非 `mv`（保 mtime 会让带缓存的构建跳过重编、于是测的是变异产物）；
       还原后 40/40 绿、文件内 MUTATION 残留 0。 -->

## 2. 接口进程构造并启动排期器

- [x] 2.1 接口仓手写入口：读部署目标 → 缺失或非法则**不启动该定时器**并留具名 fail-closed 日志
  （MUST NOT 按默认目标降级跑）→ 合法则构造调度器并启动心跳。
  <!-- api 9d021c8：`createApiContentSchedulerRuntime` + 组装根构造 + 与业务入口同起心跳。
       **实测更正**：本进程的组装根在更早一步就要求部署目标必须是 dev/ol（`deploymentTarget()` 解析失败
       即整个进程起不来），所以「缺失 → 不启动该定时器但接口其余能力照常」这条在生产路径上**到不了**
       —— 现实是更严的那一侧（整个进程拒绝启动）。该分支仍显式保留并有喂 null 的用例，理由是
       「只能靠相信它还在的闸等于没有闸」。规格里那条场景已随本注释更正口径。 -->
- [x] 2.2 把接口侧现成的依赖接上：排期配置现读、小时格占位、账号平台、
  已发数与在途自主草稿数、周历掩码。这些都在本仓，不走跨进程。
  <!-- api 9d021c8：排期配置现读 / 小时格占位 / 已发数 / 在途自主草稿数 / 周历掩码 / 联系评论尝试台账
       / FB 运营模式全部进程内直读。**另补两样本进程此前根本没构造的**：FB 自动加群配置存储
       （加群三道闸全读它，不注入即整个加群动作静默跳过），以及素材可用数那一跳（见 3.2 注）。 -->
- [x] 2.3 结构断言：**自动化侧的启动装配 MUST NOT 构造这个调度器**（同一后台任务绝不两处各跑一份）。
  按「正向委托」写，别按「没有同名局部定义」写——后者改个名就绕过去了。
  <!-- automation 0203985：按「正向委托」写——本进程对内容排期的参与形态**只有**注册被调面一种，
       且排期器类不在本仓归属表里（归属改成 automation 的那一刻下次派生就会把类送进来，这条当场红）。
       真正拦住重复触发的仍是小时格数据库原子占位（唯一键=账号+动作），本条只把归属决定钉在明面上。 -->
- [x] 2.4 用例：部署目标合法 → 调度器真在跑；缺失 → 不启动但接口其余能力照常；
  依赖暂不可达 → 仍启动（依赖不可达是运行期事实，不是启动闸）。

  <!-- api 9d021c8 test/api-content-scheduling.test.ts：合法 → 真构造得出并能起心跳；
       缺失 → 不构造 + 具名 fail-closed 痕迹 + 回程响亮抛错；依赖不可达 → 仍构造并启动，
       心跳按失败方向整轮跳过。 -->
## 3. 跨进程依赖：先确认对面有路由，再接客户端

- [x] 3.1 **先去自动化侧的启动入口逐条确认路由是否已注册**，列一张「已有 / 待加」表。
  本项目在这件事上连撞五次：客户端建得出来、调用编译得过、两仓测试各自全绿，
  只有真跑两个进程才 404。**这一步在写任何客户端之前做。**
  <!-- 2026-08-03 实测（automation@9371d59）：启动装配只注册 6 族路由
       （3 个成对指令在组装根 + 调度启停 / 委托任务 / 委托自由文本在 main），本 change 需要的
       **一条都没有**。逐条结论：
       · 在线账号清单 / 三个「在不在跑」/ 委托占用 / 已评论数 / 已加群数 / 三类扳机 —— 待加（全新）；
       · 风控状态与 join 日上限 —— `risk-read-http.ts` 契约在（RISK_READ_ROUTES.getState /
         effectiveQuotas），但**本进程从未注册过它**，属「契约在 ≠ 已接线」；
       · 素材可用数 —— 属主在 content，`registerFacebookPublishMediaAuthorityRoutes` 在
         content/src/server.ts:1048 **无条件注册**，但那个端口面刻意只开三个写口、`availableCount` 不在其中。
       ⇒ 落法：新建一族 `content-scheduling` 窄口（12 条，automation 服务）把前两类一次收口
       （不复用 risk-read：那族只有状态和配额，收不下扳机与单飞判定），另在既有 media 端口面补一条
       `availableCount`。**不接这一跳的代价不是报错**：接口进程只能不注入它 → 每格按 0 处置 →
       日志说「素材不足」而事实是根本没问，两件事的处置完全相反。 -->
- [x] 3.2 自动化仓：补齐缺的被调路由——在线账号清单、风控状态、三类触发、
  三个「是否在跑」、委托任务占用。
  <!-- automation 5f08536 + cloud e82f1fe：`content-scheduling` 12 条全部注册；
       content 侧 `availableCount` 随既有 media 端口面补齐（content 仓零改动——它的存储早就有这个方法，
       main 直接把存储当端口传）。 -->
- [x] 3.3 自动化仓：加一条**只许下降的路由清单闸**（内容侧已有同形先例可抄）：
  共享契约导出的每条应被服务的路由 MUST 在清单里，状态与启动装配逐条一致。
  **漏登记要能让闸自己红**——漏登记是静默的，比漏注册更危险。
  <!-- automation 5f08536 test/acceptance/served-route-inventory.test.ts：两个方向都锁，
       且**都做过变异实测**——删掉注册调用 → 第一条红；加一条未登记的注册 → 第二条红。 -->
- [x] 3.4 接口仓：建跨进程客户端。**MUST NOT 加任何投机性强转**——
  上一个 change 实测 29 处强转删掉后编译器点名 5 处真接错了，24:5 这个比例本身就是判据。
  收尾 grep 强转应为 0。
  <!-- api 9d021c8：`ContentSchedulingHttpClient` + `FacebookPublishMediaAuthorityHttpClient`。
       收尾 grep `as never` / `as unknown as` = 0。编译器确实点出了两处真接错（审批模式取值我凭印象写成
       `'manual_review'`，实际是 `'review'`；FB 加群配置存储缺 schemaEnsurer），都不是靠 review 发现的。 -->
- [x] 3.5 放宽几个同步签名为「同步或异步」（在线清单、三个是否在跑）。
  照风控状态与委托占用**已有**的那个形态办，不发明第二种写法。

  <!-- cloud b3450ec：在线清单 + 三个「在不在跑」放宽为同步或异步，照风控状态与委托占用已有的形态办。
       **顺带治了一个必然的坑**：可选单飞判定原先写作 `if (this.deps.isJoinBusy?.(id))`，
       改异步后 promise 恒真 ⇒ 会静默跳过每一个槽。已全部改成显式 await。 -->
## 4. 失败方向：逐条定死并做变异实测

- [x] 4.1 按规格逐条落实失败方向：在线清单问不到 → 整个心跳跳过；风控状态问不到 → 跳过该账号；
  三个是否在跑问不到 → 判为在跑；委托占用问不到 → 判为占用；素材数问不到 → 判为 0。
  <!-- cloud b3450ec：五条方向逐条落定，见下方变异实测。 -->
- [x] 4.2 把「未注入」与「问不到」在代码与日志上分开：前者是既定零回归跳过，
  后者是具名失败。两者今天在外部同形，正是本项目反复点名的静默假成功形态。
  <!-- cloud b3450ec：未注入在**启动时报一次**（`uninjectedActions()` 随「已启动」那行输出），
       之后永远沉默；问不到每次都具名留痕。每 tick 都报未注入 = 每分钟一行噪声，会把具名失败淹掉。 -->
- [x] 4.3 每条方向各配一条**喂失败输入**的用例。
  <!-- cloud b3450ec test/content-scheduler.test.ts 新增 7 条（6 条方向 + 1 条「未注入/问不到分得开」）。 -->
- [x] 4.4 **变异实测，逐条给出是哪条用例抓住的**：把每条方向改反（如在线清单失败回空数组、
  是否在跑失败回 false），确认当场有用例红。**没红的那条说明用例是空的**——
  上一个 change 实测过一次「桩把陈旧态置了空，而真实现会保留上一份，于是那条用例什么都没测」。

  <!-- 四次变异，逐条对应（还原一律用 cp 不用 mv）：
       ① 在线清单失败改回空数组 → 只红「在线清单问不到 → 整轮跳过」；
       ② `askBusy` 的 unavailable 改回 idle → 红三条（发布是否在跑 / 评论是否在跑 / 委托任务占用），
          三条各自独立，说明三个调用点都被真的钉住；
       ③ 风控失败改回落 'normal' → 只红「风控状态问不到」；
       ④ 素材数失败改回静默 catch(()=>0) → 只红「发帖素材可用数问不到」。
       残留 MUTATION 计数 0。 -->
## 5. 触发语义改成「受不受理」

- [x] 5.1 三类触发的跨进程回执改成受理即返回，区分「已受理」与「未受理 + 具名原因」。
  <!-- kernel `ScheduledTriggerAcceptance`（accepted + 具名 reason + retryable + 人话三件套）。
       **三条扳机的等待形态各不相同，照抄会挂住请求**：发帖 `triggerScheduled` 一路跑到生成结束、
       加群 `triggerScheduled` await 完整加群过程 ⇒ 两者 fire-and-forget；评论 `triggerManual`
       起了异步任务就回执 ⇒ 可以直接 await 并把回执逐字段带回。 -->
- [x] 5.2 终态结果卡**移到自动化侧**（结局在它那儿）；调度器只对「未受理」回卡。
  用例钉住：一次已受理的触发 MUST NOT 产生两张终态卡。
  <!-- automation 5f08536 发终态卡（幂等键带小时格，重复投递不刷第二张）；
       api 9d021c8 只对「未受理」回卡，且用例钉住「已受理 → 本侧零卡」。 -->
- [x] 5.3 单飞标志改成受理即释放；用例钉住账号粒度单飞的真态以向自动化现问为准，
  不以调度器内存标志为唯一判据。

  <!-- 受理即返回 ⇒ 调度器的 `inFlight` 在回执到达时就释放（`fire()` 的 finally 不变）。
       账号粒度单飞的真态以向自动化现问的三个「在不在跑」为准——它们现在是跨进程读，
       且问不到时判为在跑（4.1），所以内存标志从来不是唯一判据。 -->
## 6. 回程路由落地

- [x] 6.1 接口仓注册回程路由，落点与本进程调度器**共用同一个方法**。
  <!-- api 9d021c8：`registerScheduleFeedbackRoutes` 落点 = `scheduler.reportNotStarted`，
       与本地路径同一个方法。 -->
- [x] 6.2 缺调度器时**响亮抛具名错误**，MUST NOT 回假值。照单体那个转发器形态办。
  <!-- api 9d021c8：抛 `content_scheduler_not_running_in_this_process`，照单体那个转发器形态。 -->
- [x] 6.3 结构断言：小时格账本只有一条写入路径（回程与本地调用是同一个方法）。
  <!-- api 9d021c8 用例「回程与本地路径共用同一个方法」：给 `scheduler.reportNotStarted` 套一层记录，
       回程调用后它确实被调到——证明的是**同一个方法**，不是「两处行为看起来一样」。 -->
- [x] 6.4 用例：回程真的归还小时格并可在本小时内有界重试；无调度器的进程上调用 → 具名失败。

  <!-- 归还语义本身由 cloud 既有用例覆盖（未开始 → 归还 → 本小时非偏移分钟仍可重试）；
       本 change 补的是「回程接到那个方法」与「无调度器时具名失败」两条。 -->
## 7. 单体零回归

- [x] 7.1 单体形态下调度器仍由原路径构造，行为逐位不变（依赖仍是直调实现）。
  <!-- cloud src/server.ts **一字未改**：签名放宽是向后兼容的（同步闭包照样满足「同步或异步」），
       单体那套直调实现原样注入。 -->
- [x] 7.2 单体全量测试全绿；**红项不得写成绿色**，逐条说明是既有还是本 change 新增。

  <!-- cloud e82f1fe：4145 pass / 0 fail / 11 skip（基线 4133 → 新增 12 条：7 条失败方向 + 5 条传输）；
       acceptance 186/186。无红项。 -->
## 8. 派生对账与验收

- [x] 8.1 事实源仓改完 → 机械派生到接口 / 自动化两仓 → **逐仓 typecheck**。
  对账只比文件，它对「这个仓还编不编得过」一无所知。
  <!-- 派生到 6 个目标全部 typecheck 通过。**对账只比文件这句话当场被坐实两次**：
       automation 的 `test/acceptance/protocol-contract.test.ts` 手抄的消息类型穷举缺 `browser.status`
       （typecheck 红）、消息总数断言停在 94（测试红）；两处都因为 test/ 不参与派生而静默陈旧。
       api 的 `test/content-scheduler.test.ts` 同理，是个纯陈旧子集，已整份取事实源那一份。
       另有一件对账报得出、派生管不着的：**迁移 0109 不在 api 仓**（脚本一个字都不碰 migrations/），
       已手工补入。 -->
- [x] 8.2 六仓各自 typecheck + 全量测试；边界门禁跨域边 / 跨层读写 / 豁免保持 0。
  <!-- cloud 4145/0；api 522/0；automation 2175/0/3skip；content 444/0（零改动，只是验它没被 kernel 新方法带崩）；
       kernel 70/0；transport 36/0。边界门禁：cloud `boundaries:refresh` 跨层边 0、豁免 0；
       automation 的 boundary census 4/4（新增两个文件已登记归属，**手工文本插入、不整体重序列化**）。 -->
- [x] 8.3 按 0.2 那份落点清单逐条对，确认环境绑定三处都改了、部署目标那条没被误删。
  <!-- 逐条实测（cloud e82f1fe）：① 调度器里的环境跳过与 `VerifiedOnlineAccountIdentity` 命中数 0；
       ②③ 下发侧只剩一行 info 留痕（`publish-dispatcher.ts:717-721`），不参与判定；
       ④ 建表 `env_key TEXT`（无 NOT NULL）、入参不再强制非空；⑤ kernel 归属类型 `envKey: string | null`。
       **部署目标那条没被误删**：`publish-dispatcher.ts:359` 原样在，且 1.6 的变异实测证明它承重。 -->
- [x] 8.4 **分层验收如实分开记**：契约测试证明路由与客户端形状；单体部署只证明现网零回归；
  **三进程真跑本 change 不声称**。
  <!-- 分三层如实记：
       · **契约层已证**：路由与客户端形状往返（cloud 5 条传输用例）、注册集合逐字等于路由表、
         漏注册保留 `route_not_found` 而不被压成 `api_authority_unavailable`、target 不符必拒。
       · **单体层已证**：现网零回归（src/server.ts 一字未改，全量 4145/0）。
       · **三进程真跑本 change 不声称**：六仓测试各自全绿**不等于**两个进程一起跑得通；
         那属拆仓批次 5。真机项见 8.6。 -->
- [x] 8.5 dev 部署按安全序列（先备份 → rsync → restart → healthcheck → 失败即回滚）；
  绝不碰同机 isales。
  **本批含一条迁移（1.4a），所以顺序是硬的**：rsync → `migrate status`（确认待应用条数、
  不盲目 apply）→ `migrate up` → 重启 → healthcheck。倒过来做的后果实测过：
  schema 门在 enforce 下抛出 → 进程 exit 1 → 每 5 秒静默重启的崩溃循环、零告警。
  补迁移**只能用 `migrate up`**，另一个执行器不写账本，用它补完「表明明建好了却照样判落后」。
  <!-- 2026-08-03 已部署 dev（cloud `e82f1fe`）。顺序照办、每步都有回执：
       备份 `/opt/aidcp/cloud.bak.20260803-132923.tar.gz` + `.env.bak.20260803`
       → rsync（--exclude .env/node_modules/.git）
       → `migrate status`：**待应用恰好 1 条**且正是 `0109_content_schedule_hour_claim_env_key_optional`（kind=expand），
         content/automation 两个属主库各自 0 待应用（未盲目 apply，先看清才动）
       → `migrate up`：applied 0109（3ms）
       → restart → healthcheck：`active (running)`、8787 在听、飞书长连接已建、
         启动日志「ContentScheduler 已启动（每分钟心跳、按账号错峰）」在场，近 4 分钟 error 计数 **0**。
       同机 isales 三个服务（api / engine / scheduler）**全部照常 running，一根手指没碰**。
       **部署的是单体**：本批对现网的实际影响只有「去掉自动发帖的浏览器环境绑定」+ 迁移 0109；
       三进程形态的接线不参与本次运行（那属拆仓批次 5）。 -->
- [x] 8.6 本地桩验不了的登记 `docs/real-machine-acceptance-backlog.md`（并入簇 60）：
  排期真到点触发一次、换环境后稿件真被投出、回程真归还小时格。
  <!-- 已并入 `docs/real-machine-acceptance-backlog.md` 簇 60，共 5 条：排期真到点触发一次 /
       换环境后稿件真被投出（含「dev-ol 隔离没被顺带削弱」）/ 回程真归还小时格 /
       两个进程一起跑确认这族路由真通 / FB 素材可用数真跨得过去。 -->
- [x] 8.7 更正 `docs/cloud-service-decomposition-proposal.md` §4.6.5 第 12 项那条
  「MUST 改为按排期表扫描、MUST NOT 以 Edge 在线为前置」——**与用户 2026-08-03 裁定相左**，
  按裁定改成「保留在线闸、去掉环境绑定」，写明日期与理由，不留两份互斥的权威。
  <!-- 已改。原文「MUST 改为按排期表扫描、MUST NOT 以 Edge 在线为前置」与用户 2026-08-03 裁定相左，
       现改成「在线闸保留、经跨进程窄口问同一份清单；去掉的是浏览器环境绑定」，并写明裁定日期、
       同时把「问不到 MUST 整轮跳过、MUST NOT 回空清单」一并写进该条，不留两份互斥的权威。 -->
- [x] 8.8 `openspec validate wire-content-scheduler-into-api-process --strict` 通过后归档；
  **归档前把仍未了的债搬进 backlog**——归档会把本文件埋进 archive 目录，
  只活在任务注释里的东西从此没有任何机制会提醒人。
  <!-- validate --strict 通过。**归档前的债已全部搬走**：真机项 5 条进 backlog 簇 60（8.6）、
       文档互斥已消（8.7）。另有两条只在本文件注释里、值得在归档前点名的**性质**，
       它们不是待办而是判据，写进 backlog 那 5 条的措辞里了：
       ① 六仓测试各自全绿**不等于**两个进程一起跑得通——这正是本 change 反复点名的事故现场；
       ② 派生对账只比 src/：test/ 与 migrations/ 不参与，前者会静默陈旧（本批实测中了两次）、
          后者要手工补（0109 差点漏进 api 仓）。 -->
