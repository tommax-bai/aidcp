## 1. aidcp-automation — 两个兜底桶的原因拆分（可独立先上线）

- [x] 1.1 坐实「账号尚未就绪」的全部来源（会话启动裁决的穷举取值：人设未绑 / 人设副本陈旧 / 其他配置副本陈旧 / 平台不具备浏览能力 / 调度未激活＝运营显式全局停机；外加续场配置提供者缺席＝特性未开），确认它们今天确实合并成同一个原因字符串下发 <!-- aidcp-automation dc75643 -->
- [x] 1.2 坐实「需要浏览器才能解除」的来源：阻断浮层暂停态（确认验证码与未识别弹窗两种载荷都落到同一个暂停集合），以及「风控拦住且原因不以配额开头」这个取反补集里实际会出现的取值 <!-- aidcp-automation dc75643 取反补集对 view 实际不可达，仍具名以防将来落入未知原因 -->
- [x] 1.3 把两个桶都拆成互相可分辨的具名取值，**只扩展取值、不改消息结构**；全局性来源（显式停机 / 特性未开）与会自愈的来源（副本陈旧）必须与账号级来源明确区分 <!-- aidcp-automation dc75643 -->
- [x] 1.4 待机提示构造处按新取值透传原因，确认既有「不让位」判定与 `eligible` 取值一字不变（本步行为零变化） <!-- aidcp-automation dc75643 -->
- [x] 1.5 补单测：每个来源各自产出可分辨的具名原因；断言互不相等，且都仍然 `eligible=false` <!-- aidcp-automation dc75643 -->
- [x] 1.6 补**穷举断言**：会话启动裁决的取值集合若新增成员而未在原因映射里具名，测试失败——复用既有那张穷举分类表的做法，防止新原因默默折进已有名字 <!-- aidcp-automation dc75643 穷举 Record 编译期兜底 + 运行时断言取值互不相同（编译器只保证有值） -->

## 2. aidcp-edge — 滞留计时（纯判定函数）

- [x] 2.1 在冷待机纯判定模块新增滞留计时的记账函数：输入当前原因、上一次记账、当前时刻，输出 `{ reason, since }`；**换原因即归零**（与既有连续拒绝记账同口径，复用而非另写一套时间语义） <!-- aidcp-edge abb9584 -->
- [x] 2.2 新增**白名单**：明确该终止的具名原因（初始两项——阻断浮层暂停、人设未绑）。**清单外的原因一律不计时**；MUST NOT 写成黑名单 <!-- aidcp-edge abb9584 白名单初始两项：阻断浮层暂停、人设未绑 -->
- [x] 2.3 新增终止判定函数：在计时之上叠加四条护栏（原因在清单内 / 该环境此刻真的占着一格执行槽 / 自动化意图为已启用 / 与云端处于连接态），四条同时成立且时长达门槛才回「该终止」 <!-- aidcp-edge abb9584 -->
- [x] 2.4 「占着一格执行槽」必须复用槽位记账的同一判据，MUST NOT 另写一份——两份判据漂移的现形方式是关掉一个本来就不占槽的环境 <!-- aidcp-edge abb9584 判据抽成 handleOccupiesBrowserSlot，四处既有源码扫描断言同步改指新落点 -->
- [x] 2.5 补单测：四条护栏逐条单独证伪（每条各有一个用例：只差这一条时不终止） <!-- aidcp-edge abb9584 -->
- [x] 2.6 补单测：未具名原因持续超门槛 **不** 终止（保守方向的钉子） <!-- aidcp-edge abb9584 -->
- [x] 2.7 补单测：断连后重连的第一跳不得立刻终止，须从重连后重新计满门槛 <!-- aidcp-edge abb9584 -->

## 3. aidcp-edge — 终止处置与槽位释放

- [x] 3.1 在待机提示应用路径上接入滞留判定：判定「该终止」时调用**运营手动关闭的同一条路径**，不新造终止路径 <!-- aidcp-edge abb9584 -->
- [x] 3.2 传入一个**独立的系统终止归因**，MUST NOT 复用运营手动关闭的归因；界面与日志据它区分两者 <!-- aidcp-edge abb9584 engineStopReason 保持 user_close（多处硬匹配，换值会让关闭流程分支走不到）；归因另记 stalledEviction -->
- [x] 3.3 确认终止后该环境不进等槽位队列、不被自动重启路径捡起（逐条核对既有的重启 / 续启 / 全部启动入口） <!-- aidcp-edge abb9584 -->
- [x] 3.4 确认槽位在终止后立即归还并唤醒等待队列的队头 <!-- aidcp-edge abb9584 -->
- [x] 3.5 补单测：终止后自动化意图为已关闭、启动队列与等槽位队列均无该环境 <!-- aidcp-edge abb9584 -->
- [x] 3.6 补回归断言：系统终止归因与运营关闭归因是两个不同取值（防止将来被合并） <!-- aidcp-edge abb9584 -->

## 4. aidcp-edge — 界面持久卡片

- [x] 4.1 终止时写入持久卡片：账号、**具体**阻塞原因（验证码类 / 登录 / 人设未绑 / 调度未开 分别可读）、滞留起始时刻、槽位已释放 <!-- aidcp-edge abb9584 -->
- [x] 4.2 复用既有的持久呈现机制（与「浏览器关闭状态未确认」那张卡片同源），确认不被后续状态更新或日志刷新冲掉 <!-- aidcp-edge abb9584 撕卡收口在 updateStatus 的 edge==="running"，不逐个启动入口清 -->
- [x] 4.3 补单测：卡片在若干次无关状态更新之后仍然在位 <!-- aidcp-edge abb9584 -->

## 5. 配置与回滚

- [x] 5.1 门槛默认 15 分钟，支持环境变量与客户端设置覆盖；门槛下限须显著大于提示到达节拍 <!-- aidcp-edge abb9584 -->
- [x] 5.2 功能开关默认开启，关闭后完全回到本 change 之前的行为 <!-- aidcp-edge abb9584 -->
- [x] 5.3 补单测：开关关闭时，需人介入的阻塞持续超门槛 **不** 终止 <!-- aidcp-edge abb9584 -->

## 6. 回归与验证

- [x] 6.1 `cd ../aidcp-automation && npm run test:acceptance && npm test && npm run typecheck` <!-- aidcp-automation dc75643 acceptance 305 + full 2445 + typecheck 全绿 -->
- [x] 6.2 `cd ../aidcp-edge && npm run test:acceptance && npm test && npm run typecheck` <!-- aidcp-edge abb9584 acceptance 40 + full 3265 + typecheck 全绿 -->
- [x] 6.3 确认既有冷待机让位路径（配额 / 冻结 / 受限 / 排期）的测试**全部未被改动且全绿**——本 change 不许动它们 <!-- aidcp-edge abb9584 既有让位路径用例未改动、全绿 -->
- [x] 6.4 变异验证：把四条护栏各改坏一条，确认对应用例转红（证明护栏被真的测到，而非死代码） <!-- aidcp-edge abb9584 六条变异（四护栏 + 换因归零 + 门槛比较）逐条转红，基线还原干净 -->

## 7. 部署与真机验收

- [x] 7.1 云端先部署 dev（第 1 节可独立上线，行为零变化），观察待机提示原因取值已拆分 <!-- aidcp-automation dc75643 2026-08-10 deployed dev（写者锁已持有、8787/8094 监听、migrate --owner=automation 待应用 0） -->
- [ ] 7.2 边缘改动落地后本地起一次客户端，确认滞留计时与卡片呈现正常 <!-- 未做：需 GUI + AdsPower 真机环境，且客户端未出包（打包属用户显式触发）。归入真机簇 157。 -->
- [x] 7.3 真机验收项登记 `docs/real-machine-acceptance-backlog.md`： <!-- 簇 157 -->
  - 验证码滞留 15 分钟后确实释放槽位，且卡片写明原因；运营重新启动后阻塞仍被呈现
  - 未配续场的部署不发生集中关闭
  - 释放出的槽位确实被等待队列的下一个环境用上
- [x] 7.4 部署前确认 `src/electron/main.cjs` 与活跃 change `browser-slot-scheduling` 的单写者，避免并行冲突 <!-- land 时无冲突：edge worktree rebase 后 4 处源码扫描断言已同步 -->


## 8. 上线状态（据实记录）

- **云端侧已上线 dev**（`aidcp-automation` dc75643，2026-08-10）：两个兜底桶已按具名子原因下发。
  **该步行为零变化**——所有这些原因仍然 `eligible=false`，边缘尚未出包、不认识新取值，按「未具名原因不计时」处理。
- **客户端侧未生效**：滞留计时与终止已合入 `aidcp-edge` master（abb9584），但**未出安装包**
  （打包属用户显式触发的动作，见 CLAUDE.md §6）。运营机上跑的仍是旧客户端，**本 change 的新行为尚未在任何真机上发生**。
  MUST NOT 据「代码已合并 + 云端已部署」认定功能已上线。
- 下一步要让它真正生效：出一次 edge 桌面安装包并装机，然后走簇 157 的真机验收。
