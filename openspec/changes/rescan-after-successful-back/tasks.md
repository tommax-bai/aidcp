# Tasks

## 1. aidcp-cloud — 返回成功后主动续扫

- [ ] 1.1 在动作回执处置段加入：返回成功 → 下发一次续扫滚动，原因名 `rescan_after_back`；与返回失败兜底互斥。
- [ ] 1.2 排除三种情况：会话已结束、浏览暂停期（巡视）、就地读平台。其中「浏览暂停期」由发命令统一出口的既有闸兜住即可，但必须确认它**确实**兜住（暂停期被丢弃的命令现在有日志，可据此核对）。

## 2. 测试

- [ ] 2.1 补用例：返回成功 → 恰好一次续扫、原因名正确、不伴随失败兜底。
- [ ] 2.2 补用例：返回失败仍走原兜底，且不发续扫（守互斥）。
- [ ] 2.3 变异验证：去掉续扫这一支，2.1 必须转红；把原因名写错，2.1 也必须转红（原因名是日志与回执里唯一能区分两条路径的东西）。

## 3. 收口

- [ ] 3.1 `openspec validate rescan-after-successful-back --strict` 通过。
- [ ] 3.2 cloud `npm run test:acceptance` → `npm test` → `npm run typecheck` 全绿；边界门禁无新增违规。
- [ ] 3.3 合回 cloud `master` 并推送；按派生仓对账同步到 `aidcp-automation`，部署 dev（**不碰 ol**）。
- [ ] 3.4 登记真机验收：连续浏览多条笔记，确认每条看完返回之后**立刻**续上下一条，而不是等几分钟被空闲兜底踢一下。
- [ ] 3.5 登记边缘侧缺口（不在本 change 修）：Native 迁移后小红书的返回只回动作回执、不再随附可见卡片，而命令清单声明的回执里**写着** `page.cards`（Facebook 侧仍产出，两平台取并集后对账恰好通过，per-platform 的缺口被并集掩盖）。这是与 `restore-notification-home-return` 同一族的迁移丢报，值得单独收。
