## 1. aidcp-edge — 现状坐实与行为基线

- [ ] 1.1 逐条读清所有会拉起核心子进程的路径及其真实取消条件（常规启动 / 崩溃重起 / 待机唤醒 / 排期任务 / 无浏览器控制面 bootstrap / 受限离场清理 / 临时浏览器通道），列出每条路径「该按什么判据准入」，作为准入闸分档的事实源
- [ ] 1.2 确认 `initializeOwnedCoreChild` 是 `handle.child` 赋值的唯一入口（全仓 grep 佐证），若存在第二处赋值先收口到该入口
- [ ] 1.3 为拆分兜底：补齐锁住 `spawnEdgeChild` 现有分支行为的用例（adspower 常规 / 代理权威注入 / 控制面 bootstrap / 离场清理），确保后续搬动语句顺序时有红可依

## 2. aidcp-edge — 提交点原子化（design D1）

- [ ] 2.1 把 `spawnEdgeChild` 拆成「准备段（可 await，产出纯数据启动方案）」与「提交段（同步函数，不带 async）」两部分，所有 await 前移到准备段
- [ ] 2.2 提交段依次做：取消闸复核（当前操作代次 / 停止意图 / 已移出 / 客户端退出中）→ `spawn` → 所有权登记 → 启动态写入，中间不得有任何 await
- [ ] 2.3 提交段复核不通过时：归还启动排队名额、settle 串行启动队列的就绪等待、不写失败态，返回可区分的「已取消」结果
- [ ] 2.4 用例：准备段进行中插入关闭 → 断言未发生 spawn、名额已归还、队列已放行下一个
- [ ] 2.5 用例：断言提交段是同步函数（非 AsyncFunction），防止日后插入 await 静默重开中间地带

## 3. aidcp-edge — 所有权登记准入闸（design D2）

- [ ] 3.1 `initializeOwnedCoreChild` 新增**必填**准入判据参数，缺失时抛错（与现有 handle/child 参数校验同款）
- [ ] 3.2 准入被拒时：先完成所有权登记与退出观测者安装，再经既有终止路径终止该子进程，使退出走既有收敛（清所有权、归还浏览器执行名额、放行等槽位队列）
- [ ] 3.3 被拒如实写入原始日志（环境、拒收原因），不投影为失败态、不消耗有界重起预算
- [ ] 3.4 在 `spawnEdgeChild` 按启动意图分档提供判据：常规浏览器核心读停止意图 / 移出 / 退出中；无浏览器控制面 bootstrap 与受限离场清理按各自取消条件（依据 1.1）
- [ ] 3.5 用例：已被要求停止的环境送来子进程 → 断言被终止、名额归还、无运行态残留
- [ ] 3.6 用例：环境已移出但离场清理核心仍被放行 → 断言不被通用停止意图误杀

## 4. aidcp-edge — 原始日志按所有权全量落（design D3）

- [ ] 4.1 `handleEdgeOutput` 的操作代次不一致分支改为：逐行落原始日志并标注来自已被取代的代次，然后返回；代次一致时行为逐字不变
- [ ] 4.2 确认不产生重复行（与 `handleEdgeLogLine` 内既有的 `appendEdgeLog` 调用互斥）
- [ ] 4.3 明确不动：人设回执 / 发布审批回执 / 浏览器窗口回执 / 引擎命令诊断 / 状态投影仍保持现有代次门（在代码注释里写清为什么只拆日志这一层）
- [ ] 4.4 用例：代次已推进但子进程仍归本环境所有并输出 → 断言逐行进原始日志、界面运行态未被改写

## 5. aidcp-edge — 就绪超时如实呈现（design D5）

- [ ] 5.1 就绪预算超时（默认 180s）除既有控制台告警外，同时写入该环境状态：已超预算 / 队列已放行下一个 / 本环境仍在继续启动
- [ ] 5.2 断言该呈现不写失败态、不写终态、不置放弃标记
- [ ] 5.3 用例：超时触发后断言状态文案与非失败语义

## 6. aidcp-edge — 回归与验收

- [ ] 6.1 `npm run test:acceptance` 全过（安全红线 `AC-PROTO-*` / `AC-PUB-*` / `AC-RISK-*` 不得回归）
- [ ] 6.2 `npm test` 全量通过
- [ ] 6.3 `npm run typecheck` 通过
- [ ] 6.4 变异验证：把提交段的取消复核改成恒真（闸失效）→ 断言 2.4 的用例当场变红；把准入闸判据参数改成可选默认放行 → 断言 3.5 当场变红

## 7. 收尾

- [ ] 7.1 集成回主干（`scripts/land-change aidcp-edge cancel-in-flight-environment-launch`），推送
- [ ] 7.2 本仓 tasks.md 按 `<!-- <repo> <commit-sha> 备注 -->` 回写落地 sha（sha 必须取自已推送提交）
- [ ] 7.3 真机验收项登记到 `docs/real-machine-acceptance-backlog.md`：多环境车队下「启动中途关闭」不再产生无人认领的核心、不再滞留「关闭中」、名额如实归还（需重新出安装包后验证，出包按 CLAUDE.md §6 由用户显式触发）
- [ ] 7.4 `openspec validate cancel-in-flight-environment-launch --strict` 通过
