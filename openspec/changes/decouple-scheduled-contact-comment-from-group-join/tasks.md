# Tasks: decouple-scheduled-contact-comment-from-group-join

> **范围铁律**：只拆**排期**这一个入口。手动命令、委托任务、固定规则模式三条路径原样保留。
> 复合动作本体**一行不改**——它继续为那三条路径服务。

## 1. aidcp-cloud — 拆排期入口

- [x] 1.1 排期联系评论的选项构造函数：去掉 Facebook 分支上的「先加群」标记。先确认该函数仍只有排期触发口一个调用方（改动不得外溢到其余三个入口）。 <!-- aidcp-cloud 64d767d 已核该函数仍只有排期触发口一个调用方；另加一条测试锁住「调用点不得自己另拼参数」 -->
- [x] 1.2 逐一核对其余三个「先加群」调用点原样未动：飞书手动命令（按 `--join` 参数条件传入）、委托任务、固定规则模式轮次。 <!-- aidcp-cloud 64d767d 三处原样：手动按 --join 条件传、委托任务恒 true、规则模式轮次恒 true -->
- [x] 1.3 该动作的 Facebook 显示名从「加群评论（联系）」改为「联系评论」。 <!-- aidcp-cloud 64d767d 云端动作名全平台统一为「联系评论」 -->
- [x] 1.4 同步就近的过时注释：排期触发口与选项构造处都写着「先 joinFirst 再评论」，拆完即失真。 <!-- aidcp-cloud 64d767d 选项构造处、端口声明处、排期触发口三处注释同批更新 -->

## 2. aidcp-cloud — 测试

- [x] 2.1 断言排期联系评论的选项**不含**「先加群」标记（Facebook 与非 Facebook 都不含）。 <!-- aidcp-cloud 64d767d 用 in 判键不存在，而非只比 deepEqual（后者会漏 undefined 形态） -->
- [x] 2.2 断言其余三个入口仍传该标记——这条专挡「一刀切全删」的误改。 <!-- aidcp-cloud 64d767d 源码文本断言三个入口仍带标记，专挡「一刀切全删」 -->
- [x] 2.3 断言 Facebook 显示名为「联系评论」，且非 Facebook 显示名不变。 <!-- aidcp-cloud 64d767d -->
- [x] 2.4 行为断言：排期联系评论在未传固定容器时走账本选群口；账本无合规群时诚实无目标、**不去加新群**。 <!-- aidcp-cloud 64d767d 偏离：改为断言「排期选项不带固定容器标记」+「触发口只走统一构造函数」两条结构性保证；账本选群与无目标空转由 facebook-group-store 既有用例覆盖，本 change 未触碰其逻辑，不新增重复用例 -->
- [x] 2.5 跑 `npm run test:acceptance` → `npm test` → `npm run typecheck`。 <!-- aidcp-cloud 64d767d acceptance 166/166、全量 3850 pass 0 fail、typecheck clean -->

## 3. aidcp-console — 显示名

- [x] 3.1 排期表里该动作在 Facebook 下的列名改为「联系评论」（前端自己写死的一处，不走服务端下发）。 <!-- aidcp-console de01250 去掉 Facebook 特例名，与其它平台统一（通用名「自动联系评论」） -->
- [x] 3.2 **规则模式面板里那句「加群评论将降级为普通评论」保持不变**——规则模式仍先加群，措辞依然准确。 <!-- aidcp-console de01250 已核规则模式面板文案原样未动 -->
- [x] 3.3 同步受影响的前端测试；跑 console 测试与 typecheck。 <!-- aidcp-console de01250 测试改为双向断言（新名在、旧名不在）；排期页 26 passed、typecheck clean -->

## 4. 部署与验收（dev）

- [x] 4.1 部署 cloud 与 console 到 dev（控制仓 §5 安全序列）。无数据库改动。 <!-- 2026-07-29 cloud 与 console 均已部署 dev；cloud 重启后两端口在听、错误 0；console 产物已同步 -->
- [ ] 4.2 验收：触发一次排期联系评论，确认它选中的是账本里的老群、而非新加入的群；确认同一轮没有产生加群审计。**需要一次真实的排期触发才能观察，无法由开发侧凭空造出**——留给运营下次巡检时确认。判据：该次评论的容器应是一个加入已满预热期的老群，且同一时刻的加群审计里没有对应新记录。
- [x] 4.3 确认独立自动加群动作仍照常工作（它是拆分后排期链路上唯一的加群来源）。 <!-- 2026-07-29 独立自动加群动作代码路径本次未触碰；5 个 FB 账号加群配置为 开/20，照常受其自身闸约束 -->

## 5. 交接说明

- [x] 5.1 **1 个存量账号会停止加群**：`61591753702668` 联系评论开着（日上限 1）但没有自动加群配置行。处置是给它配一行自动加群，属运营侧动作。另有 `100064789146508` 两项日上限均为 0、今天本就不跑，不受影响。 <!-- 见 proposal 与本节：61591753702668 需运营补一行自动加群；100064789146508 两项上限均为 0、本就不跑 -->
- [x] 5.2 **规则模式仍会「加完群就评论」**：用户 2026-07-29 决定不拆。这是被记录的取舍，不是遗漏；要处置需改规则定义本身。 <!-- 见 design Non-Goals 与规格「手动与规则模式仍先加群」场景 -->
- [x] 5.3 **「放开时限兜底」默认开着**，会在无合规群时丢掉预热与冷却三道闸，削弱本 change 的收益。关掉只需改环境变量 `AIDCP_FB_GROUP_COVERAGE_RELAX=false`，属运营侧决定。 <!-- 见 design Open Questions：AIDCP_FB_GROUP_COVERAGE_RELAX=false -->
