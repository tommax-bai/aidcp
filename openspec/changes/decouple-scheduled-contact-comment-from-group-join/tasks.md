# Tasks: decouple-scheduled-contact-comment-from-group-join

> **范围铁律**：只拆**排期**这一个入口。手动命令、委托任务、固定规则模式三条路径原样保留。
> 复合动作本体**一行不改**——它继续为那三条路径服务。

## 1. aidcp-cloud — 拆排期入口

- [ ] 1.1 排期联系评论的选项构造函数：去掉 Facebook 分支上的「先加群」标记。先确认该函数仍只有排期触发口一个调用方（改动不得外溢到其余三个入口）。
- [ ] 1.2 逐一核对其余三个「先加群」调用点原样未动：飞书手动命令（按 `--join` 参数条件传入）、委托任务、固定规则模式轮次。
- [ ] 1.3 该动作的 Facebook 显示名从「加群评论（联系）」改为「联系评论」。
- [ ] 1.4 同步就近的过时注释：排期触发口与选项构造处都写着「先 joinFirst 再评论」，拆完即失真。

## 2. aidcp-cloud — 测试

- [ ] 2.1 断言排期联系评论的选项**不含**「先加群」标记（Facebook 与非 Facebook 都不含）。
- [ ] 2.2 断言其余三个入口仍传该标记——这条专挡「一刀切全删」的误改。
- [ ] 2.3 断言 Facebook 显示名为「联系评论」，且非 Facebook 显示名不变。
- [ ] 2.4 行为断言：排期联系评论在未传固定容器时走账本选群口；账本无合规群时诚实无目标、**不去加新群**。
- [ ] 2.5 跑 `npm run test:acceptance` → `npm test` → `npm run typecheck`。

## 3. aidcp-console — 显示名

- [ ] 3.1 排期表里该动作在 Facebook 下的列名改为「联系评论」（前端自己写死的一处，不走服务端下发）。
- [ ] 3.2 **规则模式面板里那句「加群评论将降级为普通评论」保持不变**——规则模式仍先加群，措辞依然准确。
- [ ] 3.3 同步受影响的前端测试；跑 console 测试与 typecheck。

## 4. 部署与验收（dev）

- [ ] 4.1 部署 cloud 与 console 到 dev（控制仓 §5 安全序列）。无数据库改动。
- [ ] 4.2 验收：触发一次排期联系评论，确认它选中的是账本里的老群、而非新加入的群；确认同一轮没有产生加群审计。
- [ ] 4.3 确认独立自动加群动作仍照常工作（它是拆分后排期链路上唯一的加群来源）。

## 5. 交接说明

- [ ] 5.1 **1 个存量账号会停止加群**：`61591753702668` 联系评论开着（日上限 1）但没有自动加群配置行。处置是给它配一行自动加群，属运营侧动作。另有 `100064789146508` 两项日上限均为 0、今天本就不跑，不受影响。
- [ ] 5.2 **规则模式仍会「加完群就评论」**：用户 2026-07-29 决定不拆。这是被记录的取舍，不是遗漏；要处置需改规则定义本身。
- [ ] 5.3 **「放开时限兜底」默认开着**，会在无合规群时丢掉预热与冷却三道闸，削弱本 change 的收益。关掉只需改环境变量 `AIDCP_FB_GROUP_COVERAGE_RELAX=false`，属运营侧决定。
