# Tasks

> **单仓 `aidcp-edge`，零协议改动、零云端改动。**
> **开工前置**：本 change 与活跃 change `browser-slot-scheduling` 共享 Electron 外壳的状态推导块，
> **须串行、不与其并行开工**（CLAUDE.md §7 热点文件单写者）。开工前确认该 change 无人在改同一段。

## 0. 开工前取证复核（登记比代码旧，先验缺陷还在）

- [ ] 0.1 确认阻断浮层标记的清除条件里**仍然**含「浏览会话结束」这一支；若已被他人修掉，停手并把本 change 改为只做第 2 组。
- [ ] 0.2 确认现役 Native 引擎**两个方向都在发**结构化阻断事件（检出与复检解除各一处），且解除那处与「通知云端阻断已解除」在同一函数内先后发出。这是「摘掉后不会永久卡住」与「两侧同源」的全部依据。
- [ ] 0.3 记录各在产平台的陪伴 UI 事件策略门实际取值（design R1）：哪些平台会真发这两个事件。**只登记、不在本 change 扩大覆盖**，结论写进本文件备注。
- [ ] 0.4 确认外壳今天**确实不解析**引擎的结构化会话行（全仓零命中），且引擎侧发射点仍在。

## 1. 阻断浮层：只认显式解除

- [ ] 1.1 从阻断浮层标记的清除条件中移除「浏览会话结束」一支；显式解除事件保持为唯一清除路径。**MUST NOT** 改为「附加复检条件后仍允许清除」（design D1 已否决该替代方案）。
- [ ] 1.2 复核该标记的其余写入点：环境启停等重置路径写「假」是正确的（新会话开始时确实无已知阻断），**保留**；逐一确认没有第二条「运行态迁移顺带清除」的旁路残留。
- [ ] 1.3 复核该标记的下游消费方（含冷待机准入的阻断否决、环境栏「需要处理」排序与计数），确认语义收紧后无消费方依赖旧的宽松清除行为。

## 2. 会话轴：改由结构化会话事件驱动

- [ ] 2.1 外壳解析器新增对引擎结构化会话行的处理，产出会话开始 / 结束两类事件。匹配 **MUST** 落在引擎侧 token 化字段上，**MUST NOT** 使用自然语言措辞或宽松正则（design D2/R3）。
- [ ] 2.2 会话运行态投影改由 2.1 的事件驱动，取代已退役路径的中文日志推断；清理该轴上已无写入方的死规则。
- [ ] 2.3 **仅改界面显示**：确认该轴未被接回冷待机准入闸，也未被接入任何新的裁决路径。
- [ ] 2.4 确认与既有 posture 覆盖层的先后关系不变（覆盖层仍最后生效且赢），本改动 **MUST NOT** 改变该顺序语义。

## 3. 回归

- [ ] 3.1 **定向回归（本 change 主断言）**：环境处于阻断态 → 引擎发出会话结束事件、且**未**发出解除事件 → 阻断态仍在，且冷待机准入仍判该环境不可让位。
- [ ] 3.2 反例配对：环境处于阻断态 → 引擎发出**显式解除**事件 → 阻断态清除。证明 3.1 不是把闸焊死。
- [ ] 3.3 会话轴定向回归：只喂结构化会话行（不含任何退役路径的中文措辞）→ 会话运行态投影正确变化。
- [ ] 3.4 **写入方存在断言**（沿用 `admit-browser-standby-on-live-facts` D2 做法）：从外壳解析器抽出会话轴所用的全部匹配串，逐个到引擎源码中找到发射点。**MUST NOT 省略**——这条断言正是那次事故的机械化教训。
- [ ] 3.5 **结构断言**：冷待机准入的输入集合中不含会话运行态轴（对应新增规格场景「会话轴接上活写入方之后仍不得进入准入闸」）。
- [ ] 3.6 变异自查：把「浏览会话结束」这一支加回清除条件，确认 3.1 真的会红；把会话轴加回准入输入，确认 3.5 真的会红。**两条都要做**（防恒真断言）。
- [ ] 3.7 `cd ../aidcp-edge && npm run test:acceptance && npm test && npm run typecheck` 全绿。已知既有 flaky：原生引擎会话恢复那条契约测试偶发管道错，**在未含本改动的默认分支上即可复现**，重跑即可，**不要去改自己的代码找原因**。

## 4. 控制仓收尾

- [ ] 4.1 本文件按 sub-repo 回写 commit-sha，格式 `<!-- <repo> <commit-sha> 备注 -->`；sha **必须取自已推送的提交**。
- [ ] 4.2 把 0.3 的平台策略门实测结果写进本文件备注（哪些平台本改动真实生效、哪些无效）。
- [ ] 4.3 `openspec validate harden-edge-blocker-and-session-axis --strict` 通过。

## 5. 集成与部署

- [ ] 5.1 合回 `aidcp-edge` 默认分支前先 fetch + rebase，跑验收 + typecheck 再 ff 合并；push 遇非快进一律 rebase 重来、绝不 force。
- [ ] 5.2 本 change **不含**桌面安装包出包（用户长期授权：默认不打包）。收尾到 commit / push 为止。
- [ ] 5.3 收尾说明写清：**本改动要到客户端重新编译装机后才生效**；且用户重编译**不抬版本号**，验收时 **MUST NOT** 用版本号判断装的是哪份代码（见 design R4）。
- [ ] 5.4 回滚口径：`git revert` 该提交即可，无残留状态、无需开关旗标（两处改动都不引入新配置）。
