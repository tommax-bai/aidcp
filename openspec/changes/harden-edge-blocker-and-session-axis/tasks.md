# Tasks

> **单仓 `aidcp-edge`，零协议改动、零云端改动。**
> **开工前置**：本 change 与活跃 change `browser-slot-scheduling` 共享 Electron 外壳的状态推导块，
> **须串行、不与其并行开工**（CLAUDE.md §7 热点文件单写者）。开工前确认该 change 无人在改同一段。

## 0. 开工前取证复核（登记比代码旧，先验缺陷还在）

- [x] 0.1 确认阻断浮层标记的清除条件里**仍然**含「浏览会话结束」这一支；若已被他人修掉，停手并把本 change 改为只做第 2 组。
- [x] 0.2 确认现役 Native 引擎**两个方向都在发**结构化阻断事件（检出与复检解除各一处），且解除那处与「通知云端阻断已解除」在同一函数内先后发出。这是「摘掉后不会永久卡住」与「两侧同源」的全部依据。
- [x] 0.2b **确认「浏览会话结束」这一支当前是否真的会触发**——追到该 UI 事件的全部产出点，再追到其日志发射点，并对照 `scripts/native-engine-inventory.cjs` 的退役名单判断该发射点是否已被剪出生产构建。**初稿设计在这一步上判错过一次**（把「阻断事件有写入方」错推成「清除支会触发」）。结论决定本 change 的性质表述与 5.3 的收尾说明，**MUST NOT 跳过**。
- [x] 0.3 记录各在产平台的陪伴 UI 事件策略门实际取值（design R1）：哪些平台会真发这两个事件。**只登记、不在本 change 扩大覆盖**，结论写进本文件备注。
- [x] 0.4 确认外壳今天**确实不解析**引擎的结构化会话行（全仓零命中），且引擎侧发射点仍在。

## 1. 阻断浮层：只认显式解除

- [x] 1.1 从阻断浮层标记的清除条件中移除「浏览会话结束」一支；显式解除事件保持为唯一清除路径。**MUST NOT** 改为「附加复检条件后仍允许清除」（design D1 已否决该替代方案）。
- [x] 1.2 复核该标记的其余写入点：环境启停等重置路径写「假」是正确的（新会话开始时确实无已知阻断），**保留**；逐一确认没有第二条「运行态迁移顺带清除」的旁路残留。
- [x] 1.3 复核该标记的下游消费方（含冷待机准入的阻断否决、环境栏「需要处理」排序与计数），确认语义收紧后无消费方依赖旧的宽松清除行为。

## 2. 会话轴：改由结构化会话事件驱动

> ⚠️ **顺序铁律**：第 1 组 MUST 先于本组落地，且**中间任何一个提交点都不得只含本组**。
> 本组正是在为「会话结束」造一个活信号源——第 1 组未拆雷就接线，等于亲手引爆（design D4）。

- [x] 2.1 外壳解析器新增对引擎结构化会话行的处理，产出会话开始 / 结束两类事件。匹配 **MUST** 落在引擎侧 token 化字段上，**MUST NOT** 使用自然语言措辞或宽松正则（design D2/R3）。
- [x] 2.2 会话运行态投影改由 2.1 的事件驱动，取代已退役路径的中文日志推断；清理该轴上已无写入方的死规则。
- [x] 2.3 **仅改界面显示**：确认该轴未被接回冷待机准入闸，也未被接入任何新的裁决路径。
- [x] 2.4 确认与既有 posture 覆盖层的先后关系不变（覆盖层仍最后生效且赢），本改动 **MUST NOT** 改变该顺序语义。

## 3. 回归

- [x] 3.1 **定向回归（本 change 主断言）**：环境处于阻断态 → 引擎发出会话结束事件、且**未**发出解除事件 → 阻断态仍在，且冷待机准入仍判该环境不可让位。
- [x] 3.2 反例配对：环境处于阻断态 → 引擎发出**显式解除**事件 → 阻断态清除。证明 3.1 不是把闸焊死。
- [x] 3.3 会话轴定向回归：只喂结构化会话行（不含任何退役路径的中文措辞）→ 会话运行态投影正确变化。
- [x] 3.4 **写入方存在断言**（沿用 `admit-browser-standby-on-live-facts` D2 做法）：从外壳解析器抽出会话轴所用的全部匹配串，逐个到引擎源码中找到发射点。**MUST NOT 省略**——这条断言正是那次事故的机械化教训。
- [x] 3.5 **结构断言**：冷待机准入的输入集合中不含会话运行态轴（对应新增规格场景「会话轴接上活写入方之后仍不得进入准入闸」）。
- [x] 3.6 变异自查：把「浏览会话结束」这一支加回清除条件，确认 3.1 真的会红；把会话轴加回准入输入，确认 3.5 真的会红。**两条都要做**（防恒真断言）。
- [x] 3.7 `cd ../aidcp-edge && npm run test:acceptance && npm test && npm run typecheck` 全绿。已知既有 flaky：原生引擎会话恢复那条契约测试偶发管道错，**在未含本改动的默认分支上即可复现**，重跑即可，**不要去改自己的代码找原因**。

## 4. 控制仓收尾

- [x] 4.1 本文件按 sub-repo 回写 commit-sha，格式 `<!-- <repo> <commit-sha> 备注 -->`；sha **必须取自已推送的提交**。
- [x] 4.2 把 0.3 的平台策略门实测结果写进本文件备注（哪些平台本改动真实生效、哪些无效）。
- [x] 4.3 `openspec validate harden-edge-blocker-and-session-axis --strict` 通过。

## 5. 集成与部署

- [x] 5.1 合回 `aidcp-edge` 默认分支前先 fetch + rebase，跑验收 + typecheck 再 ff 合并；push 遇非快进一律 rebase 重来、绝不 force。
- [x] 5.2 本 change **不含**桌面安装包出包（用户长期授权：默认不打包）。收尾到 commit / push 为止。
- [x] 5.3 收尾说明写清：**本改动要到客户端重新编译装机后才生效**；且用户重编译**不抬版本号**，验收时 **MUST NOT** 用版本号判断装的是哪份代码（见 design R4）。
- [x] 5.4 回滚口径：`git revert` 该提交即可，无残留状态、无需开关旗标（两处改动都不引入新配置）。

<!-- aidcp-edge eb89150 全部代码改动一次落地（拆雷与接线同提交，顺序：先拆后接）；验收 39/39、全量 3179（3178 pass / 1 gated skip）、typecheck 通过 -->
<!-- 0.2b 结论（更正了设计初稿）：「浏览会话结束」这一支**当时不会触发**——该 UI 事件全仓唯一产出点是壳侧那条中文正则，其日志发射点在 src/browse/browse-session.ts，该文件已列入 scripts/native-engine-inventory.cjs 的 RETIRED_DIST_MODULES、从生产构建剪除。故本 change 拆的是雷、不是漏；踩响它的正是本 change 的第 2 组。初稿把「阻断事件有写入方」错推成「清除支会触发」，proposal / design 已就地更正 -->
<!-- 0.3 平台策略门实测：Facebook emitsCompanionUi=true（阻断态真实可达）；小红书 emitsCompanionUi=false（不发陪伴界面事件，故阻断标记在 XHS 上恒假、第 1 组对其为空操作，属既有产品范围取舍、本 change 不扩大）。注意「通知云端阻断已解除 / 检出」两条**不受该策略门约束**，两平台都发——云端那道验证码否决因此在 XHS 上照常生效 -->
<!-- 第 2 组（会话轴）**两平台都生效**：引擎结构化诊断行由 diagnostic() 无条件打印，不经 emitsCompanionUi -->
<!-- 会话结束一律映射为 idle、不合成 resting：引擎四个停止原因（本地停手 / 云端结束 / 关闭 / 排空）都不表达「歇一会儿还会继续」，凭空造 resting 就是把观测不到的状态说成观测到了 -->
<!-- 3.6 变异自查双向都做过：加回「会话结束」清除支 → 2 条断言红（主断言 + 唯一清除路径断言）；把会话轴加回准入 → 3 条红（含既有两条定向回归）。首轮那条「唯一清除路径」断言曾被「同一行里再或一个条件」绕过，已改为按条件全等判后重验 -->
<!-- ⚠️ 变异还原踩过一次坑：用 git checkout -- <file> 还原，把本 change 自己对该文件的改动一并抹掉（该文件在 HEAD 上尚无本次改动）。已改用文件快照 cp 还原并复核三处改动都在。教训同 memory mutation-check-mv-restore-mtime-trap -->
<!-- 5.3 生效条件：本改动要到客户端**重新编译装机**后才存在；用户重编译**不抬版本号**，验收时不得用版本号判断装的是哪份代码 -->
<!-- 串行前置已核：browser-slot-scheduling 的 worktree 干净、最后提交 2026-08-04，未与本 change 并发改同一状态推导块 -->
