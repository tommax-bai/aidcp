## 1. aidcp-automation — 暂停带处置类别（传输层）

- [ ] 1.1 把传输层的边缘暂停集合从「一组边缘标识」改为「边缘标识 → 处置类别」（`needs_operator` / `time_only`），`pauseEdge` 接受类别、缺省按 `needs_operator`（旧调用点零行为变化）
- [ ] 1.2 下发闸逐字不变：任何类别都停发浏览 / 互动指令，`session.end` 与 `captcha.assist.*` / `task.*` / `ui.push_snapshot` 的既有豁免不动；补一条断言锁住「类别 MUST NOT 影响下发闸」
- [ ] 1.3 新增只读取值口「该边缘的暂停是否需要浏览器才能解除」，供待机提示消费；`isEdgePaused` 语义保持不变（仍答「有没有被暂停」）
- [ ] 1.4 补测：限流类暂停在受限处置策略为「只浏览」时仍停发指令（spec 场景「限流类暂停照样停发指令」）
- [ ] 1.5 补测：重连（`edge.hello`）后类别 MUST 与暂停一起存活，不被重置为 `needs_operator`

## 2. aidcp-automation — 阻断事故协调器按类别落账

- [ ] 2.1 事故处理时把既有的平台限流确认结果透传到暂停登记：命中限流判据 → `time_only`；`kind='captcha'` 或未命中判据的未知遮罩 → `needs_operator`
- [ ] 2.2 MUST NOT 新起分类器：类别只能由既有的限流文案确认结果推导，补一条测试锁住「同一份文案，告警类型与暂停类别必须同源」
- [ ] 2.3 秒级回滚旗标（默认开）：关闭后限流类一律登记为 `needs_operator`，行为逐位回到本 change 之前
- [ ] 2.4 补测：真验证码上报的暂停类别恒为 `needs_operator`，旗标开关都不改变这一条

## 3. aidcp-automation — 待机提示改问处置类别

- [ ] 3.1 待机提示的一票否决输入从「该边缘被暂停了吗」换成「该边缘的暂停需要浏览器才能解除吗」，取值口来自 1.3
- [ ] 3.2 验证受限 +「浏览也暂停」下限流类走既有的定时让位分支：携带按剩余冷却推导的恢复时刻、判为可让位（spec 场景「确认的平台限流按剩余冷却让位」）
- [ ] 3.3 补测：真验证码 / 登录墙 / 未归类未知遮罩仍一票否决、不让位（spec 场景「受限 + 验证码待解时不让位」），这条是红线回归
- [ ] 3.4 确认 `ui.push_snapshot` 的待机载荷字段零增删（协议兼容要求），并跑一次两份 protocol.ts 的漂移断言

## 4. aidcp-automation — 冷却到期的有界自动解除

- [ ] 4.1 自动恢复扫描器把账号从 `restricted` 翻回 `warned` 时，解除该账号名下 `time_only` 类别的暂停；`needs_operator` 类别 MUST NOT 被顺带解除
- [ ] 4.2 解除失败 MUST 记录并保留暂停态，MUST NOT 宣称已恢复下发（spec 场景「解除失败不得伪装成已解除」）
- [ ] 4.3 MUST NOT 引入独立的暂停过期时长；补一条测试锁住「解除只由风控恢复这一个事实驱动」
- [ ] 4.4 补测端到端：限流上报 → 受限 → 让出浏览器 → 满窗恢复 → 暂停解除 → 命令恢复下发（覆盖今天这条断掉的回路）
- [ ] 4.5 核对运营「解除受限」既有快路仍同时解除两类暂停，行为不变

## 5. aidcp-automation — 告警正文与处置对齐

- [ ] 5.1 正文「类别」行改取自限流确认结果，与标题、告警类型同源（消除「标题写限流、正文写未知遮罩」的自相矛盾）
- [ ] 5.2 已自动冷却的阻断：处置提示改写为「已按受限处置策略静默 + 浏览器已让出 + 恢复时刻 X」，删掉要求远程连接处置的文案
- [ ] 5.3 恢复时刻与待机提示、恢复扫描器同源推导；取不到时如实写「按受限处置策略自动冷却」，MUST NOT 臆造时刻
- [ ] 5.4 需人介入类别的告警文案逐字不变（云端协助入口 / 远程处置兜底说明），补一条回归断言
- [ ] 5.5 补测三条正文场景（类别一致 / 不索要远程处置 / 恢复时刻取不到不臆造）

## 6. 边界对账与回归

- [ ] 6.1 与活跃 change `release-browser-slot-on-stalled-blocker` 对账：逐条核对其「需人介入原因」取值，确认没有一个原因同时落进本 change 的 `time_only` 桶（两条各管各的一桶）
- [ ] 6.2 确认 `aidcp-edge` 零改动：既有让位、唤醒、遮罩观测均复用，不新增边缘侧机制
- [ ] 6.3 热点文件单写者确认：传输层暂停集合与阻断事故协调器是否与并行 change 冲突
- [ ] 6.4 `npm run test:acceptance` → `npm test` → `npm run typecheck` 全过；安全红线 `AC-PROTO-*` / `AC-RISK-*` 必须全绿
- [ ] 6.5 `openspec validate cool-down-facebook-throttle-block --strict`

## 7. 部署与真机验收

- [ ] 7.1 部署 dev（安全序列：target 确认 → 备份 → rsync → 重启 → healthcheck → 失败回滚），旗标默认开
- [ ] 7.2 dev 观察一次真实限流阻断：确认浏览器被让出、槽位释放、告警正文不再索要远程处置
- [ ] 7.3 真机验收项登记 backlog：冷却满窗（当前配置 48 小时）后浏览器自动醒、暂停自动解除、账号归队跑完一场
- [ ] 7.4 用户明确要求时才发 OL，且必须从发布分支执行
