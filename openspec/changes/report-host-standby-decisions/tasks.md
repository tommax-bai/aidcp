# Tasks

> **跨仓：`aidcp-edge` + `aidcp-cloud`（+ `aidcp-automation` 仅协议副本同步）。**
> ⚠️ **热点文件、须串行**：本 change 动 `protocol.ts`，属 CLAUDE.md §7 单写者文件。
> **开工前 MUST 重核协议热点是否空闲**（fleet 高度活跃，立项时的结论会过期）。

## 0. 开工前复核

- [ ] 0.1 重核协议热点空闲：edge / cloud 两侧**在开分支**（worktree 对应分支，按三点比较 `origin/master...HEAD`）无人改 `protocol.ts` 或动作映射表。**MUST NOT 用 `master..branch` 两点比较**——那会把大量早已合并的旧分支算成占用。
- [ ] 0.2 确认协议副本份数与对账门覆盖范围（当前为三份：edge / cloud / automation；`scripts/land-change` 逐字比对）。份数变了以门禁实跑为准，别抄本文件。
- [ ] 0.3 复核既有待机提示载荷的字段集合，作为「只增不减」的基线快照写进本文件备注。

## 1. 协议（三处同步 + 文档）

- [ ] 1.1 在 **三份** `protocol.ts` 中逐字一致地新增该 edge → cloud 消息类型与载荷类型。
- [ ] 1.2 新增握手能力位常量（**注意带版本后缀**，本仓有过手抄漏掉后缀导致能力静默不生效的前科）。
- [ ] 1.3 更新 `docs/protocol.md`：§2 表新增一行 + **同步头部消息计数**（人工维护、易滞后）。
- [ ] 1.4 确认**不需要**改动作↔消息映射表与边缘 onMessage 主动命令白名单（本消息是 edge → cloud 单向上报，两者都只对 cloud → edge 生效）；把这条判断写进备注，避免下一个人重新纠结。

## 2. 边缘：外壳判决 → 核心 → 云端（三跳）

- [ ] 2.1 外壳侧：在准入判决落定处产出回执事实（判决结果、具名原因、连续拒绝次数、首次拒绝时刻、对应提示标识）。**复用既有的连续拒绝记账**，MUST NOT 另起一套计数。
- [ ] 2.2 外壳侧节流：与本地日志留痕**同一套规则**（首次 / 原因变化 / 此后每 N 次）；**判决迁移（拒→让、让→拒）立即发、不受节流约束**。两处节流 MUST NOT 各写一份常量。
- [ ] 2.3 外壳 → 核心：新增一条本地消息并发送。
- [ ] 2.4 核心侧：新增**具名解析分支**并转发到云端。⚠️ 该处是逐类具名解析、非通配转发，**漏加分支的表现是静默不转发**（本仓同形状前科：命令到不了处理器、日志只显示已发送）。
- [ ] 2.5 能力位：边缘握手时声明；**未声明 ⇒ 不上报**，且该路径 MUST NOT 产生错误。
- [ ] 2.6 **回执失败 MUST NOT 影响准入**：上报异常、云端不可达、对端不支持——三种情形下让位判决与执行**逐字不变**。

## 3. 云端：接收、留存、呈现

- [ ] 3.1 新增消息处理，按环境 / 账号留存最近一次判决与连续拒绝态。
- [ ] 3.2 面板侧可读：**在运营所在的另一处能看出「某台机器上某个环境卡住了」**。这是本 change 的验收判据，不是「消息发出去了」。
- [ ] 3.3 **只读边界**：消费方 MUST NOT 出现在任何下发决策路径上（待机提示产出、命令下发、风控）。
- [ ] 3.4 未收到回执 MUST NOT 被云端当作异常或据以改变行为（旧客户端不声明能力是正常态）。

## 4. 回归

- [ ] 4.1 **端到端三跳断言**：外壳判决 → 核心转发 → 云端收到并可读。**MUST NOT** 只断言外壳发出——那正好放过链路唯一的静默失败点（核心侧漏配解析分支）。
- [ ] 4.2 中间跳漏配的定向反例：移除核心侧解析分支 ⇒ 端到端断言必须红。
- [ ] 4.3 节流断言：同因连续拒绝按节流上报（非逐跳）；**判决迁移立即上报**（定向回归，防节流吃掉状态变化）。
- [ ] 4.4 **结构断言：准入输入集合不因本 change 增加任何一项**；回执相关状态出现在准入里即红。
- [ ] 4.5 **结构断言：云端消费方不在任何下发决策路径上**（对应规格「可见性 MUST NOT 转化为否决权」）。
- [ ] 4.6 兼容断言：云端待机提示载荷字段集合**未减少任一项**（对 0.3 的基线快照）；缺字段时边缘判整条无效 —— 该行为本身也要有断言，证明这条红线不是空话。
- [ ] 4.7 能力位缺席路径：未声明能力的边缘不上报、云端不报错、让位照常。
- [ ] 4.8 变异自查：把 4.2、4.3（迁移立即发）、4.4 三条各破坏一次，确认对应断言真的会红（防恒真断言）。
- [ ] 4.9 两仓分别 `npm run test:acceptance && npm test && npm run typecheck` 全绿。安全红线必须全过：协议不漂移、未授权绝不静默发布、风控绝不自残。

## 5. 控制仓收尾

- [ ] 5.1 本文件按 sub-repo 分节回写 commit-sha，格式 `<!-- <repo> <commit-sha> 备注 -->`；sha 必须取自**已推送**的提交。
- [ ] 5.2 把 0.3 的字段基线、1.4 的判断结论写进备注。
- [ ] 5.3 `openspec validate report-host-standby-decisions --strict` 通过。

## 6. 集成与部署

- [ ] 6.1 **上线顺序不可反：云端先上，边缘后到。** 边缘先发而云端不认，消息会被当未知类型丢弃。
- [ ] 6.2 云端部署走 §5 安全序列（先 `scripts/deploy-target dev --check` → 备份 → rsync → 重启 → 健康检查 → 失败即回滚）；**绝不碰同机 isales**。
- [ ] 6.3 本 change **不含**桌面安装包出包（默认不打包）。收尾说明写清：**边缘侧要到客户端重新编译装机后才开始上报**，且**不得用版本号判断装的是哪份代码**。
- [ ] 6.4 回滚口径：云端停止消费即可；协议消息保留无害（边缘发、云端忽略）。**MUST NOT 靠删字段回滚**——删字段会让全部在跑客户端停止让位。
