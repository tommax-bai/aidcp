# Tasks

## 1. aidcp-edge — 「回通知首页」回到通知页并重报三栏

- [ ] 1.1 把 Native 小红书规则脚本里「回通知首页」分支的目的地从信息流改为通知页：不在通知页则经通知入口进入并等页面身份成立，已在通知页则直接沿用；终局观测改为通知首页三栏未读读数。
- [ ] 1.2 该分支的失败保持诚实分型（入口找不到 / 点击未生效 / 导航未确认 / 分类栏读不到），分类栏读不到时回未确认，MUST NOT 回全 0 三栏读数；动作名保持 `notification_back_home`（云端失败恢复入口按该名匹配）。

## 2. aidcp-edge — 修正被锁错的契约与补测

- [ ] 2.1 修正行为契约测试：原用例断言的是「回到信息流」，改为断言「回到通知页 + 终局是三栏读数」，并保留原有的「入口找不到 / 点不动 / 导航未确认」三态断言。
- [ ] 2.2 修正回执声明对账表里这条命令的合法产出（信息流卡片 → 通知首页读数）。
- [ ] 2.3 补一条用例：分类栏读不到时回未确认，且产出里不含任何三栏读数（守「不得用全 0 冒充已清零」）。

## 3. 构建与验证

- [ ] 3.1 重编 Native 引擎（规则脚本在构建期编进二进制，只改脚本不重编＝线上行为不变），确认构建脚本的 rerun 触发到位。
- [ ] 3.2 跑 edge `npm run test:acceptance`、`npm test`、`npm run typecheck`，全绿。
- [ ] 3.3 变异验证：把修好的分支临时改回「导航到信息流」，确认 2.1 / 2.2 的用例真的红——闸恒真通过就等于没有闸。

## 4. 收口

- [ ] 4.1 `openspec validate restore-notification-home-return --strict` 通过。
- [ ] 4.2 合回 edge `master` 并推送；控制仓 change 与 tasks.md 回写 sha（sha 必须取自已推送的提交）。
- [ ] 4.3 登记真机验收项到 `docs/real-machine-acceptance-backlog.md`：一趟巡视要走完「评论和@ → 回通知首页 → 赞收藏 → 回通知首页 → 新增关注 → 三栏清零 → 回信息流」，并确认「赞收藏 / 新增关注看一眼是否真清零」。桌面安装包不在本 change 收尾范围（按 CLAUDE.md §6，打包需用户显式触发）。
