# Tasks

> 进度回写格式：`<!-- <repo> <commit-sha> 备注 -->`，部署后追加 `<!-- <date> deployed -->`。sha 必须取自**已推送**的提交。
>
> ⚠️ 本变更**具名放弃**两条已并入主 spec 的安全保证（`group-chat-injection` 与 `facebook-group-comment-coverage` 的「缺联系方式 fail-closed」）。放弃是运营的显式决定，范围严格限定为 Facebook 规则模式的加群联系评论一条链路。任何把降级扩到其他入口的改动都超出本变更授权。

## 1. 前置闸（控制仓，开工前必须全过）

- [ ] 1.1 确认 `facebook-rule-mode-cadence` 已归档、`facebook-rule-mode` 能力已并入 `openspec/specs/`
- [ ] 1.2 与 `facebook-rule-mode-without-persona` 对齐落地顺序（同改规则轮次评论段语义；两者不冲突，一个管正文来源、一个管联系方式缺失）
- [ ] 1.3 与 `facebook-rule-mode-two-tier-cadence` 对齐：两者都改规则轮次的动作结果表达，确认后落地的一方合并而非覆盖
- [ ] 1.4 跑 `scripts/task-preflight` 与 `scripts/new-change`，在 `../aidcp-cloud.wt/facebook-rule-comment-plain-fallback` 建 worktree

## 2. aidcp-cloud — 拆分「请求意图」与「实际注入」

- [ ] 2.1 把「本次实际是否注入联系方式」提升为独立事实，从闸一路带到审批、回执、通知与结果映射；MUST NOT 继续用请求意图代表实际
- [ ] 2.2 命令式评论任务机器入口的联系方式闸改为按显式降级意图分流；**默认值 MUST 保持 fail-closed**
- [ ] 2.3 撰写后、人审前的第二道联系方式闸同步按显式意图分流，并产出可辨识的「已降级」事实带进审批与审计
- [ ] 2.4 定向触发路径的同构闸**保持 fail-closed**，不接受降级意图

## 3. aidcp-cloud — 规则模式触发处

- [ ] 3.1 规则模式触发处显式传入降级意图（仅当账号无联系方式）
- [ ] 3.2 降级时审批模式来源改取**普通评论**的配置，MUST NOT 沿用联系评论配置
- [ ] 3.3 账号级全局免审对降级产出按普通评论车道判定，MUST NOT 由联系评论车道的授权释放
- [ ] 3.4 规则轮次结果映射新增可辨识的「降级普通评论已确认」终态，与「联系评论已确认」区分
- [ ] 3.5 确认降级后加群会真实下发：加群的实时风控闸与会话预算首次真正生效，记录其对加群日配额的实际消耗

## 4. aidcp-cloud — 对外表述口径统一

- [ ] 4.1 触发回执改按实际注入值渲染，新增「已降级为普通评论」话术
- [ ] 4.2 合并结果卡改按实际注入值渲染；MUST NOT 再对不含联系方式的评论宣称「带联系方式（服务器已确认）」
- [ ] 4.3 免审通知的联系方式标记与上两处统一到实际值
- [ ] 4.4 终态原因话术：为「降级已发出」补一条，同时保留 fail-closed 路径的原话术
- [ ] 4.5 核对人审卡（本就按实际值渲染）与上述三处一致，同一次动作不得出现互相矛盾的表述

## 5. aidcp-cloud — 开关侧提示

- [ ] 5.1 规则模式开关写入对无联系方式账号**不硬拒**，回读中带出具名说明「该账号将降级为普通评论」
- [ ] 5.2 说明按读时服务端真值派生，MUST NOT 作为配置值缓存在客户端

## 6. aidcp-cloud — 测试

- [ ] 6.1 改写既有两条 fail-closed 单测为「按显式意图分流」，**MUST 保留默认 fail-closed 分支的断言**
- [ ] 6.2 新增（范围锁定，最关键）：飞书手动 `--contact`、内容排期、精选定向、引流热帖、委托任务五个入口对无联系方式账号仍 fail-closed；任何人给它们打开降级都会打红
- [ ] 6.3 新增：降级产出的评论按普通评论车道解析审批模式
- [ ] 6.4 新增：账号「联系评论免审 + 普通评论需人审」时，降级产出走人审
- [ ] 6.5 新增：降级后结果卡不得宣称带联系方式；人审卡与结果卡口径一致
- [ ] 6.6 新增：账号既无模板、群组也无区域模板时，降级仍以具名原因诚实停止，不产出任何默认文案或生成式正文
- [ ] 6.7 新增：规则轮次投影能区分联系评论与降级普通评论，重启后仍可区分
- [ ] 6.8 `npm run test:acceptance` → `npm test` → `npm run typecheck` 全绿；`AC-PUB-*` 与 `AC-RISK-*` 必须全过

## 7. aidcp-console

- [ ] 7.1 规则轮次评论段展示容纳「降级普通评论」这一结果；枚举漂移会整页白屏，MUST 同步
- [ ] 7.2 账号页/自动化视图展示「无联系方式将降级」的提示
- [ ] 7.3 前端测试与 typecheck 全绿

## 8. aidcp-edge

- [ ] 8.1 确认 JS 评论执行路径零改动（联系方式本就「有值才追加」，协议字段本就可选）
- [ ] 8.2 **单独核验原生页面引擎路径**：其正文与联系方式的拼接方式与 JS 路径不等价，且该路径正在切换中；MUST NOT 以一条通过代表另一条

## 9. 集成、部署与验收

- [ ] 9.1 各仓 rebase 到最新默认分支、跑全量测试后 ff 合并
- [ ] 9.2 按部署安全序列部署 DEV
- [ ] 9.3 DEV 验证：无联系方式账号能真实入群并发出普通评论，或诚实停在缺模板
- [ ] 9.4 DEV 验证：结果卡不宣称带联系方式；人审卡与结果卡一致
- [ ] 9.5 DEV 验证：飞书手动 `--contact` 对无联系方式账号仍 fail-closed
- [ ] 9.6 记录降级对加群日配额的实际消耗
- [ ] 9.7 真机验收项收拢进 `docs/real-machine-acceptance-backlog.md`

## 10. 控制仓回写与归档

- [ ] 10.1 各 task 标 `[x]` 并写 `<!-- <repo> <sha> 备注 -->`，部署后追加 `<!-- <date> deployed -->`
- [ ] 10.2 在本文件显式登记：本次放弃了哪两条已上线保证、由谁决定、范围边界
- [ ] 10.3 `openspec validate facebook-rule-comment-plain-fallback --strict`
- [ ] 10.4 归档，删除 worktree 与分支
