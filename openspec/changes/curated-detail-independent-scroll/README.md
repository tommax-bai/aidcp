# curated-detail-independent-scroll

Keep the shorter side of the desktop client's curated-content detail view pinned at its scroll boundary while the longer side continues, with bounded trailing whitespace and a single-column responsive fallback.
