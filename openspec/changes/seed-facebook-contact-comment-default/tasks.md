# Tasks: seed-facebook-contact-comment-default

> **前置绑定**：本 change 依赖排期联系评论**已不再携带「先加群」标记**。
> 若那条标记被加回，本 change 的种入默认值 MUST 同时撤回——两者是一对前提与结论。

## 1. aidcp-cloud

- [ ] 1.1 种入默认值常量新增联系评论：模式 `auto_approve`、日上限 5。发帖 / 评论 / 加群三项取值不动。
- [ ] 1.2 常量注释同步：把原来「联系评论刻意缺席」那段改写成「前置已解除 + 若标记被加回则必须撤回」的绑定说明，保留因果、不要删成一句默认值。

## 2. aidcp-cloud — 测试

- [ ] 2.1 断言种入取值含联系评论（免审 / 5），且发帖仍需人审、评论仍免审。
- [ ] 2.2 断言联系评论日上限不超过其**自身**硬上限 10（该硬上限刻意与发帖 / 评论的 50 分开，不得相互推导）。
- [ ] 2.3 原有那条「种入取值里不含 contact 键」的断言需**反向改写**——它是上一个 change 刻意留下的守卫，本次前置解除后必须连同理由一起更新，不能默默删掉。
- [ ] 2.4 跑 `npm run test:acceptance` → `npm test` → `npm run typecheck`。

## 3. 部署与验收（dev）

- [ ] 3.1 部署 cloud 到 dev（控制仓 §5 安全序列）。无数据库改动。
- [ ] 3.2 复核存量计数零波及：已种入账号的联系评论应仍为关闭（本 change 只影响此后新登记的账号）。
- [ ] 3.3 下一个真实新号登记时确认：排期行里联系评论为免审 / 5。

## 4. 交接说明

- [ ] 4.1 写明：新号在配上联系方式之前，联系评论每个槽位会诚实不发并回提示卡；这是 fail-closed 的正确表现，处置是去账号页补联系方式。
- [ ] 4.2 写明：联系评论与普通评论**共用同一个评论风控配额**，两者竞争同一池子；要真正放开需在后台抬评论日额。
- [ ] 4.3 写明：日上限 5 是设计判断而非用户指定，改它只需动一处常量。
